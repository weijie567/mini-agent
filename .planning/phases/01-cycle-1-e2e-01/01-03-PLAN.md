---
phase: 01-cycle-1-e2e-01
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - docs/implementation/e2e01-thin-slice-implementation-spec.md
autonomous: true
requirements:
  - E2E01-01
  - E2E01-04
user_setup: []
must_haves:
  truths:
    - "Thin Slice 第 10.1 节只有且恰好 17 个 canonical minimum-persistence item；ModelVisibleToolsetArtifact 在集合内，辅助模型、Command 与 write-result enum 不得计入。"
    - "每个 item 都有唯一、稳定的 scoped record_code 与 exact P0 record_schema_version，并保持 record_schema_version、state_version、artifact_schema_version、tool_registry_version 与 Eval version_manifest 互不替代。"
    - "每个 item 都明确 semantic owner、当前 source model / artifact、逻辑 identity、direct owner 或 owner-root proof、required link metadata 与专项 version 字段边界。"
    - "P0 persistence API 是恰含 17 项的 closed registry 与 strict logical envelope codec；source DTO 缺失的受控 external link projection 与三类 logical child 具有封闭输入格式，执行者不得自行发明。"
    - "unknown/missing/mismatch、registry confusion、owner/link/child projection mismatch 或完整 JSON payload validation failure 均 fail closed；UUID / datetime round-trip 使用 Pydantic strict JSON validation，不误用 strict Python validation。"
    - "01-04 只实现 Application integration-boundary mapping/codec 与 focused tests，不修改 Core、Tool、Intent、Memory、Eval DTO / Port，不定义 table、column、index、JSONB mapping 或 Alembic migration。"
    - "本 Plan 与 owner PR 都不声称 persistence Adapter、业务表、startup recovery、migration 或纵向切片已经实现。"
  artifacts:
    - "docs/implementation/e2e01-thin-slice-implementation-spec.md contains the unique 17-item scoped logical persistence mapping."
    - "docs/implementation/e2e01-thin-slice-implementation-spec.md contains the strict codec / closed registry contract."
    - "docs/implementation/e2e01-thin-slice-implementation-spec.md fixes the exact two-file Plan 01-04 implementation allowlist."
  key_links:
    - "Thin Slice consumes PROJECT_DIRECTION.md section 9.1 four-axis ownership, five version dimensions and Trace shared-structure approval."
    - "Thin Slice consumes Memory section 15.2 exact-version, integrity failure, owner graph, recovery readiness and migration runtime boundaries."
    - "Plan 01-04 maps this owner contract only to src/mini_agent/application/persistence.py and tests/component/application/test_persistence_contract.py."
---

<objective>
在 Thin Slice scoped owner 范围内冻结第 10.1 节当前 17 项最低持久化集合的 item code、exact P0 logical record version、source / semantic-owner mapping、identity / owner / link metadata，以及 strict codec / closed registry API。

Purpose: 让 01-04 可以在不修改 specialized-owner DTO、Port 或 physical persistence 的前提下，实现一个唯一、封闭且 fail-closed 的 Application integration-boundary mapping / codec。

Output: 只修改 `docs/implementation/e2e01-thin-slice-implementation-spec.md`；不修改 Tool、Intent、Memory、Eval、Project Direction、源码、测试、Alembic、Infrastructure 或 `.planning/`。
</objective>

<execution_context>
@AGENTS.md
@.planning/GOVERNANCE.md
@.planning/phases/01-cycle-1-e2e-01/01-02-SUMMARY.md
@docs/implementation/e2e01-thin-slice-multi-agent-plan.md

Stock `gsd-import`、`gsd-plan-phase`、`gsd-execute-phase`、`gsd-verify-work`、自动 lifecycle mutation 与 `gsd-ship` 均不得调用。Integrator 已在 workflow 外从 exact integration base 预建 Task Packet Worktree / branch；执行者只能写本 Packet 的单文件 allowlist。
</execution_context>

<context>
@PROJECT_DIRECTION.md
@docs/architecture/memory-design-reference.md
@docs/architecture/intent-design-reference.md
@docs/architecture/tool-calling-design-reference.md
@docs/evaluation/agent-evaluation-strategy.md
@docs/evaluation/p0-eval-coverage-matrix.md
@docs/implementation/e2e01-thin-slice-implementation-spec.md

Evidence-only, not canonical:

@src/mini_agent/application/records.py
@src/mini_agent/application/ports.py
@src/mini_agent/core/task_state.py
@src/mini_agent/core/tool_system.py
@src/mini_agent/core/memory.py
@src/mini_agent/core/trace.py
@src/mini_agent/infrastructure/persistence/models.py
@tests/component/application/test_record_contracts.py
@tests/component/application/test_ports_contract.py
@tests/integration/test_database_migrations.py
</context>

<interfaces>
当前 source DTO / Port 只作为 implementation evidence，不得升级为第二套 canonical owner。

- `ConversationRecord`、`MessageRecord`、`ConversationTaskLinkRecord`、`RunTaskLinkRecord`、`EvalResultRecord`、`EvalExecutionFailureRecord` 位于 `src/mini_agent/application/records.py`。
- `RequestUnderstandingRecord`、`TaskRecord`、`RequestUnitRecord`、`InputBinding` 位于 `src/mini_agent/core/task_state.py`。
- `ModelVisibleToolsetArtifact`、`GateDecision`、`ToolCallRecord` 位于 `src/mini_agent/core/tool_system.py`。
- `OrderObservation`、`ContextManifest` 位于 `src/mini_agent/core/memory.py`。
- `AgentRunRecord`、`TraceEvent` 位于 `src/mini_agent/core/trace.py`。

当前 Infrastructure 除 PostgreSQL / pgvector bootstrap 外没有业务 metadata。不得从空实现推断 table、column、JSONB、transaction 或 migration 设计。
</interfaces>

<task_packet>

repository: `https://github.com/weijie567/mini-agent`
remote: `origin`
head_branch: `codex/e2e01-01-thin-slice-persistence-mapping`
base_branch: `integration/e2e01-thin`
base_sha: `af5afd2c93d429e1b090bfaf7af22c0fc4ec3c7b`
worktree_id: `e2e01-01-thin-slice-persistence-mapping`
writer: `Thin Slice scoped contract writer as the sole writer, supervised by /root Integrator`

owned_files:

- `docs/implementation/e2e01-thin-slice-implementation-spec.md`

forbidden_files:

- every repository path not listed in `owned_files`
- `.planning/**`
- `AGENTS.md`
- `PROJECT_DIRECTION.md`
- `README.md`
- `docs/business-capabilities.md`
- `docs/architecture/**`
- `docs/evaluation/**`
- `src/**`
- `tests/**`
- `evals/**`
- `alembic/**`
- `pyproject.toml`
- `uv.lock`
- `compose.yaml`
- `graphify-out/**`

canonical_inputs:

- `AGENTS.md` sections 1 and 3–8
- `PROJECT_DIRECTION.md` section 9.1 at exact `base_sha`
- `docs/architecture/memory-design-reference.md` section 15.2 at exact `base_sha`
- current Thin Slice Spec section 10.1 at exact `base_sha`
- Tool / Intent / Eval canonical owners as read-only semantic constraints
- existing DTO / Port / tests only as implementation evidence

dependencies:

- Plan 01-02 planning merge `58f54f226fe5493457fe2806cb494f5b62ed0209`
- Plan 01-02 reviewed content head `f32306f4552e3d0b235ec68de09ab8554e4d7632`
- PR #14 reviewed remote head `b50038f9ce8398cd01289d38aeec09a183b68692`
- PR #14 owner merge / exact execution base `af5afd2c93d429e1b090bfaf7af22c0fc4ec3c7b`
- the planning-status PR containing this Plan and `01-02-SUMMARY.md` must merge before writing starts
- because the execution branch is intentionally forked before that planning-status PR, execution must resolve the fresh integration ref, prove the planning merge is the sole commit after `base_sha`, and read this Plan through captured Git objects
- precheck must prove the planning merge changes exactly the eight declared planning-status files and leaves `docs/implementation/e2e01-thin-slice-implementation-spec.md` byte-unchanged from `base_sha`

planning_provenance:

- `planning_merge_sha`: `TO_BE_CAPTURED_FROM_OFFICIAL_INTEGRATION_REF_AFTER_PLANNING_STATUS_PR_MERGE`
- `planning_plan_blob`: `TO_BE_CAPTURED:${planning_merge_sha}:.planning/phases/01-cycle-1-e2e-01/01-03-PLAN.md`
- `planning_summary_blob`: `TO_BE_CAPTURED:${planning_merge_sha}:.planning/phases/01-cycle-1-e2e-01/01-02-SUMMARY.md`
- these placeholders are not executable values; unresolved provenance is `BLOCK`

required_checks:

- branch / exact base HEAD / merge-base / clean-worktree precheck
- official GitHub ref/commit/tree fail-closed provenance when smart HTTP is unavailable
- exact planning merge SHA / Plan blob / Summary blob and eight-file containment
- current-integration unchanged precheck for the one owned file
- exactly one feature commit and exact one-file allowlist
- exactly 17 declared record codes and 17 exact versions
- all record codes and `(record_code, record_schema_version)` pairs unique
- `ModelVisibleToolsetArtifact` occurs exactly once in the canonical mapping
- no auxiliary model, Command, write-result enum or “20-record” count
- strict codec / closed registry and bounded integrity-failure API contract
- exact external-reference rules and exactly three non-top-level logical-child specs
- exact two-file 01-04 implementation allowlist
- `git diff --check`, Markdown link check and repository cross-file impact inventory
- canonical serial offline command `uv run pytest`; no canonical lint, type-check or build command exists
- independent exact-head review before PR readiness / merge

done_when:

- both tasks complete in the sole owner file
- one-file / one-commit provenance and all mechanical checks pass
- canonical serial tests pass
- independent exact-head review has no unresolved `CRITICAL / HIGH / MEDIUM`
- feature PR uses the repository template, starts as draft and targets `integration/e2e01-thin`
- Plan 01-04 is not treated as executable until this owner PR merges

contract_changes: `YES / CONTRACT ONLY` — freezes the scoped 17-item logical record map and 01-04 mapping / codec boundary without changing specialized payload/state semantics or implementing persistence.

security_impact: `YES` — prevents type confusion, version downgrade, persisted-owner privilege expansion, owner-graph tampering, partial decode and unsafe diagnostic disclosure.

eval_impact: `YES / CONTRACT ONLY` — freezes Eval record entries while preserving Eval `version_manifest` as an independent dimension; creates no Eval result, metric, threshold, Case status or lifecycle mutation.

rollback: Before merge, close the PR. After merge, use a normal revert PR and re-block 01-04; never reset / force-push protected branches, delete stored data or perform a migration as rollback.

handoff_to: `Integrator`

handoff_format:

- repository / remote / branch / worktree
- `base_sha`, planning provenance SHA and blobs
- exact feature head and every commit SHA
- actual changed files and allowlist proof
- commands and exact results
- 17-entry code/version inventory
- strict codec / registry contract and 01-04 exact allowlist
- contract / security / Eval impact
- cross-file impact inventory
- unresolved risk / deviation / rollback

</task_packet>

<threat_model>

Trust boundaries:

- storage metadata → closed registry: untrusted persisted code/version must resolve to one exact approved spec;
- persisted owner/link projection → trusted runtime graph: stored identifiers can be compared but never grant customer scope or recovery authority;
- envelope payload → source model: full strict validation must precede use as a domain record;
- specialized payload version → logical record version: artifact/state/tool/Eval versions cannot substitute for `record_schema_version`;
- logical contract → Infrastructure: physical access cannot invent code, version, compatibility or migration semantics;
- integrity diagnostics → Trace / model / HTTP: internal category remains diagnosable without raw payload or resource-existence disclosure.

STRIDE register:

| threat_id | category | component | disposition | violation_gate | mitigation_plan | file_to_check |
|---|---|---|---|---|---|---|
| `TS-PERSIST-S01` | Spoofing | record code → source-model registry | `MITIGATE` | `BLOCK` | closed registry contains exactly 17 statically imported source models; unknown code, duplicate code, dynamic registration or code/model mismatch is integrity failure | `docs/implementation/e2e01-thin-slice-implementation-spec.md` |
| `TS-PERSIST-T01` | Tampering | owner / link graph metadata | `MITIGATE` | `BLOCK` | each code has exact identity, owner strategy and required links; decoded data must match envelope projections; stored links never authorize | same |
| `TS-PERSIST-T02` | Tampering | logical version | `MITIGATE` | `BLOCK` | exact code/version pair only; missing, unknown, upgrade, downgrade or fallback is rejected; other version dimensions cannot substitute | same |
| `TS-PERSIST-E01` | Elevation of Privilege | customer scope | `MITIGATE` | `BLOCK` | only `CustomerContext`-derived `TrustedOwnerScope` authorizes; persisted owner data is validation input, never authority | same |
| `TS-PERSIST-I01` | Information Disclosure | codec diagnostics | `MITIGATE` | `BLOCK` | bounded category and correlation reference only; no raw payload, customer identity, token, prompt, cookie, secret or resource-existence detail | same |
| `TS-PERSIST-D01` | Denial of Service | invalid recovery candidate | `MITIGATE` | `BLOCK` | preserve Memory fail-closed readiness; codec never skips, coerces, migrates or claims invalid records | same |
| `TS-PERSIST-R01` | Repudiation | registry/version evolution | `MITIGATE` | `BLOCK` | future changes require owner approval, source/target, audit and rollback; no runtime self-registration or read-time migration | same |
| `TS-PERSIST-T03` | Tampering | artifact / Tool / Eval version confusion | `MITIGATE` | `BLOCK` | `artifact_schema_version`, `tool_registry_version` and Eval `version_manifest` remain independent and cannot satisfy record-version gate | same |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 冻结 17-item code/version 与 identity-owner-link mapping</name>
  <files>docs/implementation/e2e01-thin-slice-implementation-spec.md</files>
  <read_first>AGENTS.md, PROJECT_DIRECTION.md, docs/architecture/memory-design-reference.md, docs/architecture/intent-design-reference.md, docs/architecture/tool-calling-design-reference.md, docs/evaluation/agent-evaluation-strategy.md, docs/implementation/e2e01-thin-slice-implementation-spec.md, src/mini_agent/application/records.py, src/mini_agent/core/task_state.py, src/mini_agent/core/tool_system.py, src/mini_agent/core/memory.py, src/mini_agent/core/trace.py</read_first>
  <action>
在 Thin Slice Spec 第 10.1 节扩展现有最低持久化表；canonical 集合仍只有一份、恰好 17 行。不得把 `AcceptedTaskDelta`、`CandidateValidationRecord`、`TaskStateTransition`、`ToolAttemptRecord`、Command、write-result enum 或其他辅助类型计入。

冻结以下 exact scoped mapping：

| item | `record_code` | `record_schema_version` | semantic owner | current source | logical identity | owner / required link metadata |
|---|---|---|---|---|---|---|
| `ConversationRecord` | `conversation_record` | `conversation_record.p0.v1` | Application | `ConversationRecord` | `conversation_id` | direct `owner_customer_id` |
| `MessageRecord` | `message_record` | `message_record.p0.v1` | Application | `MessageRecord` | `message_id` | `conversation_id -> conversation_record` |
| `RequestUnderstandingRecord` | `request_understanding_record` | `request_understanding_record.p0.v1` | Request Understanding | `RequestUnderstandingRecord` | `run_id` | `run_id -> agent_run_record`; `message_ref -> message_record` |
| `TaskRecord` | `task_record` | `task_record.p0.v1` | Core Runtime / Task State | `TaskRecord` | `task_id` | direct `owner_customer_id`; `state_version` independent |
| `RequestUnitRecord` | `request_unit_record` | `request_unit_record.p0.v1` | Core Runtime / Task State | `RequestUnitRecord` | `request_unit_id` | `task_id -> task_record`; closed refs; `state_version` independent |
| `ConversationTaskLinkRecord` | `conversation_task_link_record` | `conversation_task_link_record.p0.v1` | Application | `ConversationTaskLinkRecord` | `(conversation_id, task_id, linked_at)` | Conversation / Task roots both required and decoded owners equal |
| `RunTaskLinkRecord` | `run_task_link_record` | `run_task_link_record.p0.v1` | Core Runtime / Task State | `RunTaskLinkRecord` | `(run_id, task_id)` | Run / Task roots; base/result state versions independent |
| `InputBindingRecord` | `input_binding_record` | `input_binding_record.p0.v1` | Request Understanding | current `InputBinding` | `binding_id` | source-derived Message / supersedes refs；external-required `request_unit_id -> request_unit_record` |
| `ModelVisibleToolsetArtifact` | `model_visible_toolset_artifact` | `model_visible_toolset_artifact.p0.v1` | Tool | `ModelVisibleToolsetArtifact` | `model_visible_toolset_hash` | content-addressed; no customer authority; artifact version independent |
| `AgentRunRecord` | `agent_run_record` | `agent_run_record.p0.v1` | Core Runtime | `AgentRunRecord` | `run_id` | customer Run uses `conversation_id -> conversation_record`; null conversation cannot establish owner proof |
| `GateDecisionRecord` | `gate_decision_record` | `gate_decision_record.p0.v1` | Tool / Gateway | current `GateDecision` | `gate_decision_id` | source-derived ContextManifest / InputBinding refs；`model_call_id` 是 payload correlation，后续与 Manifest 比对，不伪装成 top-level record ref |
| `ToolCallRecord` | `tool_call_record` | `tool_call_record.p0.v1` | Tool | `ToolCallRecord` | `tool_call_id` | Run / Task / RequestUnit / ContextManifest / GateDecision / InputBinding refs form one owner-consistent graph |
| `ObservationRecord` | `observation_record` | `observation_record.p0.v1` | Memory | first-slice `OrderObservation` | `observation_id` | external-required source ToolCall / Run / Task / RequestUnit refs；resource ref never authorizes |
| `ContextManifestRecord` | `context_manifest_record` | `context_manifest_record.p0.v1` | Memory | current `ContextManifest` | `context_manifest_id` | source-derived Run / Message / Task / Observation / Toolset refs；首切片 Evidence / Action refs 必须为空；registry version independent |
| `TraceEventRecord` | `trace_event_record` | `trace_event_record.p0.v1` | Core shared + specialized payload owners | current `TraceEvent` | `trace_event_id` | Run root and every populated registered-record ref resolve within one graph；model-call / presentation / child refs 保持专项 payload correlation |
| `EvalResultRecord` | `eval_result_record` | `eval_result_record.p0.v1` | Eval | `EvalResultRecord` | `(eval_run_id, case_id, lane, attempt)` | Eval-internal；`trace_ref` 按 Eval status matrix：PASS / FAIL 必填，SKIPPED / NOT_RUN 必须为空；`version_manifest` independent |
| `EvalExecutionFailureRecord` | `eval_execution_failure_record` | `eval_execution_failure_record.p0.v1` | Eval | `EvalExecutionFailureRecord` | `(eval_run_id, lane, case_id?, attempt?, failure_phase, safe_error_code, occurred_at)` | Eval-internal optional diagnostic / Trace refs; manifest independent |

同时明确：

1. `record_schema_version` 是 logical envelope dimension；version string 只在对应 `record_code` 下有意义，不能作为跨 item 的全局版本。
2. `ConversationRecord`、`MessageRecord`、`RequestUnderstandingRecord`、`ConversationTaskLinkRecord`、`RunTaskLinkRecord`、`EvalResultRecord` 与 `EvalExecutionFailureRecord` 当前已有的泛型 payload `schema_version`，在 P0 scoped persistence 中是 logical `record_schema_version` 的 mirror，必须精确等于该 item 的表列值；当前测试字符串不是 canonical exact version。
3. `artifact_schema_version`、`state_version`、`tool_registry_version` 与 Eval `version_manifest` 保持 specialized owner 语义，不能替代、推断或充当 mirror。
4. Direct / linked owner 都不能授权；可信 scope 仍来自 `CustomerContext`。Owner/link metadata 只是 strict comparison 与 graph-validation 输入。
5. 对 InputBinding、Observation、GateDecision 等不直接携带 owner-root link 的 source model，link 由 logical envelope metadata 提供；本 Plan 不修改 specialized DTO。
6. `AcceptedTaskDelta` 是 RequestUnderstanding logical child，`TaskStateTransition` 是 Task / RequestUnit audit child，`ToolAttemptRecord` 是 ToolCall logical child；它们可以使用物理 child storage，但没有独立 top-level `record_code` 或独立 `record_schema_version`。它们的结构变化分别推进父记录版本并服从对应 semantic owner。
7. `TraceEventRecord` 首版 code/version 不改变 shared structure；future shared / specialized migration 继续遵守 Project Direction 联合批准。
8. 不定义物理 table、column、index、JSONB、foreign key、transaction layout 或 migration。
9. 在 canonical 表前后分别写入唯一 marker `<!-- P0-PERSISTENCE-REGISTRY:START -->` 与 `<!-- P0-PERSISTENCE-REGISTRY:END -->`；marker 之间只有这一张 17-row registry 表，供精确机械验证使用。
10. Link projection 分成两类：source DTO 已携带的关系必须由 codec 重算，调用方不得覆盖；external-required 只有以下 exact token / target / cardinality：`InputBindingRecord` 恰好一个 `request_unit_id -> request_unit_record`；`ObservationRecord` 各恰好一个 `source_tool_call_id -> tool_call_record`、`source_run_id -> agent_run_record`、`source_task_id -> task_record`、`source_request_unit_id -> request_unit_record`。其他 external relation、重复 relation、错误 target code / identity 或 cardinality 均拒绝。
  </action>
  <verify>
    <automated>
set -euo pipefail
spec=docs/implementation/e2e01-thin-slice-implementation-spec.md
base_sha=af5afd2c93d429e1b090bfaf7af22c0fc4ec3c7b
node <<'NODE'
const fs = require("fs");
const spec = fs.readFileSync(
  "docs/implementation/e2e01-thin-slice-implementation-spec.md",
  "utf8",
);
const start = "<!-- P0-PERSISTENCE-REGISTRY:START -->";
const end = "<!-- P0-PERSISTENCE-REGISTRY:END -->";
if (spec.split(start).length !== 2 || spec.split(end).length !== 2) {
  throw new Error("registry markers must each occur exactly once");
}
const section = spec.slice(spec.indexOf(start) + start.length, spec.indexOf(end));
const rows = section
  .split("\n")
  .map((line) =>
    line.match(
      /^\| `([^`]+)` \| `([^`]+)` \| `([^`]+)` \| ([^|]+) \| ([^|]+) \| ([^|]+) \| ([^|]+) \|$/,
    ),
  )
  .filter(Boolean)
  .map((match) => ({
    item: match[1],
    code: match[2],
    version: match[3],
    owner: match[4].trim(),
    source: match[5].trim(),
    identity: match[6].trim(),
    links: match[7].trim(),
  }));
const expected = [
  ["ConversationRecord", "conversation_record"],
  ["MessageRecord", "message_record"],
  ["RequestUnderstandingRecord", "request_understanding_record"],
  ["TaskRecord", "task_record"],
  ["RequestUnitRecord", "request_unit_record"],
  ["ConversationTaskLinkRecord", "conversation_task_link_record"],
  ["RunTaskLinkRecord", "run_task_link_record"],
  ["InputBindingRecord", "input_binding_record"],
  ["ModelVisibleToolsetArtifact", "model_visible_toolset_artifact"],
  ["AgentRunRecord", "agent_run_record"],
  ["GateDecisionRecord", "gate_decision_record"],
  ["ToolCallRecord", "tool_call_record"],
  ["ObservationRecord", "observation_record"],
  ["ContextManifestRecord", "context_manifest_record"],
  ["TraceEventRecord", "trace_event_record"],
  ["EvalResultRecord", "eval_result_record"],
  ["EvalExecutionFailureRecord", "eval_execution_failure_record"],
];
if (rows.length !== 17) throw new Error(`expected 17 rows, got ${rows.length}`);
for (let index = 0; index < expected.length; index += 1) {
  const [item, code] = expected[index];
  const expectedVersion = `${code}.p0.v1`;
  const row = rows[index];
  if (
    row.item !== item ||
    row.code !== code ||
    row.version !== expectedVersion
  ) {
    throw new Error(`unexpected registry row ${index + 1}: ${JSON.stringify(row)}`);
  }
}
for (const key of ["item", "code", "version"]) {
  if (new Set(rows.map((row) => row[key])).size !== 17) {
    throw new Error(`${key} values must be unique`);
  }
}
if (
  new Set(rows.map((row) => `${row.code}\u0000${row.version}`)).size !== 17
) {
  throw new Error("code/version pairs must be unique");
}
for (const row of rows) {
  for (const key of ["owner", "source", "identity", "links"]) {
    if (!row[key]) throw new Error(`${row.item} has empty ${key}`);
  }
}
if (rows.filter((row) => row.item === "ModelVisibleToolsetArtifact").length !== 1) {
  throw new Error("ModelVisibleToolsetArtifact must occur exactly once");
}
for (const forbidden of [
  "AcceptedTaskDelta",
  "CandidateValidationRecord",
  "TaskStateTransition",
  "ToolAttemptRecord",
]) {
  if (rows.some((row) => row.item === forbidden)) {
    throw new Error(`${forbidden} must not be a top-level registry row`);
  }
}
NODE
test -z "$(
  git diff -U0 "$base_sha" -- "$spec" |
    rg '^\+.*(20-record|20 条记录|20 项记录)' || true
)"
rg -n 'record_schema_version|state_version|artifact_schema_version|tool_registry_version|version_manifest|owner_customer_id|owner.*graph|link.*metadata|external-required|logical child' "$spec"
git diff --check "$base_sha" -- "$spec"
    </automated>
  </verify>
  <acceptance_criteria>
    - canonical minimum-persistence set contains exactly the 17 declared items
    - all 17 code/version pairs are explicit and unique
    - each item defines semantic/source mapping, identity and owner/root links
    - the seven existing payload `schema_version` fields are exact mirrors, not a free second version
    - Eval Result Trace linkage remains conditional on the Eval-owned status matrix
    - auxiliary objects do not become item 18–20
    - no physical persistence or implementation claim is introduced
  </acceptance_criteria>
  <done>Thin Slice owner has one exact, closed and reviewable 17-item logical persistence inventory.</done>
</task>

<task type="auto">
  <name>Task 2: 冻结 strict codec / closed registry 与 01-04 implementation boundary</name>
  <files>docs/implementation/e2e01-thin-slice-implementation-spec.md</files>
  <read_first>AGENTS.md, .planning/GOVERNANCE.md, PROJECT_DIRECTION.md, docs/architecture/memory-design-reference.md, docs/implementation/e2e01-thin-slice-implementation-spec.md, docs/implementation/e2e01-thin-slice-multi-agent-plan.md, src/mini_agent/core/common.py, src/mini_agent/application/records.py, src/mini_agent/application/ports.py</read_first>
  <action>
在同一第 10.1 节固定 01-04 的 logical Application integration-boundary API；01-04 不得自行改名或扩大 scope：

- `P0RecordCode`: 恰含上述 17 个 value 的 `StrEnum`；
- `P0RecordReference`: relation、target code 与 target logical identity 的 immutable typed reference；
- `P0LogicalChildCode`: 恰含 `accepted_task_delta`、`task_state_transition`、`tool_attempt_record` 三个 value；它不是 `P0RecordCode`；
- `P0LogicalChildPayload`: child code、parent code / identity、child identity 与 Pydantic JSON data；child 无独立 record version，严格继承 parent code/version；
- `P0VersionedPayload`: 内层再次携带 code/version、Pydantic JSON `data` 与 typed logical children，用于检测 metadata / payload code-version / child tampering；child 是否能 local-closed 由 child spec 明确，不能证明时不得伪称完整；
- `P0PersistenceEnvelope`: Runtime-private logical envelope，携带 outer code/version、logical identity、direct-owner projection 或 owner-root refs、required link refs 与 `P0VersionedPayload`；不是 table schema；
- `P0RecordSchemaSpec`: frozen static spec，包含 code、exact version、direct source model class、identity projector、owner strategy、source-derived relation projector、external / conditional relation-cardinality rules、allowed child rules 与 optional specialized-version validator；Eval `trace_ref` 必须通过 conditional rule 消费 Eval status matrix；
- `P0_PERSISTENCE_REGISTRY`: immutable、恰含 17 项；source model 通过直接 import 绑定，不接受 module/class 字符串、不 dynamic import、不提供 `register()`、插件扩展或 fallback；
- `P0_LOGICAL_CHILD_SPECS`: immutable、恰含上述三类 child，分别静态绑定 `AcceptedTaskDelta` → RequestUnderstanding、`TaskStateTransition` → Task、`ToolAttemptRecord` → ToolCall，并固定 child identity、parent matcher 与 `LOCAL_CLOSED` / `GRAPH_REQUIRED` closure strategy；它不是第二个 top-level record registry；
- `P0PersistenceIntegrityCategory`: 至少覆盖 unknown / missing / mismatched code/version、metadata/payload mismatch、identity mismatch、owner projection mismatch、link / conditional-cardinality mismatch、child mismatch 与 payload validation failure；
- `P0PersistenceIntegrityError`: 只暴露 bounded category 和由可信 Runtime 生成的 opaque UUID correlation reference；不持有或格式化 raw payload、customer identity、Token、Prompt、Cookie 或 secret；
- `encode_persistence_record(record_code: P0RecordCode, record: ContractModel, *, external_references: tuple[P0RecordReference, ...] = (), logical_children: tuple[ContractModel, ...] = ()) -> P0PersistenceEnvelope`: 只接受已知 code 与 exact source model；direct owner 和 source-derived refs 必须从 record 重算，调用方只能提供 registry 明列的 external-required refs；children 必须匹配 closed child spec、parent identity 与父 payload refs；
- `decode_persistence_record(envelope: P0PersistenceEnvelope | Mapping[str, object] | str | bytes, *, expected_record_code: P0RecordCode, correlation_ref: UUID) -> DecodedP0PersistenceRecord`: 对 P0 model / JSON-compatible mapping 先转成 JSON bytes，对 raw JSON 直接使用；mapping 含非 JSON 值时拒绝，禁止 `default=str` 或其他 coercion；先分类 outer / inner code/version 与 registry，再以对应 source / child model `model_validate_json(..., strict=True)` 完整校验，随后重算并比较 identity、direct owner、source-derived / external links、children 与七个 mirror 字段；任一失败整体 fail closed；
- `DecodedP0PersistenceRecord`: 返回已验证 code/version、typed source record 与 typed logical children，但不授予 trusted scope，也不证明完整 owner graph 已闭合。

JSON round-trip 规则必须显式写入 owner contract：encode 使用 Pydantic JSON mode；decode 使用 `model_validate_json(..., strict=True)`，因为 UUID / datetime 的合法 JSON 表示是字符串。不得把 JSON-mode dict 交给 `model_validate(..., strict=True)`，也不得为使其通过而关闭 strict、预先构造 UUID / datetime 或宽松 coercion。

Child 校验也必须无歧义：

- `AcceptedTaskDelta` child identity 是 `accepted_delta_id`；RequestUnderstanding parent 的 `accepted_delta_refs` 与 children identity 必须一一对应且顺序无关，closure strategy 为 `LOCAL_CLOSED`。
- `TaskStateTransition` child identity 是 `(task_id, request_unit_id, result_state_version)`；`task_id` 必须等于 Task parent identity，每条 transition 继续满足 owner model 的单步 `base + 1 = result`，同一 envelope 内 identity / result version 唯一并按 result version 排序。Task parent 没有 transition refs，codec 不能证明完整历史，因此 closure strategy 必须为 `GRAPH_REQUIRED`；完整 cardinality / contiguous history 留给 01-05/01-06 record-graph gate，不得在 01-04 伪报为 local-closed。
- `ToolAttemptRecord` child identity 是 `(tool_call_id, attempt_no)`；`tool_call_id` 必须等于 ToolCall parent identity。`attempt_count=N` 时 children 必须恰为唯一连续的 `attempt_no=1..N`；`N=0` 时必须为空，closure strategy 为 `LOCAL_CLOSED`，每个 attempt 的 lifecycle 继续服从 Tool owner。

任何 locally provable 的缺失、多余、重复、wrong-parent、wrong-identity 或 child payload validation failure 均整体拒绝；`GRAPH_REQUIRED` 不能被 codec 的局部成功升级为完整 owner graph 证明。

边界：

1. Codec 不是授权器、Repository、Adapter、migration runner 或 recovery claimant。
2. Owner-scoped lookup、跨记录 graph closure、transactionally consistent recovery decode/claim 和 readiness 继续服从 Memory 15.2，在后续 Runtime / Infra Task Packet 实现。
3. Unknown/mismatch 不返回 `None`、partial object、latest model 或 fallback version。
4. Specialized version 继续由各 owner 校验；logical codec 只保证它们不能替代 record gate。
5. 01-04 exact owned files 只有：
   - `src/mini_agent/application/persistence.py`
   - `tests/component/application/test_persistence_contract.py`
6. 01-04 禁止其他所有路径，特别是 `application/records.py`、`application/ports.py`、`core/**`、`infrastructure/**`、integration tests、Alembic、toolchain、active docs 与 `.planning/**`。
7. 01-04 tests 必须覆盖 registry exactly 17 / bijection / immutability / no registration、17 positive JSON round trips（含 UUID / datetime）、missing/unknown/mismatch、outer/inner tampering、wrong source model、strict field failures、identity/owner/link/mirror mismatch、external relation 缺失/多余/重复/错误 cardinality、AcceptedDelta / ToolAttempt local-closed、TaskTransition graph-required 不误报、三类 child wrong-parent / wrong-identity / tampering、version substitution rejection、安全 error projection，以及 Toolset record/artifact version 双独立门禁。
8. 01-04 不新增 Alembic revision、不改 database metadata、不创建 table。Physical mapping / migration 留给 01-06；complete graph / fenced claim 留给 01-05/01-06。
9. Future logical version 先回到 semantic owner，定义 source/target、不变量、安全、审计、失败原子性和 rollback；普通 read/recovery 永不迁移。
10. 执行 repository cross-file impact scan；结果只进 handoff，不在本 owner branch 修改其他文件。
  </action>
  <verify>
    <automated>
set -euo pipefail
spec=docs/implementation/e2e01-thin-slice-implementation-spec.md
base_sha=af5afd2c93d429e1b090bfaf7af22c0fc4ec3c7b
for marker in \
  P0RecordCode P0RecordReference P0LogicalChildCode P0LogicalChildPayload \
  P0VersionedPayload P0PersistenceEnvelope P0_LOGICAL_CHILD_SPECS \
  P0RecordSchemaSpec P0_PERSISTENCE_REGISTRY \
  P0PersistenceIntegrityCategory P0PersistenceIntegrityError \
  encode_persistence_record decode_persistence_record \
  DecodedP0PersistenceRecord \
  accepted_task_delta task_state_transition tool_attempt_record \
  LOCAL_CLOSED GRAPH_REQUIRED attempt_count \
  source_tool_call_id source_run_id source_task_id source_request_unit_id \
  src/mini_agent/application/persistence.py \
  tests/component/application/test_persistence_contract.py
do
  rg -F -q -- "$marker" "$spec"
done
rg -n 'closed registry|恰含 17|external_references|logical_children|model_validate_json|dynamic import|register\(\)|fallback|strict=True|raw payload|授权|Alembic|physical|migration|rollback' "$spec"
test -z "$(
  git diff -U0 "$base_sha" -- "$spec" |
    rg '^\+.*(已经实现|现已可运行|Adapter 已完成|migration 已执行|业务表已创建)' ||
    true
)"
rg -n 'record_schema_version|state_version|artifact_schema_version|tool_registry_version|version_manifest|integrity|owner.*graph|recovery|migration|RESULT_UNKNOWN' \
  AGENTS.md PROJECT_DIRECTION.md docs/architecture docs/evaluation \
  docs/implementation README.md src tests
git diff --check "$base_sha" -- "$spec"
uv run pytest
    </automated>
  </verify>
  <acceptance_criteria>
    - 01-04 has one exact two-file Application integration-boundary allowlist
    - registry and codec API are deterministic enough to implement without owner interpretation
    - no specialized source model or Port edit is required
    - source-derived refs, exact external refs and three logical-child formats are closed and independently testable
    - UUID / datetime payloads round-trip through strict JSON validation
    - codec failures cannot become safe-None, partial decode, fallback or authorization
    - physical schema, graph closure, fenced claim and migration remain outside 01-04
    - no implementation claim is made
  </acceptance_criteria>
  <done>Plan 01-04 can implement the scoped logical mapping / codec without crossing semantic-owner or physical-persistence boundaries.</done>
</task>

</tasks>

<verification>

Before writing, capture official planning provenance:

```bash
set -euo pipefail
base_sha=af5afd2c93d429e1b090bfaf7af22c0fc4ec3c7b
plan_path=.planning/phases/01-cycle-1-e2e-01/01-03-PLAN.md
summary_path=.planning/phases/01-cycle-1-e2e-01/01-02-SUMMARY.md
spec_path=docs/implementation/e2e01-thin-slice-implementation-spec.md
test "$(git branch --show-current)" = \
  "codex/e2e01-01-thin-slice-persistence-mapping"
test "$(git rev-parse HEAD)" = "$base_sha"
test -z "$(git status --short)"

planning_merge_sha="$(
  gh api repos/weijie567/mini-agent/git/ref/heads/integration/e2e01-thin \
    --jq '.object.sha'
)"
test "$planning_merge_sha" != "$base_sha"
test "$(
  gh api "repos/weijie567/mini-agent/git/commits/${planning_merge_sha}" \
    --jq '.parents | length'
)" -eq 1
remote_parent="$(
  gh api "repos/weijie567/mini-agent/git/commits/${planning_merge_sha}" \
    --jq '.parents[0].sha'
)"
remote_tree="$(
  gh api "repos/weijie567/mini-agent/git/commits/${planning_merge_sha}" \
    --jq '.tree.sha'
)"
test "$remote_parent" = "$base_sha"
if ! git cat-file -e "${planning_merge_sha}^{commit}"; then
  printf 'BLOCK: Integrator must materialize the verified official commit/tree locally\n' >&2
  exit 1
fi
test "$(git show -s --format='%T' "$planning_merge_sha")" = "$remote_tree"
test "$(git rev-parse "${planning_merge_sha}^")" = "$base_sha"
test "$(git rev-list --count "${base_sha}..${planning_merge_sha}")" -eq 1

expected_planning_files="$(printf '%s\n' \
  .planning/PROJECT.md \
  .planning/REQUIREMENTS.md \
  .planning/ROADMAP.md \
  .planning/STATE.md \
  .planning/phases/01-cycle-1-e2e-01/01-02-SUMMARY.md \
  .planning/phases/01-cycle-1-e2e-01/01-03-PLAN.md \
  README.md \
  docs/implementation/e2e01-thin-slice-multi-agent-plan.md | sort)"
actual_planning_files="$(
  git diff --name-only "${base_sha}..${planning_merge_sha}" | sort
)"
test "$actual_planning_files" = "$expected_planning_files"
git diff --exit-code "${base_sha}..${planning_merge_sha}" -- \
  .planning/GOVERNANCE.md "$spec_path"
checkbox_signature() {
  sed -E \
    -e 's/^- \[([ x])\] \*\*Phase ([0-9]+):.*/\1|Phase-\2/' \
    -e 's/^- \[([ x])\] (01-[0-9]+):.*/\1|\2/' \
    -e 's/^- \[([ x])\] \*\*([^*]+)\*\*.*/\1|\2/'
}
for lifecycle_file in .planning/ROADMAP.md .planning/REQUIREMENTS.md; do
  base_checkboxes="$(
    git show "${base_sha}:${lifecycle_file}" |
      rg '^- \[[ x]\]' || true
  )"
  planning_checkboxes="$(
    git show "${planning_merge_sha}:${lifecycle_file}" |
      rg '^- \[[ x]\]' || true
  )"
  base_signature="$(printf '%s\n' "$base_checkboxes" | checkbox_signature)"
  planning_signature="$(
    printf '%s\n' "$planning_checkboxes" | checkbox_signature
  )"
  test -z "$(printf '%s\n' "$base_signature" | rg '^- \[' || true)"
  test -z "$(printf '%s\n' "$planning_signature" | rg '^- \[' || true)"
  test "$planning_signature" = "$base_signature"
  test -z "$(printf '%s\n' "$planning_checkboxes" | rg '^- \[x\]' || true)"
done
test -z "$(
  git show "${planning_merge_sha}:.planning/ROADMAP.md" |
    rg -n '^- \[x\]|1/8|12\.5%|13%' || true
)"
git show "${planning_merge_sha}:.planning/STATE.md" |
  rg -n 'completed_plans:[[:space:]]*0'
git show "${planning_merge_sha}:.planning/STATE.md" |
  rg -n 'percent:[[:space:]]*0'
planning_plan_blob="$(git rev-parse "${planning_merge_sha}:${plan_path}")"
planning_summary_blob="$(git rev-parse "${planning_merge_sha}:${summary_path}")"
printf 'planning_merge_sha=%s\nplanning_plan_blob=%s\nplanning_summary_blob=%s\n' \
  "$planning_merge_sha" "$planning_plan_blob" "$planning_summary_blob"
git show "${planning_merge_sha}:${plan_path}"
git show "${planning_merge_sha}:${summary_path}"
```

After the one atomic owner commit:

```bash
set -euo pipefail
base_sha=af5afd2c93d429e1b090bfaf7af22c0fc4ec3c7b
spec=docs/implementation/e2e01-thin-slice-implementation-spec.md
test "$(git branch --show-current)" = \
  "codex/e2e01-01-thin-slice-persistence-mapping"
test "$(git merge-base HEAD "$base_sha")" = "$base_sha"
test "$(git rev-list --count "${base_sha}..HEAD")" -eq 1
test "$(git diff --name-only "${base_sha}...HEAD")" = "$spec"
git log --format='%H %P %s' "${base_sha}..HEAD"
git diff --check "${base_sha}...HEAD"
rg -n \
  'conversation_record\.p0\.v1|model_visible_toolset_artifact\.p0\.v1|eval_execution_failure_record\.p0\.v1|P0_PERSISTENCE_REGISTRY|decode_persistence_record|tests/component/application/test_persistence_contract.py' \
  "$spec"
rg -n \
  'record_schema_version|state_version|artifact_schema_version|tool_registry_version|version_manifest|integrity|owner.*graph|recovery|migration|RESULT_UNKNOWN' \
  AGENTS.md PROJECT_DIRECTION.md docs/architecture docs/evaluation \
  docs/implementation README.md src tests
uv run pytest
```

Expected:

- planning merge is exactly one commit after `af5afd2...` and changes exactly the declared eight planning-status files;
- Thin Slice Spec is byte-identical between execution base and planning merge;
- lifecycle remains `0/8`, `completed_plans: 0`, `percent: 0`;
- feature branch contains one commit and one owner file;
- exact 17-item mapping and 01-04 two-file allowlist are present;
- no physical migration or implementation-complete claim appears;
- `uv run pytest` exits 0 with the current 181-test baseline.

Independent Reviewer must inspect the recorded exact feature head. Unexpected file/commit, owner conflict, duplicate/missing item, registry ambiguity, security regression or unresolved `CRITICAL / HIGH / MEDIUM` is `BLOCK`; PR stays draft.
</verification>

<success_criteria>

- Both tasks complete.
- Exactly 17 canonical items, 17 unique codes and 17 exact versions are frozen.
- `ModelVisibleToolsetArtifact` remains in the 17-item set.
- All items have source, semantic owner, identity and owner/link metadata.
- Strict logical codec and immutable closed registry are fully specified.
- 01-04 is restricted to two Application component files and no migration.
- Exact one-file / one-commit containment holds.
- Canonical serial regression exits 0.
- Independent exact-head review passes.
- No implementation, Adapter, migration, recovery or Case lifecycle completion is claimed.
- Plan 01-04 remains blocked until this owner PR merges.

</success_criteria>

<output>
Execution branch 不得写 `SUMMARY.md` 或共享 State。Executor 只返回 Task Packet handoff，包含 planning provenance、exact head、changed files、commands / results、17-entry inventory、contract / security / Eval impact 与 rollback。

Owner PR 合并后，Integrator 才创建新的 dedicated planning-status PR，为 `01-03-SUMMARY.md`、Roadmap / State consumers 与 exact Plan 01-04 建立证据；不调用自动 lifecycle API。
</output>
