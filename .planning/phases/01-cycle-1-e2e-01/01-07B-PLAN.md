---
phase: 01-cycle-1-e2e-01
plan: 07B
type: tdd
wave: 14
depends_on:
  - 01-07A
files_modified:
  - src/mini_agent/evaluation/harness.py
  - src/mini_agent/evaluation/graders.py
  - src/mini_agent/evaluation/scripted_provider.py
  - tests/component/evaluation/test_e2e01_graders.py
  - tests/component/evaluation/test_e2e01_scripted_model_provider.py
  - tests/integration/evaluation/test_e2e01_offline_harness.py
autonomous: true
requirements:
  - E2E01-01
  - E2E01-04
user_setup: []
must_haves:
  truths:
    - "The SUT receives an execution-only Case projection with closed role/content messages, and ScriptedModelProvider retains closed behavior-specific executable-step projections/runtime fault; neither object can expose semantic Case/script identity, nested setup/answer fields, expectations, expected_control_result, grading configuration, lifecycle, requirement mapping, version expectations or observable-equivalence answers."
    - "The SUT returns the same opaque one-time execution_ref plus unbound evidence/observable payloads with no semantic case_id; the ref and Provider execution identity are fresh non-semantic nonces generated by a zero-argument injected factory, and only the Harness authenticates the correlation and constructs grader-facing EvalEvidence/SafeCaseObservable."
    - "A real observed value that differs from the Case expectation produces a grader FAIL; the SUT cannot turn the mismatch into oracle-derived PASS evidence."
    - "TraceCompletenessGrader rejects violations of case-scoped safety-causal happens-before edges even when all event types, counts, UUIDs and monotonically increasing timestamps remain valid."
    - "Safety precedence is occurrence-aware for the two ContextManifestRecorded and two TaskStateChanged events in E2E01-01/04, but does not freeze a total order or reject unrelated legal events."
    - "This Packet changes no dataset, Fixture, Provider script, Core/Application DTO, PostgreSQL schema, Composition Root, evidence reader or Case lifecycle."
  artifacts:
    - "EvalCaseExecutionInput carries only an opaque per-attempt execution_ref, immutable closed role/content execution messages and the trusted Session-fixture reference; EvalCaseSutResult echoes that ref and carries unbound evidence/observable payloads; an execution-only ScriptedModelProvider with closed behavior-specific steps is the only script projection crossing the Harness-to-SUT boundary."
    - "OfflineEvalHarness retains full authenticated expectations exclusively on the grading side."
    - "Component and Harness tests prove oracle isolation, actual-mismatch FAIL and safety-causal Trace precedence."
  key_links:
    - "OfflineEvalHarness creates EvalCaseExecutionInput from the authenticated execution subset, privately retains execution_ref to case_id correlation and rebinds case identity only after SUT execution."
    - "OfflineEvalHarness generates execution_ref without passing Case/script/expectation data, accepts it exactly once, rejects collision/unknown/mismatched/replayed refs before grading, and only then binds authenticated case_id into EvalEvidence and SafeCaseObservable."
    - "EvalCaseSut receives a per-Case ScriptedModelProvider whose reachable state contains closed behavior-specific executable-step DTOs/runtime fault and opaque execution identity but no semantic model_script_ref, full ModelScriptArtifact, expected_control_result or grader answer fields."
    - "TraceCompletenessGrader validates occurrence-aware safety-causal edges before accepting typed-record references while allowing unrelated legal event insertion."
---

# Phase 1 Plan 01-07B｜Eval oracle isolation and Trace precedence

> **ISSUED DEPENDENCY TASK PACKET / IMPLEMENTATION NOT STARTED**
> 01-08 preflight 对 status-aligned exact integration `8544137...` 的只读安全 / Eval 审计确认两个阻断：现有 `EvalCaseSut` 直接接收完整 `EvalCaseArtifact`，可读取 expectations / grading 后按答案制造 evidence；现有 `TraceCompletenessGrader` 只检查存在性、数量和时间单调性，安全关键事件重新排序后仍可能通过。二者均属于 Eval ownership，必须在真实 Composition Root 之前以独立 Packet 关闭。

> **DERIVED / NON_NORMATIVE**
> 本 Plan 只把 canonical Eval owner、Thin Slice Spec 与真实执行反馈转换为精确 Task Packet，不拥有产品、Case、Dataset、Trace DTO、持久化、lifecycle 或发布语义。

<objective>
关闭 Eval Harness / Scripted Provider 的 oracle exposure，并使 Trace grader 验证 E2E01-01/04 的安全关键因果偏序。

Purpose: 让后续真实 SUT 只能执行输入，不读取 grader 答案；让完整但顺序错误的 Trace 可靠失败。

Output: exact six-file Eval Packet；一个 test-only RED commit和一个对应 GREEN / REFACTOR commit。不创建 real SUT、Composition Root、PostgreSQL evidence reader、HTTP E2E或 lifecycle 更新。
</objective>

<execution_context>
@AGENTS.md
@.planning/GOVERNANCE.md
@.planning/phases/01-cycle-1-e2e-01/01-07-PLAN.md
@.planning/phases/01-cycle-1-e2e-01/01-07-SUMMARY.md
@.planning/phases/01-cycle-1-e2e-01/01-07A-SUMMARY.md
@.planning/phases/01-cycle-1-e2e-01/01-W2-RESEARCH.md
@.planning/phases/01-cycle-1-e2e-01/01-W2-PATTERNS.md
@docs/evaluation/agent-evaluation-strategy.md
@docs/evaluation/p0-eval-coverage-matrix.md
@docs/implementation/e2e01-thin-slice-implementation-spec.md
@evals/cases/e2e01-thin-slice.v1.json
@src/mini_agent/evaluation/harness.py
@src/mini_agent/evaluation/graders.py
@src/mini_agent/evaluation/scripted_provider.py
@tests/component/evaluation/test_e2e01_graders.py
@tests/component/evaluation/test_e2e01_scripted_model_provider.py
@tests/integration/evaluation/test_e2e01_offline_harness.py

本 Plan 使用受控 GSD planner / checker adapter，不调用 stock plan / execute / verify / lifecycle mutation / ship。planning-status PR 必须先合并；随后 Integrator 从固定 execution base 建立独立 branch / Worktree，并从 official planning merge 读取本 Plan。Executor 只能写 exact six owned files。
</execution_context>

<packet_contract>
repository: `https://github.com/weijie567/mini-agent`
remote: `origin`
head_branch: `codex/e2e01-01-eval-evidence-boundary`
base_branch: `integration/e2e01-thin`
base_sha: `8544137cfbcaebda603cd3000312fb5d2406327c`
base_tree: `0061dac32005add077845f092b6e8b3be9cefae8`
worktree_id: `e2e01-01-eval-evidence-boundary`
worktree_path: `/Users/ming/projects/mini-agent-worktrees/e2e01-01-eval-evidence-boundary`
writer: `Eval sole writer, supervised by /root Integrator`
agent_role: `eval-engineer`

owned_files:

- `src/mini_agent/evaluation/harness.py`
- `src/mini_agent/evaluation/graders.py`
- `src/mini_agent/evaluation/scripted_provider.py`
- `tests/component/evaluation/test_e2e01_graders.py`
- `tests/component/evaluation/test_e2e01_scripted_model_provider.py`
- `tests/integration/evaluation/test_e2e01_offline_harness.py`

forbidden_files:

- every repository path not listed in `owned_files`
- `AGENTS.md`
- `PROJECT_DIRECTION.md`
- `README.md`
- `.planning/**`
- `docs/**`
- `evals/**`
- every other `src/mini_agent/evaluation/**` file
- `src/mini_agent/core/**`
- `src/mini_agent/application/**`
- `src/mini_agent/infrastructure/**`
- any Composition Root, app entrypoint or PostgreSQL evidence-reader path
- every other test file
- `tests/conftest.py`
- `alembic/**`
- `pyproject.toml`
- `uv.lock`
- `compose.yaml`
- `graphify-out/**`

canonical_inputs:

- `AGENTS.md` sections 0–8 and graphify
- `docs/evaluation/agent-evaluation-strategy.md` expectations/evidence separation, typed grading and Result/Failure semantics
- `docs/evaluation/p0-eval-coverage-matrix.md` E2E01-01/04 Case and Critical failure mapping
- `docs/implementation/e2e01-thin-slice-implementation-spec.md` sections 13.2–13.4
- authenticated Case artifacts at `base_sha`; artifacts remain read-only
- frozen Core/Application/Trace DTO source at `base_sha`

dependencies:

- 01-07A merge `4cfac0a4ccfac6b75afa565f6010f7b1544abd7a`
- Business status alignment merge `b46a96756d6e45a49d51ddbb681a3f29e8d510d8`
- Eval status alignment merge `d15f8dbd58035e5baa5dd9cfceae330c9654a151`
- project-rule status alignment / exact execution base `8544137cfbcaebda603cd3000312fb5d2406327c`
- current post-merge gates: `100` Runtime focused, `40` migration and `936 passed, 1 deselected` full
- this planning-status PR must merge before any Eval write
- execution preflight records `PLANNING_CONTRACT_SHA`, Plan blob and 01-07A Summary blob from official `origin/integration/e2e01-thin`, then proves all six owned files are byte-identical between `base_sha` and planning merge
- 01-07C RU semantic ruling and 01-07G Thin Slice source-version ruling remain unissued until this Packet obtains reviewed merge and post-merge gates; 01-07D–01-07F, 01-07H–01-07L, 01-08 and 01-08A remain transitively blocked
- Thin Slice §10.3 already requires invalid Request Understanding Pydantic/source/authority/InputBinding/trusted-field output to become `COMPLETED / INPUT_INVALID`, but the current Scripted Provider leaks `ValidationError`, Qwen collapses it to `ProviderProtocolError`, and Runtime lacks a bounded dedicated signal; this Packet does not own that external failure contract. Unissued 01-07I must freeze the fresh parameterless/raw-free Application signal and Port taxonomy, 01-07J must map it in Runtime, and 01-07L must adapt the Eval-owned Scripted/Qwen consumers before 01-08

required_checks:

- exact base, branch, merge-base, planning provenance and clean-worktree preflight
- one test-only RED commit proving both the full-Case oracle surface and reordered-Trace false PASS against unchanged production
- execution-only DTO contains only an opaque per-attempt `execution_ref`, exactly one immutable closed execution-message DTO for the current P0 Cases (`role=Literal["user"]`, non-empty `content`, no extra fields) and trusted Session-fixture ref; `execution_ref` comes from a zero-argument injected nonce factory whose production default is `uuid4`, is unique across Cases/attempts and is never derived from or parameterized by Case/script/expectation data; top-level and nested unknown fields are never copied, and it does not expose semantic `case_id`, `model_script_refs`, environment/initial fixture refs, expectations, grading, lifecycle, requirement refs, version manifest or observable equivalence
- Harness alone retains `execution_ref → authenticated case_id` and rebinds that Eval-only correlation after SUT execution; no business, HTTP, Runtime, PostgreSQL or Trace fact is synthesized by this rebinding
- EvalCaseSutResult echoes the opaque `execution_ref`; execution-stage evidence and safe-observable payloads contain no field named or semantically equivalent to `case_id`, and Harness constructs grader-facing `EvalEvidence` / `SafeCaseObservable` only after one-time correlation succeeds
- nonce collision, unknown, mismatched or replayed `execution_ref`, semantic Case identity in the unbound output, or disagreement between unbound evidence and observable becomes a bounded `RESULT_COMPLETENESS` execution failure before any grader/result PASS; no execution/script ref is reused across Cases or attempts
- ScriptedModelProvider stores only immutable closed behavior-specific executable-step DTOs, runtime-fault directive and a fresh non-semantic script-execution nonce produced without script/Case/expectation arguments; each behavior copies an explicit minimum key allowlist, so arbitrary nested artifact keys are dropped/rejected, and neither public nor reachable private state retains semantic `model_script_ref`, full `ModelScriptArtifact` or `expected_control_result`
- full authenticated Case remains available only inside Harness setup/grading and Result version-manifest construction
- a deliberately mismatched actual Runtime/Provider-derived field yields structured grader FAIL, never expectation-derived PASS or Harness execution failure
- existing per-Case Scripted Provider isolation, cursor exhaustion and runtime-fault injection remain unchanged
- every existing authenticated Case/script variant uses an explicit closed safety-causal DAG, rather than one unconditional happy-path chain or silently skipped edge; an edge whose endpoint is required but absent is a grader configuration/evidence FAIL
- the common prefix is `MessageAccepted → RunStarted → RequestUnderstandingStarted → Manifest#1`; task-less Request Understanding/provider/input-failure variants require their last reached safety stage `→ ResponseRendered → RunStopped → EvalCaseGraded`, while every with-Task variant requires `last reached safety stage → ResponseRendered → final TaskStateChanged occurrence → RunStopped → EvalCaseGraded`
- variants that require candidate processing enforce `Manifest#1 → NextMoveProposed → TaskDeltaValidated → TaskDeltaAccepted → InputBindingRecorded → TaskState#1 → NextMoveRevalidated → GateDecision`; Gateway-reject variants then require `GateDecision → ResponseRendered → final TaskState`
- variants that require Tool execution enforce `GateDecision → ToolCallCreated → ToolCallStarted → terminal Tool event → ToolResultNormalized`; not-found/foreign-order variants then require `Normalized → ResponseRendered → TaskState#2`
- successful observation/presentation variants require `Normalized → Observation → Manifest#2`; normal success continues `→ PresentationPlan → ResponseRendered → final TaskState`, while presentation-protocol-fault variants require the explicit alternate `Manifest#2/Observation → ResponseRendered → final TaskState` edge without requiring a forbidden PresentationPlan
- Request Understanding/provider/input failure variants require `Manifest#1 → ResponseRendered` and do not invent forbidden candidate/Gate/Tool/Task events
- for each existing Case/script variant, swapping any applicable proposal/validation/acceptance/binding/state/Gate, Tool created/started/terminal/normalized, Observation/Manifest/Presentation, reached-stage/Response, Response/final-TaskState, final-TaskState/RunStopped or RunStopped/EvalCaseGraded edge fails even after reassigning monotonically increasing timestamps; state-advanced faults use the last actual TaskState occurrence, and each canonical variant and unrelated legal insertion passes
- inserting an unrelated legal Trace event without breaking required counts/references/happens-before remains allowed; no complete total-order equality is introduced
- required / forbidden events, count assertions, typed-reference checks and ordinary trace shape remain in force
- no ModelProvider external Protocol/failure-taxonomy, Provider script artifact, Qwen Adapter/runner, Core/Application DTO/Port, persistence record/schema, HTTP or lifecycle change; the ScriptedModelProvider internal execution-only state change is the authorized Eval-local change, and current invalid-RU error mapping remains an explicit I→J→L blocker rather than a hidden 01-07B fix
- directed Eval tests, inherited Eval focused suite, migration, full, compileall and `git diff --check`
- exact six-file feature and latest-integration overlay containment
- independent correctness/security/contract/test-gap review against exact feature and overlay heads
- read-only cross-file impact scan; any required forbidden-file change stops execution and returns to owner planning

done_when:

- one real test-only RED and one corresponding GREEN / REFACTOR commit are recorded in order
- feature and overlay changed-file sets both equal the exact six-file allowlist
- SUT boundary is mechanically unable to read or infer semantic Case/script identity or grader answers through either injected argument; refs are zero-argument non-semantic nonces, and the output contract cannot require or carry semantic case_id
- reordered safety-critical Trace is rejected while canonical E2E01-01/04 traces continue to pass
- no unresolved `CRITICAL / HIGH / MEDIUM` review finding remains
- draft PR targets `integration/e2e01-thin` and records all nonclaims
- merge does not advance Case, Requirement or numbered Phase lifecycle

contract_changes: `YES / EVAL-LOCAL PROTOCOL` — replace the full `EvalCaseArtifact` SUT argument, semantic-case-bearing SUT result and full `ModelScriptArtifact` Provider state with nonce-correlated execution-only/unbound projections, then add occurrence-aware safety-causal Trace precedence validation; no external ModelProvider failure taxonomy, HTTP, Core/Application or persistence contract changes.
security_impact: `YES / CRITICAL` — removes direct, nested, output-side and deterministic-ref grader-answer exposure from the real SUT boundary and prevents reordered Trace from masquerading as valid evidence. It does not close the distinct invalid-RU raw-exception / misclassification risk, which remains blocked on the I/J/L owner chain.
eval_impact: `YES / CONTRACT HARDENING` — existing synthetic Harness tests adapt to the narrower protocol; no Case expectation, artifact version, grader catalog name, Result status or lifecycle changes.
new_dependencies: `NONE`
graphify_disposition: `INTEGRATOR POST-MERGE GATE` — writer never modifies `graphify-out/**`; after reviewed merge the Integrator runs Graphify AST refresh, verifies structure/freshness/no stale marker and blocks downstream planning on failure.
rollback: Close before merge or use a normal revert PR and re-block 01-07C–01-07L/01-08/01-08A. Never reset, force-push, rewrite history or remove shared Worktrees.

handoff_to: `/root Integrator`

handoff_format:

- branch, exact base/planning/head/commit/tree and Plan/Summary blobs
- exact six-file feature/overlay containment
- RED and GREEN commits plus directed/focused/migration/full commands
- execution-input / Provider-reachable-state field matrix and actual-mismatch result
- canonical/reordered/extra-legal-event Trace behavior matrix
- contract/security/Eval impact and nonclaims
- independent feature/overlay reviews
- cross-file scan, unresolved risks and rollback
</packet_contract>

<threat_model>

| threat_id | category | boundary | disposition | mitigation |
|---|---|---|---|---|
| `EVB-E01` | Elevation of Privilege | Harness expectations → SUT | `MITIGATE / BLOCK` | SUT receives a dedicated execution-only DTO with no grader-answer fields |
| `EVB-T01` | Tampering | SUT evidence → grader | `MITIGATE / BLOCK` | deliberate actual/expected mismatch must produce structured FAIL |
| `EVB-R01` | Repudiation | persisted Trace → Trace grader | `MITIGATE / BLOCK` | occurrence-aware safety-causal happens-before edges reject invalid reordering without total-order equality |
| `EVB-I01` | Information Disclosure | Case/script metadata → SUT | `MITIGATE` | semantic Case/script refs, fixture setup refs, lifecycle, requirement, grading and observable-equivalence metadata never cross execution boundary; opaque refs are fresh zero-argument `uuid4`-class nonces, never deterministic hashes/UUIDs of semantic data |
| `EVB-D01` | Denial of Service | strict grading | `ACCEPT / BOUNDED` | invalid input/evidence remains a bounded FAIL or typed execution failure; no retry/network introduced |
| `EVB-S01` | Spoofing | Case/result correlation | `MITIGATE / BLOCK` | zero-argument injected nonce factory produces unique per-attempt refs; Harness rejects collisions/unknown/mismatched/replayed refs and alone binds authenticated Case identity into grader-facing evidence/observable |

</threat_model>

<feature>
  <name>Execution-only Eval SUT and Scripted Provider input</name>
  <files>src/mini_agent/evaluation/harness.py, src/mini_agent/evaluation/scripted_provider.py, tests/component/evaluation/test_e2e01_scripted_model_provider.py, tests/integration/evaluation/test_e2e01_offline_harness.py</files>
  <behavior>
    - a dedicated immutable typed projection carries only a fresh zero-argument-factory per-attempt execution_ref, the current P0 Case's exact one closed `user`/non-empty-content execution message and trusted Session-fixture reference
    - EvalCaseSut cannot receive semantic case_id, model_script_refs, environment/initial fixture refs, expectations, grading, lifecycle, requirement, version or equivalence fields
    - ScriptedModelProvider retains closed behavior-specific executable-step DTOs/runtime fault and a separately generated non-semantic script-execution nonce; arbitrary nested script keys are never reachable
    - Harness privately retains execution_ref to authenticated case_id correlation and the full Case for expectation construction and Result metadata
    - EvalCaseSutResult contains execution_ref plus unbound evidence/observable payloads with no semantic case_id; Harness performs one-time ref validation before constructing the bound grader-facing models
    - per-Case Provider and runtime fault remain separately injected controlled dependencies
    - existing synthetic test SUT adapts without changing production Case artifacts
  </behavior>
</feature>

<feature>
  <name>Occurrence-aware safety-causal Trace precedence</name>
  <files>src/mini_agent/evaluation/graders.py, tests/component/evaluation/test_e2e01_graders.py</files>
  <behavior>
    - canonical first and repeated event occurrences are validated against an explicit closed Case/script-variant safety happens-before DAG
    - timestamp monotonicity remains necessary but is no longer sufficient
    - required, forbidden, exact-count and typed-reference validation remains unchanged
    - reordered applicable candidate/Gate/Tool, Observation/Manifest/Presentation and terminal events fail closed; fault-path alternate terminal edges are not silently skipped
    - unrelated legal events are not rejected merely because they are absent from one canonical example
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — expose Case/script oracle input and reordered-Trace acceptance</name>
  <files>tests/component/evaluation/test_e2e01_graders.py, tests/component/evaluation/test_e2e01_scripted_model_provider.py, tests/integration/evaluation/test_e2e01_offline_harness.py</files>
  <read_first>AGENTS.md, .planning/phases/01-cycle-1-e2e-01/01-W2-RESEARCH.md, .planning/phases/01-cycle-1-e2e-01/01-W2-PATTERNS.md, docs/evaluation/agent-evaluation-strategy.md, docs/implementation/e2e01-thin-slice-implementation-spec.md, evals/cases/e2e01-thin-slice.v1.json, src/mini_agent/evaluation/harness.py, src/mini_agent/evaluation/graders.py, src/mini_agent/evaluation/scripted_provider.py, all three owned test files</read_first>
  <action>Add focused assertions that the SUT Case argument exposes semantic case_id/model_script_refs/setup refs and answer-bearing fields before GREEN, including a nested message with an extra setup/answer key; prove the injected ScriptedModelProvider exposes semantic model_script_ref/full script/expected_control_result before GREEN, including an authenticated step with an arbitrary answer-like extra key. Prove the current SUT result contract requires semantic case_id, then specify RED cases for unknown/mismatched/replayed execution_ref, nonce collision/reuse and any semantic-case-id-bearing unbound output. Specify an injected nonce-factory spy whose call accepts zero arguments, returns controlled distinct UUIDs and fails if Case/script/expectation data is supplied; assert execution/script refs cannot equal deterministic uuid5/hash derivations of semantic IDs. Prove a real observed mismatch reaches graders and FAILs. For every existing Case/script expectation variant, keep the exact event set/counts and monotonic timestamps while swapping each applicable safety-causal edge; record the current false PASS or missing closed-DAG behavior without modifying production. Commit only all three test files as `test(01-07B): expose eval evidence boundary gaps`.</action>
  <verify>
    <automated>uv run pytest tests/component/evaluation/test_e2e01_scripted_model_provider.py tests/integration/evaluation/test_e2e01_offline_harness.py -k 'execution_only or execution_ref or result_correlation or nonce or oracle or actual_mismatch'; uv run pytest tests/component/evaluation/test_e2e01_graders.py -k 'precedence or reordered'</automated>
    Record both independent expected RED commands before running the combined three-file command without `-x`. RED must arise from full-Case/full-script answer reachability and missing safety precedence. Syntax/import/fixture failures do not qualify.
  </verify>
  <acceptance_criteria>
    - the RED demonstrates both blockers against unchanged production
    - mismatch evidence is not constructed from Case expectations
    - Provider inspection proves semantic `model_script_ref`, full `ModelScriptArtifact` and `expected_control_result` are reachable before GREEN
    - result-boundary RED proves the current SUT must know semantic case_id and that zero-argument non-semantic nonce correlation/replay rejection is absent
    - nonce-factory RED proves there is no zero-argument non-semantic generation seam and prevents a uuid5/hash of case_id/model_script_ref from satisfying “opaque”
    - reorder tests preserve event set/counts, unique IDs and monotonic timestamps while independently violating every applicable edge in all normal, Gateway-reject, Request Understanding/provider/input-fault and presentation-fault variants
    - changed-file set is exactly the three owned test files
  </acceptance_criteria>
  <done>Both Eval contract blockers are reproducible without production edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN / REFACTOR — narrow SUT/provider inputs and enforce causal precedence</name>
  <files>src/mini_agent/evaluation/harness.py, src/mini_agent/evaluation/graders.py, src/mini_agent/evaluation/scripted_provider.py, tests/component/evaluation/test_e2e01_graders.py, tests/component/evaluation/test_e2e01_scripted_model_provider.py, tests/integration/evaluation/test_e2e01_offline_harness.py</files>
  <read_first>Task 1 RED commit, .planning/phases/01-cycle-1-e2e-01/01-W2-RESEARCH.md, .planning/phases/01-cycle-1-e2e-01/01-W2-PATTERNS.md and all six owned files</read_first>
  <action>Introduce the exact execution-only Case projection containing only a fresh per-attempt execution_ref, immutable closed role/content execution-message DTOs and trusted Session-fixture ref. Inject a zero-argument nonce factory into OfflineEvalHarness (production default `uuid4`), call it without semantic inputs to create separate execution/script nonces, reject collisions/reuse, and privately retain a one-time execution_ref to authenticated case_id mapping. Replace the SUT output with execution_ref plus unbound evidence/observable payload models that contain no semantic case_id or equivalent; validate the echoed ref, reject unknown/mismatched/replayed refs as RESULT_COMPLETENESS, verify unbound evidence/observable agreement, consume the mapping exactly once, and only then construct grader-facing EvalEvidence/SafeCaseObservable with authenticated case_id. Make ScriptedModelProvider convert each authenticated step to an immutable behavior-specific DTO that copies only the exact execution keys used by that behavior, plus a closed runtime-fault directive and separately supplied non-semantic script-execution nonce; never retain semantic model_script_ref, ModelScriptArtifact or arbitrary nested keys. Update the Protocol/test SUTs, refactor `_synthetic_trace` so repeated Manifest/TaskState occurrences interleave at their real Runtime stages, and add a closed occurrence-aware Case/script-variant DAG in TraceCompletenessGrader. Cover common prefix plus applicable candidate/Gate, Tool lifecycle, success, not-found, Gateway-reject, Request Understanding/provider/input-fault and presentation-fault alternate paths; require ResponseRendered→last actual TaskState→RunStopped for every with-Task path (including state-advanced faults) and ResponseRendered→RunStopped only for task-less paths. Missing required endpoints fail rather than silently skip. Add canonical PASS, every applicable edge-swap FAIL and legal-extra-event PASS tests for each existing variant so this remains a partial order rather than total-order equality. Preserve all artifact loaders, configured grader names, Result/Failure behavior, pair equivalence and typed-reference checks. Commit as `fix(01-07B): harden eval evidence boundary`.</action>
  <verify>
    <automated>uv run pytest tests/component/evaluation/test_e2e01_graders.py tests/component/evaluation/test_e2e01_scripted_model_provider.py tests/integration/evaluation/test_e2e01_offline_harness.py</automated>
    Then run all inherited Eval focused tests, migration regression, full offline suite, compileall and diff containment.
  </verify>
  <acceptance_criteria>
    - SUT Case argument exposes only execution_ref/closed role-content messages/trusted Session-fixture ref; Provider reachable state exposes only closed behavior-specific step DTOs/runtime fault/opaque execution identity and no semantic Case/script identity, nested unknown/setup ref, answer-bearing field or expected_control_result
    - SUT result echoes only the one-time execution_ref plus unbound evidence/observable without semantic case_id; wrong/ref replay is RESULT_COMPLETENESS and Harness alone binds case_id after successful correlation
    - zero-argument factory spy proves refs receive no semantic arguments; distinct Cases/attempts use distinct nonces, collision/reuse fails, and no deterministic case/script-derived UUID/hash is accepted as the generation mechanism
    - actual mismatch yields grader FAIL and persists a normal FAIL Result when evidence is otherwise complete
    - canonical interleaved traces for every existing Case/script variant PASS; each applicable causal-edge violation fails, a required missing endpoint fails, and unrelated legal insertion still passes
    - no forbidden file or external contract changes
  </acceptance_criteria>
  <done>Eval execution and grading boundaries are safe inputs for downstream real SUT wiring.</done>
</task>

</tasks>

<verification>

```bash
uv sync --all-groups
docker compose up --wait -d db
docker compose --profile test up --wait -d db-test
uv run alembic upgrade head
uv run pytest \
  tests/component/evaluation/test_e2e01_graders.py \
  tests/component/evaluation/test_e2e01_scripted_model_provider.py \
  tests/integration/evaluation/test_e2e01_offline_harness.py
uv run pytest \
  tests/component/evaluation/test_e2e01_versioned_artifact_loader.py \
  tests/component/evaluation/test_e2e01_artifact_consistency.py \
  tests/component/evaluation/test_e2e01_scripted_model_provider.py \
  tests/component/model/test_qwen_responses_adapter.py \
  tests/component/evaluation/test_e2e01_graders.py \
  tests/integration/evaluation/test_e2e01_offline_harness.py -x
uv run pytest tests/integration/test_database_migrations.py -x
uv run pytest
env -u DASHSCOPE_API_KEY -u DASHSCOPE_BASE_URL \
  uv run pytest -m qwen_baseline tests/baseline/test_qwen_baseline.py -x
DASHSCOPE_API_KEY=not-a-real-key \
DASHSCOPE_BASE_URL=https://example.invalid \
  uv run pytest -m qwen_baseline tests/baseline/test_qwen_baseline.py -x
uv run python -m compileall -q src tests
git diff --check
```

以上五条canonical项目命令均从feature Worktree仓库根目录执行。还必须机械比较 `base_sha...feature_head` 与 latest-integration overlay 的 changed-file set，二者都必须精确等于 six-file allowlist；显式清除 Qwen 凭据运行零网络 preflight；检查 `evals/**`、Core/Application/Infra、共享 bootstrap 与 lifecycle owner 相对 base byte-identical。
</verification>

<success_criteria>

- SUT 只接收opaque `execution_ref`、实际消息与trusted Session-fixture ref；SUT / Provider都不再接收semantic Case/script identity、setup refs或grader答案。
- 实际不匹配只能形成真实 FAIL，不能按 expectations 补造 PASS。
- E2E01-01/04 的安全因果偏序通过，关键edge violation失败，合法额外事件不因total-order equality被拒绝。
- feature / overlay exact-head tests、双路独立 review、containment与Graphify gate通过。
- 本 Packet reviewed merge后只先签发01-07C RU semantic ruling与01-07G Thin Slice source-version ruling；Integrator必须先串行合并C/G形成共同exact barrier，再严格按`{01-07D,01-07H}→{01-07E,01-07F}→01-07I→01-07J→{01-07K,01-07L}→01-08→01-08A`从各自新的exact integration SHA推进。D/H不得从不同predecessor启动；I/J/L还必须分别关闭Application Provider failure signal、Runtime `INPUT_INVALID` mapping与Eval-owned Scripted/Qwen consumers，K保持strict reader ownership。
- Case lifecycle与派生 Requirements继续保持`CONTRACT_DEFINED / 0/8`。

</success_criteria>
