---
phase: 01-cycle-1-e2e-01
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/mini_agent/application/persistence.py
  - tests/component/application/test_persistence_contract.py
autonomous: true
requirements:
  - E2E01-01
  - E2E01-04
user_setup: []
must_haves:
  truths:
    - "Registry contains exactly 17 immutable code/version/source-model entries and exposes no dynamic registration or fallback."
    - "All 17 records round-trip through strict Pydantic JSON serialization without Python-value or default=str coercion shortcuts."
    - "Outer/inner metadata, identity, owner, 66 top-level projections, 7 child projections, version mirrors and logical children are recomputed and compared fail closed."
    - "Only the owner-approved five external references are caller supplied; all source-derived and child-derived references are recomputed."
    - "AcceptedTaskDelta and ToolAttemptRecord are LOCAL_CLOSED; TaskStateTransition remains GRAPH_REQUIRED and cannot claim complete history."
    - "Integrity errors expose only a bounded category and opaque UUID correlation reference."
    - "Codec does not authorize, query storage, migrate data, claim recovery state or prove complete owner-graph closure."
  artifacts:
    - "src/mini_agent/application/persistence.py implements the approved closed registry and logical codec."
    - "tests/component/application/test_persistence_contract.py proves all positive, negative and diagnostic-safety contracts."
  key_links:
    - "P0_PERSISTENCE_REGISTRY maps the exact Thin Slice Spec 10.1 17-item table to directly imported source models."
    - "Projection implementation maps exactly the 66 top-level and 7 logical-child owner decisions; 45 reference-producing decisions target the closed 17-item set."
    - "decode_persistence_record validates JSON with source_model.model_validate_json(..., strict=True)."
    - "P0_LOGICAL_CHILD_SPECS binds exactly three children to their approved parents and closure strategy."
---

<objective>
实现 Thin Slice Spec 第 10.1 节已经批准的 Application integration-boundary persistence mapping / codec。

Purpose: 为后续 Runtime 与 Infrastructure 提供唯一、封闭、exact-version、fail-closed 的 logical serialization boundary，同时不越过授权、owner graph、recovery 或 physical persistence 边界。

Output: 只创建 `src/mini_agent/application/persistence.py` 与 `tests/component/application/test_persistence_contract.py`。不修改现有 DTO、Port、Core、Infrastructure、active docs、migration、Composition Root 或 toolchain。
</objective>

<execution_context>
@AGENTS.md
@.planning/GOVERNANCE.md
@.planning/phases/01-cycle-1-e2e-01/01-03-SUMMARY.md
@docs/implementation/e2e01-thin-slice-implementation-spec.md
@docs/implementation/e2e01-thin-slice-multi-agent-plan.md

不得调用 stock `gsd-import`、`gsd-plan-phase`、`gsd-execute-phase`、`gsd-verify-work`、自动 lifecycle mutation 或 `gsd-ship`。Integrator 已在 workflow 外从 exact base 预建 execution Worktree；executor 只写本 Packet 两个 owned files，不写 Summary 或共享 State。
</execution_context>

<context>
Canonical inputs:

@PROJECT_DIRECTION.md
@docs/architecture/memory-design-reference.md
@docs/architecture/intent-design-reference.md
@docs/architecture/tool-calling-design-reference.md
@docs/evaluation/agent-evaluation-strategy.md
@docs/implementation/e2e01-thin-slice-implementation-spec.md

Implementation evidence only:

@src/mini_agent/core/common.py
@src/mini_agent/application/records.py
@src/mini_agent/core/task_state.py
@src/mini_agent/core/tool_system.py
@src/mini_agent/core/memory.py
@src/mini_agent/core/trace.py
@tests/component/application/test_record_contracts.py
@tests/component/application/test_ports_contract.py
</context>

<interfaces>
Plan 01-04 必须实现 owner 已冻结的 public surface，不得自行改名、扩展 registry 或改变 source DTO：

- `P0RecordCode`
- `P0RecordReference`
- `P0LogicalChildCode`
- `P0LogicalChildPayload`
- `P0VersionedPayload`
- `P0PersistenceEnvelope`
- `P0RecordSchemaSpec`
- `P0_PERSISTENCE_REGISTRY`
- `P0_LOGICAL_CHILD_SPECS`
- `DecodedP0PersistenceRecord`
- `P0PersistenceIntegrityCategory`
- `P0PersistenceIntegrityError`
- `encode_persistence_record(...)`
- `decode_persistence_record(...)`

`encode_persistence_record` 的签名没有 caller-supplied `correlation_ref`。实现遇到 integrity error 时生成新的 opaque `uuid4()`；`decode_persistence_record` 必须使用调用方传入的可信 UUID。该选择只实现 bounded-error API，不产生业务 identity、授权或追踪语义。
</interfaces>

<task_packet>

repository: `https://github.com/weijie567/mini-agent`
remote: `origin`
head_branch: `codex/e2e01-01-persistence-codec`
base_branch: `integration/e2e01-thin`
base_sha: `9602fc18148b19c841889a8041daf10ccc5b8f1c`
worktree_id: `e2e01-01-persistence-codec`
writer: `Application persistence codec sole writer, supervised by /root Integrator`

owned_files:

- `src/mini_agent/application/persistence.py`
- `tests/component/application/test_persistence_contract.py`

forbidden_files:

- every repository path not listed in `owned_files`
- `AGENTS.md`
- `PROJECT_DIRECTION.md`
- `README.md`
- `.planning/**`
- `docs/**`
- `src/mini_agent/application/records.py`
- `src/mini_agent/application/ports.py`
- `src/mini_agent/core/**`
- `src/mini_agent/infrastructure/**`
- `src/mini_agent/__init__.py`
- `src/mini_agent/main.py`
- `src/mini_agent/bootstrap.py`
- every existing test file
- `tests/component/core/**`
- `tests/component/evaluation/**`
- `tests/integration/**`
- `tests/e2e/**`
- `tests/baseline/**`
- `evals/**`
- `alembic/**`
- `pyproject.toml`
- `uv.lock`
- `compose.yaml`
- `graphify-out/**`

canonical_inputs:

- `AGENTS.md` at exact `base_sha`
- Thin Slice Spec current blob `64efddf805ba6a1d8e7fdb5eb9f333a3562e82ee`
- Project Direction persistence ownership contract
- Memory exact-version / integrity / owner graph / recovery boundary
- Intent logical-delta and InputBinding provenance
- Tool lifecycle and attempt semantics
- Eval result status / Trace aggregate cardinality

dependencies:

- Plan 01-03 planning merge `00a37ec8beb43f4a73a15e619ce43ec8daa0d490`
- Mapping owner PR #16 merge `9632c18532baa2f4cd6ab7526d0e6db30328ea65`
- Projection clarification PR #17 merge / exact execution base `9602fc18148b19c841889a8041daf10ccc5b8f1c`
- the planning-status PR containing this Plan and `01-03-SUMMARY.md` must merge before implementation writing starts
- because the execution branch is intentionally forked before that planning-status PR, executor must resolve the fresh official integration ref, prove the planning merge is the sole commit after `base_sha`, and read this Plan / Summary through captured Git objects
- planning merge must change exactly the eight declared planning-status files and leave both owned implementation files and every canonical input byte-unchanged from `base_sha`

planning_provenance:

- `planning_merge_sha`: `TO_BE_CAPTURED_FROM_OFFICIAL_INTEGRATION_REF_AFTER_PLANNING_STATUS_PR_MERGE`
- `planning_plan_blob`: `TO_BE_CAPTURED:${planning_merge_sha}:.planning/phases/01-cycle-1-e2e-01/01-04-PLAN.md`
- `planning_summary_blob`: `TO_BE_CAPTURED:${planning_merge_sha}:.planning/phases/01-cycle-1-e2e-01/01-03-SUMMARY.md`
- unresolved placeholder, wrong parent, wrong changed-file set or mismatched blob is `BLOCK`

required_checks:

- branch / exact base HEAD / merge-base / clean-worktree precheck
- official GitHub ref/commit/tree provenance when smart HTTP is unavailable
- exact planning merge SHA / Plan blob / Summary blob and eight-file containment
- exact two-file implementation allowlist and exactly one feature commit
- immutable registry exactly 17 with code/source-model bijection and no runtime registration
- 17 positive strict-JSON round trips including UUID / datetime
- 66 top-level and 7 child projection coverage; exactly 45 reference-producing decisions target the 17-item set
- exactly five external-required relations; missing / extra / duplicate / wrong token / target / identity / cardinality rejected
- all payload / diagnostic / conditional correlations excluded from `P0RecordReference`
- three `MUST_BE_EMPTY` decisions reject non-empty data
- all three child specs, local closure and graph-required non-claim
- bounded error category, opaque UUID, empty unsafe exception chain and non-disclosing `str` / `repr` / `args`
- `git diff --check`, compileall, focused component tests, complete `uv run pytest` and repository cross-file impact scan
- independent exact-head review before PR readiness / merge
- post-merge Integrator-only `graphify update .` gate before 01-05/06/07 dispatch

done_when:

- both tasks complete in exactly the two owned files
- one-commit / two-file containment and all mechanical checks pass
- complete canonical regression passes
- independent exact-head review has no unresolved `CRITICAL / HIGH / MEDIUM`
- feature PR uses the repository template, starts as draft and targets `integration/e2e01-thin`
- merge does not by itself advance Requirements, Case or Phase lifecycle

contract_changes: `NO NEW SEMANTICS / IMPLEMENTS APPROVED CONTRACT`

security_impact: `YES` — implements type/version/identity/owner/relation/child fail-closed gates and safe diagnostics, but never constructs trusted scope or authorization.

eval_impact: `YES / COMPONENT TESTS ONLY` — adds codec Component tests; no Eval Case, result, metric, threshold or lifecycle change.

graphify_disposition: `NOT_WRITTEN_IN_FEATURE_WORKTREE` — this checkout has no tracked graph and the Task Packet hard allowlist forbids `graphify-out/**`. After exact feature merge, Integrator runs `graphify update .` serially in root integration checkout, confirms only ignored graph output changes, tracked status remains clean and stale marker is removed. Failure blocks 01-05/06/07.

rollback: Close the PR before merge. After merge use a normal revert PR; no reset, force-push, data rewrite, Worktree deletion or migration.

handoff_to: `Integrator`

handoff_format:

- exact base / planning / local head / published head / commit SHAs, trees and blobs
- two-file allowlist and one-commit proof
- focused / full test and compile output
- 17-entry registry inventory
- 66 + 7 projection coverage and 45 reference-target closure
- integrity category and diagnostic-safety coverage
- contract / security / Eval impact
- graphify disposition
- unresolved risks / deviations / rollback

</task_packet>

<threat_model>

| threat_id | category | component | disposition | mitigation |
|---|---|---|---|---|
| `P04-S01` | Spoofing | code/source registry | `MITIGATE/BLOCK` | exact type equality, direct imports, immutable 17-entry registry, no register/plugin/dynamic import |
| `P04-T01` | Tampering | outer/inner envelope | `MITIGATE/BLOCK` | compare outer and inner code/version before source validation |
| `P04-T02` | Tampering | identity/owner projection | `MITIGATE/BLOCK` | recompute from decoded model; caller cannot override source-derived projections |
| `P04-T03` | Tampering | external relations | `MITIGATE/BLOCK` | accept only five owner-approved tokens, target codes and cardinalities |
| `P04-T04` | Tampering | logical children | `MITIGATE/BLOCK` | exact parent/code/identity/correlation/closure rules and strict child validation |
| `P04-T05` | Tampering | JSON coercion | `MITIGATE/BLOCK` | `model_validate_json(..., strict=True)`; reject non-JSON mappings and `default=str` |
| `P04-T06` | Tampering | graph-closure claim | `MITIGATE/BLOCK` | decoded result has no authorization, complete-graph or recovery-ready assertion |
| `P04-E01` | Elevation of Privilege | persisted owner | `MITIGATE/BLOCK` | projection comparison never creates `TrustedOwnerScope` or authorization |
| `P04-I01` | Information Disclosure | integrity errors | `MITIGATE/BLOCK` | bounded enum + opaque UUID only; discard payload-bearing exceptions before raising so `args` / `__context__` / `__cause__` are safe |
| `P04-D01` | Denial of Service | malformed JSON | `MITIGATE/BLOCK` | deterministic rejection without fallback, partial decode or repeated migration |
| `P04-R01` | Repudiation | registry evolution | `MITIGATE/BLOCK` | static exact-version map; no runtime mutation or read-time migration |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 immutable 17-item registry 与 strict JSON envelope codec</name>
  <files>src/mini_agent/application/persistence.py, tests/component/application/test_persistence_contract.py</files>
  <read_first>AGENTS.md, .planning/GOVERNANCE.md, docs/implementation/e2e01-thin-slice-implementation-spec.md, src/mini_agent/core/common.py, src/mini_agent/application/records.py, src/mini_agent/core/task_state.py, src/mini_agent/core/tool_system.py, src/mini_agent/core/memory.py, src/mini_agent/core/trace.py, tests/component/application/test_record_contracts.py</read_first>
  <behavior>
    - registry has exactly 17 unique codes, versions and source-model bindings
    - registry and schema specs cannot be mutated or extended at runtime
    - exact source model is required; subclasses and wrong models are rejected
    - all 17 records encode and decode through Pydantic JSON mode
    - UUID/datetime JSON strings pass strict JSON validation
    - Python UUID/datetime values inside mapping input are rejected as non-JSON
    - seven schema_version mirrors equal their exact record version
    - Toolset record version and artifact_schema_version are independently enforced
    - missing/unknown/mismatched code/version and outer/inner tampering fail with the required bounded category
    - no failure returns None, partial data, fallback version or unsafe diagnostic content
  </behavior>
  <action>
Create the exact public API frozen by the Thin Slice owner. Use existing `ContractModel`, `RuntimePrivateModel`, `freeze_json_value` and `thaw_json_value` where appropriate. Nested JSON in the envelope must be deeply immutable and restored to native JSON only for serialization.

Represent logical identity as an ordered tuple of `(field_name, JSON-value)` pairs in exact registry field order. Composite optional fields remain present with `null`; do not silently drop them.

Use `MappingProxyType` plus frozen/slotted schema-spec objects. Bind source models through direct Python imports. Do not store import strings, expose `register()`, accept subclasses or fall back to a latest version.

Encode requires `type(record) is spec.source_model` and uses Pydantic JSON mode. Decode:

1. serializes a `P0PersistenceEnvelope` with `model_dump_json()`;
2. serializes a JSON-compatible `Mapping` through stdlib JSON without `default=str`;
3. consumes raw `str` / `bytes` as JSON;
4. safely classifies outer code/version;
5. validates envelope and inner headers;
6. validates source data with `source_model.model_validate_json(..., strict=True)`;
7. recomputes identity, mirrors and projections before returning.

Catch validation / JSON exceptions only long enough to select a bounded category. Do not raise the public integrity error while the payload-bearing exception is active: leave the `except` block, discard the original exception, then raise the safe error so both `__context__` and `__cause__` are `None`. Decode uses its supplied correlation UUID. Encode may generate a fresh opaque `uuid4()` for the same safe integrity error and must never include `repr(record)`.

Tests use local deterministic fixtures with fixed UTC datetimes and UUIDs. Existing test helpers are evidence, not an import dependency.
  </action>
  <verify>
    <automated>
set -euo pipefail
uv run pytest tests/component/application/test_persistence_contract.py \
  -k 'registry or round_trip or version or json or source_model or error' -x
uv run python -m compileall -q src/mini_agent/application/persistence.py
    </automated>
  </verify>
  <done>The exact 17-entry registry and strict logical envelope codec pass positive and failure-path tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 projection、external refs 与三类 logical-child closure</name>
  <files>src/mini_agent/application/persistence.py, tests/component/application/test_persistence_contract.py</files>
  <read_first>docs/implementation/e2e01-thin-slice-implementation-spec.md, docs/architecture/intent-design-reference.md, docs/architecture/memory-design-reference.md, docs/architecture/tool-calling-design-reference.md, docs/evaluation/agent-evaluation-strategy.md, src/mini_agent/core/task_state.py, src/mini_agent/core/tool_system.py, src/mini_agent/core/memory.py, src/mini_agent/core/trace.py</read_first>
  <behavior>
    - 66 top-level and 7 logical-child decisions are implemented exactly
    - all 45 reference-producing decisions use the approved token and 17-item target
    - payload, conditional and restricted correlations never become P0RecordReference
    - all three first-slice MUST_BE_EMPTY fields reject non-empty payloads
    - InputBinding accepts exactly one approved external request_unit reference
    - Observation accepts exactly four approved external source references
    - external refs reject missing, extra, duplicate, wrong token, wrong target and wrong identity/cardinality
    - AcceptedTaskDelta resolves Candidate ACCEPT, parent Message and InputBinding refs and exactly closes accepted_delta_refs
    - ToolAttempt children exactly cover 1..attempt_count
    - TaskStateTransition validates parent Task, RequestUnit ref, ordering and local identity but remains GRAPH_REQUIRED
    - child wrong-parent, duplicate identity, tampering or invalid payload fails closed
    - decoded objects never expose an owner_graph_valid, recovery_ready or authorization assertion
  </behavior>
  <action>
Implement immutable private relation and child specs consumed by `P0RecordSchemaSpec`. Normalize metadata references deterministically by relation token, target code and canonical identity JSON; reject duplicates before sorting. Preserve source payload tuple order.

Implement the owner matrices literally. Do not infer target codes from field names outside them. Source-derived and child-derived refs are recomputed; caller input is limited to:

- InputBinding: exactly one `request_unit_id -> request_unit_record`;
- Observation: exactly one each of `source_tool_call_id`, `source_run_id`, `source_task_id`, `source_request_unit_id`.

Implement exactly three immutable child specs:

- `AcceptedTaskDelta -> RequestUnderstandingRecord`, `LOCAL_CLOSED`;
- `TaskStateTransition -> TaskRecord`, `GRAPH_REQUIRED`;
- `ToolAttemptRecord -> ToolCallRecord`, `LOCAL_CLOSED`.

For Accepted Delta, require parent message equality, exactly one parent Candidate with matching ref and `ACCEPT`, and child-derived InputBinding refs. For Task Transition, require Task parent equality and child-derived RequestUnit ref while keeping `reason_ref` payload-only. A local Task Transition success must not assert contiguous complete history.

Test safe errors through category and UUID only. Assert `str(error)`, `repr(error)` and `error.args` contain no raw payload, customer IDs, Token, Prompt, Cookie, secret or resource-existence details；同时断言 `error.__context__ is None` 与 `error.__cause__ is None`，不能只依赖 traceback display suppression。
  </action>
  <verify>
    <automated>
set -euo pipefail
uv run pytest tests/component/application/test_persistence_contract.py -x
git diff --check
rg -n \
  'P0_PERSISTENCE_REGISTRY|P0_LOGICAL_CHILD_SPECS|model_validate_json|LOCAL_CLOSED|GRAPH_REQUIRED|external_references' \
  src/mini_agent/application/persistence.py \
  tests/component/application/test_persistence_contract.py
uv run pytest
    </automated>
  </verify>
  <done>All approved projection, child and diagnostic-safety boundaries are implemented and the complete canonical regression passes.</done>
</task>

</tasks>

<verification>

Preflight after the planning-status PR merges:

```bash
set -euo pipefail
base_sha=9602fc18148b19c841889a8041daf10ccc5b8f1c
plan_path=.planning/phases/01-cycle-1-e2e-01/01-04-PLAN.md
summary_path=.planning/phases/01-cycle-1-e2e-01/01-03-SUMMARY.md

test "$(git branch --show-current)" = \
  "codex/e2e01-01-persistence-codec"
test "$(git rev-parse HEAD)" = "$base_sha"
test -z "$(git status --short)"

planning_merge_sha="$(
  gh api repos/weijie567/mini-agent/git/ref/heads/integration/e2e01-thin \
    --jq '.object.sha'
)"
test "$planning_merge_sha" != "$base_sha"
test "$(git rev-parse "${planning_merge_sha}^")" = "$base_sha"
test "$(git rev-list --count "${base_sha}..${planning_merge_sha}")" -eq 1

expected_planning_files="$(printf '%s\n' \
  .planning/PROJECT.md \
  .planning/REQUIREMENTS.md \
  .planning/ROADMAP.md \
  .planning/STATE.md \
  .planning/phases/01-cycle-1-e2e-01/01-03-SUMMARY.md \
  .planning/phases/01-cycle-1-e2e-01/01-04-PLAN.md \
  README.md \
  docs/implementation/e2e01-thin-slice-multi-agent-plan.md | sort)"
actual_planning_files="$(
  git diff --name-only "${base_sha}..${planning_merge_sha}" | sort
)"
test "$actual_planning_files" = "$expected_planning_files"

git diff --exit-code "${base_sha}..${planning_merge_sha}" -- \
  AGENTS.md \
  .planning/GOVERNANCE.md \
  docs/implementation/e2e01-thin-slice-implementation-spec.md \
  src tests pyproject.toml uv.lock alembic

planning_plan_blob="$(git rev-parse "${planning_merge_sha}:${plan_path}")"
planning_summary_blob="$(git rev-parse "${planning_merge_sha}:${summary_path}")"
test -n "$planning_plan_blob"
test -n "$planning_summary_blob"

test -z "$(
  git show "${planning_merge_sha}:.planning/ROADMAP.md" |
    rg '^- \[x\]|1/8|12\.5%|13%' || true
)"
git show "${planning_merge_sha}:.planning/STATE.md" |
  rg 'completed_plans:[[:space:]]*0'
git show "${planning_merge_sha}:.planning/STATE.md" |
  rg 'percent:[[:space:]]*0'
```

Postcheck after the one atomic feature commit:

```bash
set -euo pipefail
base_sha=9602fc18148b19c841889a8041daf10ccc5b8f1c

test "$(git branch --show-current)" = \
  "codex/e2e01-01-persistence-codec"
test -z "$(git status --porcelain=v1)"
test "$(git merge-base HEAD "$base_sha")" = "$base_sha"
test "$(git rev-list --count "${base_sha}..HEAD")" -eq 1

expected_files="$(printf '%s\n' \
  src/mini_agent/application/persistence.py \
  tests/component/application/test_persistence_contract.py | sort)"
actual_files="$(git diff --name-only "${base_sha}...HEAD" | sort)"
test "$actual_files" = "$expected_files"

git diff --check "${base_sha}...HEAD"
uv run python -m compileall -q src/mini_agent/application/persistence.py
uv run pytest tests/component/application/test_persistence_contract.py -x
uv run pytest

rg -n \
  'record_schema_version|P0_PERSISTENCE_REGISTRY|P0_LOGICAL_CHILD_SPECS|model_validate_json|integrity|owner.*graph|migration' \
  src/mini_agent/application/persistence.py \
  tests/component/application/test_persistence_contract.py \
  AGENTS.md PROJECT_DIRECTION.md docs
test -z "$(git status --porcelain=v1)"
```

After feature PR exact-head review and merge, Integrator runs in the root integration checkout:

```bash
set -euo pipefail
test "$(git branch --show-current)" = "integration/e2e01-thin"
test -z "$(git status --short)"
graphify update .
test -z "$(git status --short)"
test ! -e graphify-out/needs_update
test ! -e graphify-out/.needs_update
```

`graphify-out/**` is ignored and must not appear in the feature diff. Unexpected file, commit, target, lifecycle change, unsafe error, test failure, stale Graphify marker or unresolved `CRITICAL / HIGH / MEDIUM` exact-head review finding is `BLOCK`.
</verification>

<success_criteria>

- Both tasks complete.
- Exactly one feature commit changes exactly the two owned files.
- Registry and children have the exact approved sizes, direct source bindings and closure strategies.
- All 66 + 7 projection decisions and 45 reference targets are covered.
- Focused tests, compileall and complete canonical regression pass.
- No existing DTO, Port, Core, Infrastructure, migration, docs, planning or toolchain file changes.
- All failure categories remain bounded and non-disclosing.
- Codec makes no authorization, owner-graph, recovery-readiness, physical persistence or migration claim.
- Independent exact-head review passes.
- Phase lifecycle remains 0/8.
- Integrator graph maintenance gate passes before 01-05/06/07 dispatch.

</success_criteria>

<output>
Executor writes no Summary or shared State. It returns only the exact Task Packet handoff. After feature merge, Integrator creates a separate planning-status PR for `01-04-SUMMARY.md` and Plans 01-05/06/07 dispatch; it does not call any automatic lifecycle API.
</output>
