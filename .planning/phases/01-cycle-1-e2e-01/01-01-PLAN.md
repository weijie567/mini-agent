---
phase: 01-cycle-1-e2e-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PROJECT_DIRECTION.md
autonomous: true
requirements:
  - E2E01-01
  - E2E01-04
user_setup: []
must_haves:
  truths:
    - "Project Direction distinguishes semantic owner, Python source owner, Port declaration owner and Infrastructure adapter owner; source placement alone never transfers semantic ownership."
    - "Project Direction distinguishes persistence record_schema_version from state_version, artifact_schema_version, tool_registry_version and Eval version_manifest."
    - "TraceEvent structural schema and record-version approval have one explicit owner, while specialized owners retain the semantics of their event payloads."
    - "Future logical record-version migrations require semantic-owner approval before Infrastructure implements physical/data migration mechanics."
  artifacts:
    - "PROJECT_DIRECTION.md contains the project-wide four-axis persistence ownership rule."
    - "PROJECT_DIRECTION.md assigns TraceEvent structural schema/version ownership without overriding specialized Trace payload owners."
    - "PROJECT_DIRECTION.md defines logical-versus-physical migration responsibility and does not claim an implementation exists."
  key_links:
    - "PROJECT_DIRECTION.md continues to reference memory-design-reference.md for Memory/Context Manifest semantics."
    - "PROJECT_DIRECTION.md continues to reference tool-calling-design-reference.md for Tool/Gate/ToolCall/ToolAttempt semantics."
    - "PROJECT_DIRECTION.md continues to reference agent-evaluation-strategy.md for Eval record and version_manifest semantics."
---

<objective>
在唯一的项目架构方向 owner `PROJECT_DIRECTION.md` 中先固定 persistence schema/version 的跨层 ownership、版本维度和 TraceEvent 结构版本裁决者，为后续 Memory owner 与 Thin Slice scoped mapping 提供单一上游规则。

Purpose: 关闭“文件位于 Application 就等于 Application semantic owner”“Infrastructure 可以自行定义 record version”“TraceEvent 没有唯一结构版本 owner”三类歧义，同时遵守一个 Plan / Task Packet 不跨 ownership boundary。
Output: 只修改 `PROJECT_DIRECTION.md`；不写其他 canonical owner、`.planning/**`、源码、测试、migration 或数据库表，不声称 persistence codec 已实现。
</objective>

<execution_context>
@AGENTS.md
@.planning/GOVERNANCE.md
@docs/implementation/e2e01-thin-slice-multi-agent-plan.md

Stock `gsd-import`、`gsd-plan-phase`、`gsd-execute-phase`、`gsd-verify-work`、自动 lifecycle mutation 与 `gsd-ship` 均不得调用。Integrator 已在 workflow 外预建 Task Packet Worktree / branch；执行者只能写本 Packet 的单文件 allowlist。
</execution_context>

<context>
@PROJECT_DIRECTION.md
@docs/architecture/memory-design-reference.md
@docs/architecture/intent-design-reference.md
@docs/architecture/tool-calling-design-reference.md
@docs/evaluation/agent-evaluation-strategy.md
@docs/implementation/e2e01-thin-slice-implementation-spec.md

@src/mini_agent/core/trace.py
@src/mini_agent/application/records.py
@src/mini_agent/application/ports.py
@src/mini_agent/infrastructure/persistence/models.py
</context>

<task_packet>

repository: `https://github.com/weijie567/mini-agent`
remote: `origin`
head_branch: `codex/e2e01-01-schema-owner-alignment`
base_branch: `integration/e2e01-thin`
base_sha: `624475681847be5a8e463e32dafd28a0483b213b`
worktree_id: `e2e01-01-schema-owner-alignment`
writer: `Codex /root Integrator as the Project Direction single writer`

owned_files:

- `PROJECT_DIRECTION.md`

forbidden_files:

- `.planning/**`
- `AGENTS.md`
- `README.md`
- `docs/**`
- `src/**`
- `tests/**`
- `evals/**`
- `alembic/**`
- `pyproject.toml`
- `uv.lock`
- `compose.yaml`
- `graphify-out/**`

canonical_inputs:

- `AGENTS.md` sections 1 and 3–8
- `PROJECT_DIRECTION.md` at exact `base_sha`
- specialized active owners and current source only as read-only impact evidence

dependencies:

- PR #9 W2.0 contract-freeze merge `85eb2a7fc4cc131e67e44dbba132b526e36ae6a3`
- PR #10 activation reviewed feature head `957cabd6b31dd2156848acd515d2e8dc3d19bd50`
- PR #10 activation merge / exact execution base `624475681847be5a8e463e32dafd28a0483b213b`
- the planning-status PR containing this Plan must merge before writing starts
- because this execution branch was intentionally forked before that planning-status PR, the execution checkout does not contain this Plan; the Integrator must serialize merges, resolve the exact planning merge from `origin/integration/e2e01-thin`, prove it is the sole commit after `base_sha`, and provide this Plan through `git show`
- precheck must prove the planning merge changes exactly the eight declared planning-status files and leaves `PROJECT_DIRECTION.md` byte-unchanged from `base_sha`

required_checks:

- branch / exact base HEAD / merge-base / clean-worktree precheck
- exact planning merge SHA / Plan blob provenance and eight-file containment precheck
- current-integration unchanged precheck for the one owned file
- postcheck of current branch, merge-base, exact head, all commits and exact changed-file allowlist
- `git diff --check`
- owner-term / forbidden-claim scan and active cross-file impact inventory
- canonical serial offline test `uv run pytest`
- independent exact-head review before push/merge decision

done_when:

- `PROJECT_DIRECTION.md` contains the concrete rules in both tasks
- one-file containment and commit provenance checks pass
- canonical serial tests pass
- independent exact-head review has no unresolved `CRITICAL / HIGH / MEDIUM`
- PR uses the repository template and targets `integration/e2e01-thin`
- no later Plan is treated as executable until this owner PR merges

contract_changes: `YES` — project-wide persistence ownership, version-dimension and TraceEvent structural-version authority become canonical only after this PR merges.

security_impact: `YES` — ownership rules must keep trusted identity outside model/payload authority, forbid fail-open/fallback semantics, and prevent Infrastructure from changing authorization or record meaning.

eval_impact: `YES` — Eval `version_manifest` remains a distinct run snapshot under the Eval owner; this Plan creates no Eval result or lifecycle change.

rollback: Before merge, close the PR. After merge, use a revert PR and re-block downstream owner Plans; never reset or force-push protected branches.

handoff_to: `Integrator`

handoff_format:

- branch, exact head and all commit SHAs
- actual changed files
- commands and exact results
- allowlist / forbidden-file proof
- canonical decisions and contract changes
- security / Eval impact
- cross-file impact inventory for Plan 01-02 Memory owner and Plan 01-03 Thin Slice scoped mapping
- unresolved risk and rollback

</task_packet>

<threat_model>

Trust boundaries:

- authenticated Application context → persistence Port: `customer_id` and authorization remain server-derived;
- semantic owner contract → Port declaration → Infrastructure adapter: source/package placement and adapter technology cannot transfer semantic authority;
- stored metadata/payload → future strict decoder: no code/version/type fallback may be authorized by Infrastructure;
- specialized Trace event meaning → shared TraceEvent record structure: shared fields/version and specialized payload semantics have separate named approvers.

STRIDE register:

| threat_id | category | component | disposition | mitigation_plan | file_to_check |
|---|---|---|---|---|---|
| `PERSIST-T01` | Tampering | schema/version ownership | `MITIGATE / BLOCK` | Project Direction states that Infrastructure cannot add versions, fallback-to-latest, relax owner validation or reinterpret persisted payloads | `PROJECT_DIRECTION.md` |
| `PERSIST-E01` | Elevation of Privilege | owner scope | `MITIGATE / BLOCK` | Project Direction states that persistence metadata/payload, model output and source placement never create or enlarge trusted owner scope | `PROJECT_DIRECTION.md` |
| `PERSIST-I01` | Information Disclosure | failure mapping / diagnostics | `MITIGATE / BLOCK` | Project Direction requires specialized/scoped contracts to preserve indistinguishable resource failures and exclude raw token, payload and unnecessary PII from ordinary Trace/diagnostics | `PROJECT_DIRECTION.md` |
| `PERSIST-R01` | Repudiation | logical/physical migration | `MITIGATE / BLOCK` | semantic owner approves logical transforms and audit/rollback obligations before Infrastructure implements explicit migration; runtime silent rewrite is not authorized | `PROJECT_DIRECTION.md` |
| `TRACE-T01` | Tampering | TraceEvent schema/payload | `MITIGATE / BLOCK` | Core Runtime / Project Direction owns the shared TraceEvent record structure and record version; each specialized owner retains its event payload semantics; a migration touching both requires both approvals | `PROJECT_DIRECTION.md` |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 固定 persistence 四轴 ownership 与版本维度</name>
  <files>PROJECT_DIRECTION.md</files>
  <read_first>AGENTS.md, PROJECT_DIRECTION.md, docs/architecture/memory-design-reference.md, docs/architecture/intent-design-reference.md, docs/architecture/tool-calling-design-reference.md, docs/evaluation/agent-evaluation-strategy.md, docs/implementation/e2e01-thin-slice-implementation-spec.md, src/mini_agent/application/records.py, src/mini_agent/application/ports.py, src/mini_agent/infrastructure/persistence/models.py</read_first>
  <action>
在 `PROJECT_DIRECTION.md` 的会话 / Memory ownership 与代码依赖边界附近新增项目级规则，逐项写明：

1. `semantic owner` 决定记录字段、不变量、逻辑 `record_schema_version`、兼容/转换和安全失败语义；
2. `Python source owner` 只说明当前类/Protocol 的代码位置和允许依赖方向，文件位于 `application/` 不会把 Core、Tool 或 Eval 语义转给 Application；
3. `Port declaration owner` 决定调用接口与事务协调位置，但不能复制或改变记录 semantic owner；当前 Protocol 可继续位于 `application/ports.py`，本 Plan 不搬文件；
4. `adapter owner` / Infrastructure 只拥有 table、column、index、JSONB mapping、transaction、Alembic mechanics 和数据库错误适配，不得定义第二套 DTO、record code、version、动态类型 import、fallback-to-latest、授权或状态迁移；
5. `record_schema_version` 是持久化逻辑记录版本；`state_version` 是 optimistic concurrency，`artifact_schema_version` 是 artifact 内容/Hash contract，`tool_registry_version` 是工具配置快照，Eval `version_manifest` 是一次评价运行快照，五者不得互相替代；
6. `CustomerContext`、`TrustedOwnerScope`、模型输出或 persisted payload/metadata 均不能创建、覆盖或扩大可信身份；不存在与无权访问仍不可区分；
7. 任何 decode/integrity 失败的后续 scoped contract 都必须避免 raw payload、Token 和不必要 PII 进入普通 Trace/诊断，也不得把损坏记录伪装成安全的 absent/unauthorized `None`。

明确这些是后续 owner/scoped contract 的约束，不是 `PersistenceEnvelope`、registry、decoder、business table 或 migration 已经实现。
  </action>
  <verify>`rg -n 'semantic owner|Python source owner|Port declaration owner|adapter owner|record_schema_version|state_version|artifact_schema_version|tool_registry_version|version_manifest|fallback-to-latest|TrustedOwnerScope' PROJECT_DIRECTION.md` 必须返回全部目标术语；`git diff -- PROJECT_DIRECTION.md` 不得出现“已实现 decoder/registry/table/migration”结论。</verify>
  <acceptance_criteria>
    - `PROJECT_DIRECTION.md` 包含四个 owner 轴及各自不能越过的责任
    - `PROJECT_DIRECTION.md` 明确五种 version 维度互不替代
    - `PROJECT_DIRECTION.md` 明确 persisted data/model/source placement 不授予 owner scope
    - `PROJECT_DIRECTION.md` 明确完整性错误不等于 absent/unauthorized 且普通诊断不含 raw payload/token/PII
    - 文档不声称任何 persistence implementation 已存在
  </acceptance_criteria>
  <done>项目级 owner 与版本维度不存在由包路径或数据库技术隐式推导的空间。</done>
</task>

<task type="auto">
  <name>Task 2: 指定 TraceEvent 结构版本 owner 与 migration approval</name>
  <files>PROJECT_DIRECTION.md</files>
  <read_first>AGENTS.md, PROJECT_DIRECTION.md, docs/architecture/memory-design-reference.md, docs/architecture/tool-calling-design-reference.md, docs/evaluation/agent-evaluation-strategy.md, docs/implementation/e2e01-thin-slice-implementation-spec.md, src/mini_agent/core/trace.py</read_first>
  <action>
在同一 ownership 规则中消除 Trace 模糊项：

- `TraceEvent` 共享 record structure、公共字段和逻辑 `record_schema_version` 由 Core Runtime / Project Direction owner 批准；
- Request Understanding、Tool、Memory、RAG、Action/Eval 等专项 owner 继续拥有其 event type、payload 字段、可见性和必备事件语义；
- 只改共享 envelope/公共字段的 future logical migration 由 Core Runtime / Project Direction owner 批准；修改专项 payload 的 migration 还必须取得相应 specialized owner 批准；
- Infrastructure 只能执行已批准的显式 physical/data migration，不能在读取时静默升级、降级、quarantine 或重写；
- P0 是否 exact-version-only、unknown/mismatch decode、recovery readiness 与 Thin Slice 17-item minimum-persistence scoped mapping 分别延期到 Plan 01-02 Memory owner 和 Plan 01-03 Thin Slice scoped owner，本文件不抢占它们的规则正文。这里的 17 项严格派生自 Thin Slice Spec 第 10.1 节当前 17 行最低持久化集合（其中包含 `ModelVisibleToolsetArtifact`，不得误称为 20 条 Record）。

运行 repository cross-file impact scan，至少覆盖 AGENTS、Project Direction、Memory、Intent、Tool、Eval、Thin Slice Spec、README 与 multi-agent plan。Handoff 列出 Plan 01-02/01-03 必须消费的上游规则；不得在本 branch 修改扫描出的其他文件。
  </action>
  <verify>`rg -n 'TraceEvent|shared record structure|specialized owner|logical migration|physical.*migration|runtime.*rewrite|Plan 01-02|Plan 01-03' PROJECT_DIRECTION.md` 返回唯一结构 owner和延期边界；`rg -n 'TraceEvent|record_schema_version|schema_version|state_version|migration|owner' AGENTS.md PROJECT_DIRECTION.md docs/architecture docs/evaluation docs/implementation README.md` 完成 impact inventory。</verify>
  <acceptance_criteria>
    - `TraceEvent` shared structure/version 恰有一个 owner
    - specialized event payload owner 未被 Project Direction 覆盖
    - 共享与专项 payload migration 的批准者均明确
    - runtime silent rewrite/upgrade/downgrade 未获授权
    - downstream owner files 被列入 handoff 而未在本 Packet 越界修改
  </acceptance_criteria>
  <done>TraceEvent 结构版本和未来 migration 决策不再使用“Core 或专项 owner”的模糊表述。</done>
</task>

</tasks>

<verification>

在 execution worktree 从仓库根目录运行以下可复制检查：

```bash
set -euo pipefail
test "$(git branch --show-current)" = "codex/e2e01-01-schema-owner-alignment"
test "$(git rev-parse HEAD)" = "624475681847be5a8e463e32dafd28a0483b213b"
test -z "$(git status --short)"
test "$(git merge-base HEAD 624475681847be5a8e463e32dafd28a0483b213b)" = "624475681847be5a8e463e32dafd28a0483b213b"
git fetch origin integration/e2e01-thin
planning_merge_sha="$(git rev-parse FETCH_HEAD)"
test "$(git rev-parse origin/integration/e2e01-thin)" = "$planning_merge_sha"
git merge-base --is-ancestor 624475681847be5a8e463e32dafd28a0483b213b "$planning_merge_sha"
test "$(git rev-list --count 624475681847be5a8e463e32dafd28a0483b213b.."$planning_merge_sha")" -eq 1
expected_planning_files="$(printf '%s\n' \
  .planning/ACTIVATION.md \
  .planning/PROJECT.md \
  .planning/REQUIREMENTS.md \
  .planning/ROADMAP.md \
  .planning/STATE.md \
  .planning/phases/01-cycle-1-e2e-01/01-01-PLAN.md \
  README.md \
  docs/implementation/e2e01-thin-slice-multi-agent-plan.md | sort)"
actual_planning_files="$(git diff --name-only 624475681847be5a8e463e32dafd28a0483b213b.."$planning_merge_sha" | sort)"
test "$actual_planning_files" = "$expected_planning_files"
git cat-file -e "${planning_merge_sha}:.planning/phases/01-cycle-1-e2e-01/01-01-PLAN.md"
git cat-file -e "${planning_merge_sha}:docs/implementation/e2e01-thin-slice-multi-agent-plan.md"
planning_plan_blob="$(git rev-parse "${planning_merge_sha}:.planning/phases/01-cycle-1-e2e-01/01-01-PLAN.md")"
planning_multi_agent_blob="$(git rev-parse "${planning_merge_sha}:docs/implementation/e2e01-thin-slice-multi-agent-plan.md")"
for required_marker in \
  'repository: `https://github.com/weijie567/mini-agent`' \
  'remote: `origin`' \
  'head_branch: `codex/e2e01-01-schema-owner-alignment`' \
  'base_branch: `integration/e2e01-thin`' \
  'base_sha: `624475681847be5a8e463e32dafd28a0483b213b`' \
  'worktree_id: `e2e01-01-schema-owner-alignment`' \
  'owned_files:' \
  '- `PROJECT_DIRECTION.md`' \
  'forbidden_files:' \
  '- `.planning/**`' \
  '- `AGENTS.md`' \
  '- `README.md`' \
  '- `docs/**`' \
  '- `src/**`' \
  '- `tests/**`' \
  '- `evals/**`' \
  '- `alembic/**`' \
  '- `pyproject.toml`' \
  '- `uv.lock`' \
  '- `compose.yaml`' \
  '- `graphify-out/**`'
do
  git grep -F -q -- "$required_marker" "$planning_merge_sha" -- \
    .planning/phases/01-cycle-1-e2e-01/01-01-PLAN.md
done
git grep -F -q -- '本地 checkout 不得作为 Plan provenance' "$planning_merge_sha" -- \
  docs/implementation/e2e01-thin-slice-multi-agent-plan.md
test \
  "$(git rev-parse 624475681847be5a8e463e32dafd28a0483b213b:PROJECT_DIRECTION.md)" = \
  "$(git rev-parse "${planning_merge_sha}:PROJECT_DIRECTION.md")"
printf 'planning_merge_sha=%s\nplanning_plan_blob=%s\nplanning_multi_agent_blob=%s\n' \
  "$planning_merge_sha" "$planning_plan_blob" "$planning_multi_agent_blob"
```

写入并形成一个原子 commit 后运行：

```bash
test "$(git branch --show-current)" = "codex/e2e01-01-schema-owner-alignment"
test "$(git merge-base HEAD 624475681847be5a8e463e32dafd28a0483b213b)" = "624475681847be5a8e463e32dafd28a0483b213b"
test "$(git rev-list --count 624475681847be5a8e463e32dafd28a0483b213b..HEAD)" -eq 1
git log --format='%H %P %s' 624475681847be5a8e463e32dafd28a0483b213b..HEAD
test "$(git diff --name-only 624475681847be5a8e463e32dafd28a0483b213b...HEAD)" = "PROJECT_DIRECTION.md"
git diff --check 624475681847be5a8e463e32dafd28a0483b213b...HEAD
rg -n 'semantic owner|Python source owner|Port declaration owner|adapter owner|record_schema_version|TraceEvent|logical migration|physical.*migration' PROJECT_DIRECTION.md
rg -n 'TraceEvent|record_schema_version|schema_version|state_version|migration|owner' AGENTS.md PROJECT_DIRECTION.md docs/architecture docs/evaluation docs/implementation README.md
uv run pytest
```

Expected:

- planning-status provenance resolves to exactly one integration commit, the declared eight files and a readable Plan blob;
- current exact head is non-base and recorded for review;
- exactly one commit and one changed file;
- `git diff --check` exits 0;
- required owner/version/Trace/migration terms are present without an implementation claim;
- `uv run pytest` exits 0; this proves only the existing offline gate did not regress.

Independent Reviewer must inspect the exact recorded head and all commit/diff output. Any unexpected commit, file, owner conflict, security invariant regression or unresolved `CRITICAL / HIGH / MEDIUM` finding is `BLOCK`; do not push.

</verification>

<success_criteria>

- Both tasks complete.
- All copied checks pass with captured output.
- Exact one-file / one-commit containment holds.
- Project Direction provides one unambiguous upstream rule for Plans 01-02 and 01-03.
- No specialized owner, source, tests, migration, derived State or Case lifecycle changes.
- Plan 01-02 remains blocked until this exact-head PR is reviewed and merged.

</success_criteria>

<output>
Execution branch不得写 `SUMMARY.md` 或共享 State。Executor must read this Plan from the captured `planning_merge_sha` with `git show`, and its handoff must include `planning_merge_sha` plus `planning_plan_blob`; a local pre-merge checkout is not Plan provenance。Executor only returns the Task Packet handoff. After owner PR merge, Integrator creates a separate planning-status PR for `01-01-SUMMARY.md`, Roadmap/State consumers and exact Plan 01-02.
</output>
