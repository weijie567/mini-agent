---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R4E2B
type: infrastructure-request-understanding-v3-cutover
depends_on: [02-18R19R4E2AR]
files_modified:
  - src/mini_agent/infrastructure/persistence/models.py
  - src/mini_agent/infrastructure/persistence/postgres.py
  - alembic/versions/20260803_0007_request_understanding_v3_cutover.py
  - tests/integration/test_database_migrations.py
  - tests/integration/test_postgres_v3_request_understanding_writes.py
requirements: [E2E01-01, E2E01-02, E2E01-03, E2E01-04, E2E01-05, E2E01-06]
---

# 02-18R19R4E2B｜W12 RU v3 physical cutover and staged Infrastructure

```yaml
task_id: 02-18R19R4E2B
goal: 在单一Infrastructure ownership与五文件边界内实现已冻结的request_understanding_record.p0.v3 physical pair、原子v2→v3 Alembic cutover、六个staging-only v3 writer、Phase 1/Cycle 2两个one-snapshot evidence reader及无参数global readiness fence；保持全部现有v2 caller/active route不变，等待后续Composition统一激活。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-r4e2b-infrastructure
base_branch: integration/e2e01-cycle2
base_sha: f9c9c49344d2f25a2883ace6f7c6a26d4b2301f7
base_tree: 84ec2baf8e7c521bf5892bcd1b5a35f2800e811a
worktree_id: e2e01-cycle2-w12-r4e2b-infrastructure
agent_role: infra-engineer
writer: w12-ru-v3-infrastructure-owner
owned_files:
  - src/mini_agent/infrastructure/persistence/models.py
  - src/mini_agent/infrastructure/persistence/postgres.py
  - alembic/versions/20260803_0007_request_understanding_v3_cutover.py
  - tests/integration/test_database_migrations.py
  - tests/integration/test_postgres_v3_request_understanding_writes.py
forbidden_files:
  - all files outside owned_files
  - src/mini_agent/core/**
  - src/mini_agent/application/**
  - src/mini_agent/evaluation/**
  - src/mini_agent/runtime/**
  - tests/component/**
  - tests/e2e/**
  - evals/**
  - docs/**
  - .planning/**
  - AGENTS.md
  - pyproject.toml
  - uv.lock
  - graphify-out/**
required_contract_blobs:
  docs/implementation/e2e01-thin-slice-implementation-spec.md: f145f31983022167dae077dc956f13365cf5ff13
  docs/implementation/e2e01-cycle2-implementation-spec.md: 7d62c6990cb4b742071262d2f0d9a52727763b0a
  docs/architecture/intent-design-reference.md: 8fe1120a49ba2b4b4c12f428ada9e199472a0c
  docs/architecture/memory-design-reference.md: fdcdd3c751eb03b7462257cfd62354c9c2917a69
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
required_dependency_blobs:
  .planning/phases/02-cycle-2-e2e-01/02-18R19R4E1-PLAN.md: f5b02d7fde3a231971eacb5f38629016a73e1fb5
  .planning/phases/02-cycle-2-e2e-01/02-18R19R4E2AR-PLAN.md: fd4f37d182c576878e45ede2c833fb57d05657a2
  src/mini_agent/application/persistence.py: d556bfd98f4e6d79ddc2ba888b0a1cd1cc8b35e8
  src/mini_agent/application/records.py: 8ffcce78b8b0dc23bb35877143afdf67415cfa34
  src/mini_agent/application/ports.py: 38c909155afac43e4880cbc54c0e6b3ecfd056a3
  src/mini_agent/core/request_processing.py: 6183510fce33049758ffec9d19ce7e32b6e4dd36
  src/mini_agent/core/task_state.py: 3a279b9156eb03768d58c5aa7029479c1da9a974
required_source_blobs:
  src/mini_agent/infrastructure/persistence/models.py: 722e4aa3d17525d404b453e8950b2e2fbcdd64ab
  src/mini_agent/infrastructure/persistence/postgres.py: a5e332e5279f7b3faf074cb77b47768b74b80b8c
  tests/integration/test_database_migrations.py: 57bead90880e2cf55519d6dbf4813fb59ca7d868
  alembic/versions/20260803_0007_request_understanding_v3_cutover.py: NOT_FOUND_NEW_FILE
  tests/integration/test_postgres_v3_request_understanding_writes.py: NOT_FOUND_NEW_FILE
preflight_facts:
  - signed Application codec已经提供显式v2 source/v3 target versioned codec，但active semantic registry仍指向v2；signed Application evidence contract已经声明六个v3 command、两个v3 evidence closure/Port与一个readiness Port
  - 当前models.py与数据库closed physical pair只接受Request Understanding v1/v2；PostgresRecordAdapter尚未实现任何v3 writer、v3 evidence reader或readiness，因而R4F与Composition被真实阻断
  - 当前v2 writer/reader及其caller仍是active路径；本Packet不得修改、替换、删除或重新路由它们，源码合并与isolated migration tests都不等于active switch
  - physical schema继续使用既有p0_records、p0_record_references、p0_record_state_history及parent-local accepted_task_delta child；不新增table、column、index、record family或version
packet_split_ruling:
  - R4E1只拥有Application semantic codec；R4E2AR只拥有Application evidence DTO/Port；本R4E2B只拥有Infrastructure/Alembic实现，严格串行消费两者
  - migration、PostgreSQL staging writer/reader与physical readiness均属于同一Infrastructure owner，可在一个Packet内共同闭合；不得跨入Application、Runtime、Eval或Composition source
  - R4F只消费本包staged v3 readers并修改Eval；Composition随后以多个owner-local串行小包执行active registry/runtime/eval/Composition Root switch，不以本包偷跑激活
contract_freeze:
  baseline: 253bb6bd8dc2f84d6ca95a98f625d80fa80e5461
  status: FROZEN
  allowed_changes: only the physical migration, six writers, two readers and readiness barrier required by 27 Cycle 2 Cases, 16 Phase 1 variants and final Composition
  forbidden_expansion: new schema version, record family, table, column, Case, metric, feature, architecture abstraction, repository layer, generic persistence API, unrelated refactor or non-blocking optimization
  code_freeze_gate: after reviewed R19R5R merge
owner_decisions:
  physical_pair:
    - add only (request_understanding_record, request_understanding_record.p0.v3) to the existing closed physical pair set in models.py and revision 0007; keep v1 archival and v2 migration-source pairs physically admitted until an exact successful downgrade/cutover lifecycle permits otherwise
    - AcceptedTaskDelta remains a parent-local logical child selected by the exact parent version and gains no independent physical pair, table or version axis
  migration_upgrade:
    - revision 20260803_0007 revises exact 20260802_0006, acquires a table lock before any preflight/write and replaces the closed constraint only inside the same transactional upgrade
    - preflight every active request_understanding_record.p0.v2 parent, its inherited accepted_task_delta children, references and owner graph with the frozen explicit v2 codec; convert only the generic Phase 1 ADD_GOAL/e2e01-thin-v2 branch through the frozen explicit v3 codec
    - preserve every parent/child identity, run/message, trusted time, both model axes, contextualization/provenance bytes, Candidate emitted order, keyed validation, accepted exact set, child storage order, Binding ref order, per-Task base/result chain, NextMove audit, logical identity, owner, run_id and canonical references
    - write no target until all sources are exactly convertible; any malformed/partial/mixed/unknown/wrong-owner/dangling/non-invertible graph aborts the entire upgrade with one bounded stable error and zero mutation
    - after conversion require global active v2 residual zero and exact v3 decode/owner/provenance/reference closure for every target; v1 remains archival and is not converted or accepted as current
  migration_downgrade:
    - lock and preflight the entire v3 set before mutation; only exact generic Phase 1 ADD_GOAL + e2e01-thin-v2 v3 closures are reversible
    - any Cycle 2 initial, SUPPLY_INPUT, continuation output, unknown/future/malformed/incomplete/non-invertible v3 blocks the whole downgrade with zero writes
    - atomically restore exact v2 bytes/refs/identity/owner graph, require zero residual v3, then restore the pre-v3 constraint; no delete-and-ignore, fallback, read-time conversion, repair or partial rollback
  staged_writers:
    - implement exactly save_request_understanding_v3_no_task_if_current, create_initial_task_graph_v3_if_current, create_cycle2_initial_task_graph_v3_if_current, save_rejected_continuation_understanding_if_current, apply_continuation_task_delta_if_current and apply_order_candidate_selection_v3_if_current
    - encode Request Understanding only through the explicit v3 versioned codec; all other families retain their already-frozen exact physical versions; never call an existing v2 writer as a sub-step and never fall back to v2
    - every writer uses one owner-scoped transaction and deterministic lock order. It validates authoritative Message provenance, expected current roots, complete owner/reference closure and exact Task/RequestUnit/Binding effects before mutation
    - identity-first precedence is mandatory: an existing same logical identity with exact canonical envelope/references/effects returns ALREADY_APPLIED even if current Task advanced; same identity with any byte/reference/effect divergence returns PROJECTION_CONFLICT; only an absent identity proceeds to exact current CAS and returns APPLIED or NOT_APPLICABLE
    - all ALREADY_APPLIED/PROJECTION_CONFLICT/NOT_APPLICABLE paths perform zero writes and grant no dispatch authority. First-writer races produce exactly one APPLIED and deterministic exact replay/conflict outcomes without partial rows
    - generic no-task preserves zero/all-reject/partial/multi semantics; generic initial preserves all accepted ADD_GOAL effects; Cycle 2 initial atomically creates its v3 RU/Task/RequestUnit/Binding/link graph; accepted continuation advances Task/RequestUnit exactly once
    - continuation rejected/accepted and ordinal selection keep the frozen mutually-exclusive command paths. DNR preserves same-fresh-order dual Binding and exact same-name/same-value USER_CLAIM supersession; ordinal selection performs RU v3 closure plus selection/target/Task/RequestUnit effects in one CAS
  staged_readers:
    - implement load_exact_run_evidence_v3_for_owner and load_cycle2_exact_run_evidence_v3_for_owner with exact TrustedOwnerScope/UUID inputs and their frozen v3 return types; None means only absent/unauthorized root
    - each reader performs all root discovery, closed graph traversal, exact versioned decode, owner/reference/cardinality checks and authoritative Message span/SHA-256 verification in one REPEATABLE READ, READ ONLY transaction; no second-call RU stitching
    - selected current graph containing RU v1/v2/future, mixed parent/child version, incomplete/dangling/wrong-owner provenance or wrong branch fails through existing bounded P0PersistenceIntegrityError before evidence is returned
    - Phase 1 returns independent ExactRunEvidenceV3Closure; Cycle 2 returns the complete existing Cycle2 graph plus every applicable RequestUnderstandingClosureV3, preserving no-record versus persisted zero-child distinctions and exact cross-parent Task timeline
  readiness:
    - implement assert_request_understanding_v3_ready() with no parameters and None-only success under one consistent global fence
    - prove revision/constraint/static pair installation even when there are no active RU rows, global active v2 residual zero, and exact decode plus owner/reference/provenance/Task closure of every active v3 Phase 1/Cycle 2 aggregate
    - v1 may exist only as archival evidence; readiness accepts no owner, Case, expectation, bypass or repair input, returns no boolean/count/data and grants no dispatch/result authority
    - use only the existing bounded P0PersistenceIntegrityError taxonomy; caller-controlled values, raw Message/Binding content, row counts and database diagnostics must not escape in error text, cause or context
  activation_boundary:
    - this Packet adds inactive implementations and a migration revision but changes no active caller, Application registry, Runtime service, Eval field/adapter, Composition Root or product startup route
    - deployment/activation must apply the reviewed migration and owner-local active switches as one stopped-traffic Composition barrier; no v2 request may race between successful conversion and v3-only route activation
implementation_tasks:
  - update the static physical pair set and add revision 0007 with full locked preflight, atomic upgrade conversion, postcondition validation and exact reversible/blocked downgrade matrix
  - implement the six Port methods in PostgresRecordAdapter using minimum private v3-specific helpers or branch-neutral extraction; preserve every public v2 method signature/result/behavior and avoid a public generic abstraction
  - implement both one-snapshot v3 evidence readers and the no-argument readiness fence; reuse existing bounded database/integrity boundaries without weakening exact version selection or sanitization
  - add migration tests for pre-0007 v3 DB rejection, exact v2→v3 full conversion including zero/all-reject/partial/multi, preserved bytes/refs/order/effects, preflight/postcondition failure atomicity, unknown pair rejection, v1 archival behavior, clean reversible downgrade, every v3-only downgrade blocker and upgrade/downgrade lock races
  - add one new focused PostgreSQL integration module covering all six writers: first APPLIED, exact replay after Task advance, same-ID mutation conflict, absent-ID stale base, concurrent first-writer race, owner isolation, provenance rejection, transaction rollback and no v2 fallback/residual
  - in the same focused module cover both v3 readers and readiness: complete Phase 1/Cycle 2 closures, one-snapshot behavior, authoritative Message hash/span, no-record versus zero-child, version/branch/owner/dangling/partial rejection, empty-table installed-schema readiness and global residual-v2 failure
  - run a repository-wide read-only active-caller/implementer scan allowing only the already-frozen inactive agent_run_service staging helpers, declarations and tests; mechanically prove no active route, Eval or Composition caller activates the new methods and all legacy v2 routes remain unchanged until Composition
contract_changes: IMPLEMENTS_APPROVED — materializes the already-frozen physical v3 cutover and Infrastructure staging surfaces. No canonical contract, active route, Case, metric, schema version, record family or user outcome change.
security_impact: POSITIVE — locked fail-atomic conversion, owner-scoped exact provenance reads, identity-first replay and a global residual-v2 fence prevent mixed-version authority, cross-owner stitching, partial effects and false readiness.
eval_impact: INFRASTRUCTURE_PREREQUISITE_ONLY — supplies the actual v3 evidence sources and readiness required by R4F/Composition; changes no EvalEvidence field, grader, Case, provider, dataset, artifact, metric or lifecycle.
review_profile:
  planning_review: INDEPENDENT_EXACT_FILE
  implementation_review: W12_RU_V3_INFRASTRUCTURE_EXACT_HEAD
  targeted_risk_checks:
    - exact five-file Infrastructure/Alembic allowlist and base/blob containment
    - migration full-set preflight before writes, exact preservation, zero v2 postcondition and zero-write failure/downgrade blockers
    - six writer identity-first precedence, zero-write non-APPLIED outcomes, deterministic locks/races and no v2 fallback
    - both readers use one owner-scoped REPEATABLE READ READ ONLY snapshot and authoritative Message provenance verification
    - readiness has zero inputs/None success, verifies installed physical barrier on empty data and fails global residual v2/malformed v3
    - no active Application/Runtime/Eval/Composition caller or route change and no new schema/table/record family
required_checks:
  - uv run pytest tests/integration/test_database_migrations.py
  - uv run pytest tests/integration/test_postgres_v3_request_understanding_writes.py
  - uv run pytest tests/integration/test_postgres_v2_request_understanding_writes.py
  - uv run pytest tests/integration/test_postgres_record_adapters.py tests/integration/test_postgres_atomicity.py
  - uv run pytest tests/integration
  - git diff --check
  - repository-wide read-only impact scan for all six staged writers, both v3 readers, readiness, RU physical pairs, migration head, frozen inactive agent_run_service staging helpers, active v2 implementers/callers and final R4F/Composition consumers
full_suite_gate: DEFERRED_TO_FINAL_REFROZEN_02_18
done_when:
  - migration, focused v3, legacy v2 neighbor, record-adapter/atomicity and complete Integration suites pass with isolated PostgreSQL namespaces
  - exact five-file scope, unchanged active v2 routes and absence of Application/Core/Runtime/Eval/Composition changes are mechanically proven
  - local independent implementation review and remote exact-head review are PASS with zero open findings
  - product merges as B_C2_W12_RU_V3_INFRASTRUCTURE and only then 02-18R19R4F planning begins
rollback:
  - before Composition activation, revert this inactive five-file Infrastructure packet atomically; revision downgrade is permitted only after its exact preflight succeeds and no v3-only evidence exists
  - after activation, first stop v3 writes/dispatch and run the exact global downgrade preflight; any Cycle 2 initial, SUPPLY_INPUT, continuation, unknown, malformed or incomplete v3 aggregate blocks all rollback with zero writes
  - after successful atomic v3→v2 conversion and zero-v3 proof, reverse owner-local Composition/Eval/Application switches, then revert this Packet and R4E2AR/R4E1 in dependency order
  - never delete or ignore evidence, restore a constraint over residual v3, re-enable mixed active readers/writers, repair on read or bypass readiness
handoff_format:
  - exact branch/base/head/tree, migration revision and changed files
  - upgrade/downgrade/readiness postconditions, six writer/two reader behavior, commands/results/NOT_RUN and active-caller scan
  - unchanged active routes, R4F/Composition blockers, risks and rollback eligibility
output_barrier: B_C2_W12_RU_V3_INFRASTRUCTURE
handoff_to: tech-lead
```
