---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R4D
type: application-implementation
depends_on: [02-18R19R4C]
files_modified:
  - src/mini_agent/application/records.py
  - src/mini_agent/application/ports.py
  - src/mini_agent/application/agent_run_service.py
  - tests/component/application/test_record_contracts.py
  - tests/component/application/test_ports_contract.py
  - tests/component/application/test_agent_run_service.py
requirements: [E2E01-01, E2E01-02, E2E01-03, E2E01-04, E2E01-05, E2E01-06]
---

# 02-18R19R4D｜W12 Request Understanding v3 Application closure

```yaml
task_id: 02-18R19R4D
goal: 在Application边界消费已合并的RU v3 Core closure，冻结generic Phase 1 initial、Cycle 2 initial、rejected/accepted continuation与ordinal selection的exact command/Port/service编排；实现DNR双Binding一次Task投影与accepted effect Trace，但保持现有v2运行路径、PostgreSQL、Eval与Composition不切换。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-dnr-application
base_branch: integration/e2e01-cycle2
base_sha: c702e87325017e854011f425ed59246db36d9e67
base_tree: 79acb18075213dc5c70f7917c87eb6e7c83f7605
worktree_id: e2e01-cycle2-w12-dnr-application
agent_role: runtime-engineer
writer: w12-ru-v3-application
owned_files:
  - src/mini_agent/application/records.py
  - src/mini_agent/application/ports.py
  - src/mini_agent/application/agent_run_service.py
  - tests/component/application/test_record_contracts.py
  - tests/component/application/test_ports_contract.py
  - tests/component/application/test_agent_run_service.py
forbidden_files:
  - all files outside owned_files
  - src/mini_agent/core/**
  - src/mini_agent/infrastructure/**
  - src/mini_agent/evaluation/**
  - tests/component/core/**
  - tests/integration/**
  - tests/e2e/**
  - alembic/**
  - evals/**
  - docs/**
  - .planning/**
  - AGENTS.md
  - graphify-out/**
required_contract_blobs:
  docs/implementation/e2e01-thin-slice-implementation-spec.md: f145f31983022167dae077dc956f13365cf5ff13
  docs/implementation/e2e01-cycle2-implementation-spec.md: 7d62c6990cb4b742071262d2f0d9a52727763b0a
  docs/architecture/intent-design-reference.md: 8fe1120a49ba2b4b4b4c12f428ada9e199472a0c
  docs/architecture/memory-design-reference.md: fdcdd3c751eb03b7462257cfd62354c9c2917a69
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
required_dependency_blobs:
  .planning/phases/02-cycle-2-e2e-01/02-18R19R4C-PLAN.md: 5b8187b7d3eee9058b01c1467d18f62101850bd8
  src/mini_agent/core/request_understanding.py: 9f77de879c902c13365410fa65ca333c092b02cf
  src/mini_agent/core/task_state.py: 3a279b9156eb03768d58c5aa7029479c1da9a974
  src/mini_agent/core/request_processing.py: 6183510fce33049758ffec9d19ce7e32b6e4dd36
required_source_blobs:
  src/mini_agent/application/records.py: 04195b8f473d1275aba3be968cb134cc235be5d3
  src/mini_agent/application/ports.py: d7cb33f8bdf73ed1dd32bdaa46079e072a53c43c
  src/mini_agent/application/agent_run_service.py: 870fa2c346af3bcee9590d10830087b6b8678775
  tests/component/application/test_record_contracts.py: c19ee6f232247da8da8dab37611127b80c45361c
  tests/component/application/test_ports_contract.py: c4d684e8b5e6580fb648beebfc295b588983cd3a
  tests/component/application/test_agent_run_service.py: 08418d88eeea47fba048c845176bb7df70c5e312
preflight_facts:
  - merged Core exposes RequestUnderstandingClosureV3, Cycle2InitialRequestDecisionV3, Cycle2ContinuationDecisionV3, RejectedCycle2ContinuationDecisionV3 and the exact identity allocations/reducers
  - current generic Phase 1 commands and PersistencePort methods persist only RequestUnderstandingRecordV2/AcceptedTaskDeltaV2
  - current Cycle 2 initial command embeds Cycle2InitialRequestDecisionV2 and current continuation Port writes one flat ApplyContinuationInputBindingV2Command
  - current Cycle2RequestUnderstandingProvider.propose_cycle2_continuation returns one flat Cycle2InputCandidate; active service branches by that one binding
  - current ordinal selection command has no RU closure or accepted effect Trace, and rejected schema-valid continuation has no durable RU parent writer
owner_decisions:
  - add exact v3 Application command models for generic Phase 1 initial/no-task closure, Cycle 2 initial, rejected continuation, accepted ordinary/DNR continuation and accepted ordinal selection; every nested Core model must be exact type and recursively canonical
  - generic Phase 1 v3 commands preserve existing Message/Conversation/Run/Task/RequestUnit/Binding/link ownership, NextMove audit and the full zero/multi/partial/multi-accept closure. Zero/all-reject uses the no-task command; any accepted closure uses one atomic tuple command whose accepted task graph items equal Candidate-sequence-filtered ACCEPT children in exact order. Each item closes its exact ordered non-empty Binding refs, Task, RequestUnit and links; per-task base/result chains are contiguous and the whole tuple commits or writes nothing. Current ADD_GOAL creates base-null/result-one Task effects, but validators must not copy the old exact-one emitted-Candidate restriction. v2 commands remain byte-compatible migration-source consumers and are not aliases or fallback
  - Cycle 2 initial v3 command replaces only its embedded reducer decision with Cycle2InitialRequestDecisionV3 and verifies the parent-local accepted child, one product_description Binding, result version 1, non-null search NextMove ref and exact accepted effect Trace
  - SaveRejectedContinuationUnderstandingV3Command contains only the loaded owner closure and RejectedCycle2ContinuationDecisionV3; it writes no accepted child, Binding, Task/RequestUnit effect, target, selection or RU-correlated Trace
  - ApplyContinuationTaskDeltaV3Command contains loaded closure, accepted decision, one next Task, one next RequestUnit, exact accepted effect Trace and optional rejected-ordinal child. It accepts 1..2 ordered fresh Bindings from the decision, exact-replaces each unique current same-name ref or appends it, and advances Task/RequestUnit exactly once regardless of Binding cardinality
  - accepted effect Trace is exactly one TaskDeltaValidated, one TaskDeltaAccepted, one InputBindingRecorded per ordered Binding and one TaskStateChanged; all events share root Run, accepted_delta_ref, Task, RequestUnit and the Core trusted timestamp. Rejected RU has no such accepted effect Trace
  - ApplyOrderCandidateSelectionV3Command preserves the live in-process target issuance fence and existing selection graph while adding the exact continuation decision and accepted effect Trace; ordinal Binding identity must be the decision's only Binding
  - DNR routing is selected only by decision.routing_trigger_binding_ref, which must name the second shipment_not_received=true Binding. The verified target and get_shipment argument origin remain the loaded product_description AutoTarget closure; fresh order/DNR Claims never become target authority or argument_binding_refs
  - all v3 writers use identity-first result precedence in their Port contract: exact persisted request_understanding_record_id closure retry returns ALREADY_APPLIED before stale-current checks; same id/different bytes returns PROJECTION_CONFLICT; absent id then performs current-graph CAS and returns APPLIED or NOT_APPLICABLE. Every non-APPLIED result grants no Tool dispatch
  - because R19R4D is expand-only/non-routed, add explicitly staging-only v3 Port/provider entry points where an active v2 name collides. Existing service entry points continue calling v2 methods. R19 Composition alone removes staging names and atomically switches canonical active names/signatures after R4E/R4F/R5R gates; staging entry points are never fallback or dual-read authority
  - Application service gains deterministic, separately testable v3 command/projection orchestration for generic initial, Cycle 2 initial and continuation. It receives every UUID and trusted UTC sample from existing factories/clock, calls the v3 Core reducer once, builds one atomic command and interprets Cycle2WriteResult without treating enums as authority. Only first-write APPLIED may proceed to a new Tool dispatch; ALREADY_APPLIED, PROJECTION_CONFLICT and NOT_APPLICABLE never create or dispatch a new ToolCall. An ALREADY_APPLIED re-read may only support bounded safe result mapping/recovery of already persisted downstream state and cannot enter a new routing path; this Packet does not invent a downstream-effect replay reader
  - no Application code may sample business fact, infer DNR from wording, manufacture owner/target scope, reconstruct safe provenance, alter Case expectations or write persistence directly
public_application_surface:
  command_types:
    - InitialAcceptedTaskGraphV3CommandItem
    - SaveRequestUnderstandingV3NoTaskCommand
    - CreateInitialTaskGraphV3Command
    - CreateCycle2InitialTaskGraphV3Command
    - SaveRejectedContinuationUnderstandingV3Command
    - ApplyContinuationTaskDeltaV3Command
    - ApplyOrderCandidateSelectionV3Command
  generic_command_shapes:
    InitialAcceptedTaskGraphV3CommandItem:
      - accepted_delta: AcceptedAddGoalTaskDeltaV3
      - initial_task: CreateTaskCommand
      - initial_request_unit: CreateRequestUnitCommand
      - input_bindings: tuple[SaveInputBindingCommand, ...] with min_length=1 and exact ordered refs equal accepted_delta.input_binding_refs
      - conversation_task_link: ConversationTaskLinkRecord
      - run_task_link: CreateRunTaskLinkCommand
    SaveRequestUnderstandingV3NoTaskCommand:
      - owner_scope: TrustedOwnerScope
      - expected_conversation_record: ConversationRecord
      - expected_message_records: tuple[MessageRecord, ...] with 1..8 unique exact referenced Messages
      - expected_active_run_record: AgentRunRecord
      - request_understanding: RequestUnderstandingClosureV3 with zero accepted children and zero accepted refs
    CreateInitialTaskGraphV3Command:
      - owner_scope: TrustedOwnerScope
      - expected_conversation_record: ConversationRecord
      - expected_message_records: tuple[MessageRecord, ...] with 1..8 unique exact referenced Messages
      - expected_active_run_record: AgentRunRecord
      - request_understanding: RequestUnderstandingClosureV3 with accepted_count >= 1
      - accepted_task_graphs: tuple[InitialAcceptedTaskGraphV3CommandItem, ...] with length exactly accepted_count
      - accepted_task_graphs order equals task_delta_candidates emitted sequence filtered by keyed ACCEPT, never accepted_delta_refs order or storage order
      - the tuple validates per-task base/result continuity, exact child/Binding/Task/RequestUnit/link identity sets and commits as one indivisible graph
  staging_port_methods:
    - save_request_understanding_v3_no_task_if_current
    - create_initial_task_graph_v3_if_current
    - create_cycle2_initial_task_graph_v3_if_current
    - save_rejected_continuation_understanding_if_current
    - apply_continuation_task_delta_if_current
    - apply_order_candidate_selection_v3_if_current
    - propose_cycle2_continuation_v3
  staging_rules:
    - the suffix marks pre-Composition non-routed expansion only, not a second active version family
    - no caller may try v3 then v2, v2 then v3, decode a union, infer latest, repair on read or accept either command type on one method
    - final Composition must switch the canonical Spec names and remove/deactivate every staging-only method atomically
implementation_tasks:
  - add recursively exact v3 command models and shared validators/projection helpers in records.py; require exact closure/child/binding/order/version/time/identity sets and preserve live ordinal target issuance. Generic no-task and accepted-tuple commands must implement the frozen field/cardinality shapes above, Candidate-order filtering, per-Task chain validation and all-closure atomicity without inheriting the old exact-one Candidate rule
  - extend PersistencePort/Cycle2RuntimeRecordPort/Cycle2RequestUnderstandingProvider with exact-type staging v3 methods and closed Cycle2WriteResult semantics; keep every existing v2 signature unchanged in this Packet
  - add non-routed service orchestration helpers/methods that allocate all v3 identities, invoke Core v3 reducers, construct exact initial/rejected/accepted/ordinal commands, project multi-Binding RequestUnit refs in decision order, emit accepted effect Trace and choose the DNR trigger without altering active _handle_continuation_turn or active generic Phase 1 flow
  - add record contract tests for generic zero-Candidate, all-reject, partial-accept and multi-accept closures; require accepted task graph tuple order to equal Candidate-filtered ACCEPT order, validate exact per-child Binding refs and per-Task base/result chains, and reject missing/extra/reordered/partial graph or forked chain atomically. Also cover Cycle 2 initial, rejected no-effect, ordinary single Binding, DNR dual replacement/append, dual-current supersession, one Task advance, exact trace cardinality/order/correlation, ordinal live issuance, identity collisions, recursive exact type and mutation rejection
  - add Port contract tests for exact staging method names/signatures/types/results, v2 surface preservation and explicit absence of aliases/fallback unions
  - add service tests using spies for generic zero/all-reject no-task plus partial/multi-accept atomic tuple command construction; continuation zero/one/two Binding decisions, keyed rejection, exact UUID/time allocation, exact command bytes on deterministic retry, APPLIED/ALREADY_APPLIED/PROJECTION_CONFLICT/NOT_APPLICABLE handling, DNR second-trigger routing and unchanged active v2 call graph. Assert only APPLIED enters a new Tool route and every non-APPLIED result performs zero new ToolCall creation/dispatch
contract_changes: IMPLEMENTS_APPROVED — implements the approved RU v3 Application atomic command and orchestration boundary; temporary staging-only names exist solely because the active v2 route cannot switch before Infrastructure/data/Eval readiness. No canonical owner, model schema, Case, fixture or user outcome changes.
security_impact: POSITIVE — owner scope stays server-derived; exact nested types, all-or-nothing dual Binding, identity-first replay and live ordinal issuance prevent partial writes, replay mutation and target-authority borrowing.
eval_impact: COMPONENT_ONLY — adds Application contract/trajectory-seam evidence but does not change EvalEvidence, artifact, provider script, Case lifecycle or Result.
review_profile:
  planning_review: INDEPENDENT_EXACT_FILE
  implementation_review: W12_RU_V3_APPLICATION_EXACT_HEAD
  targeted_risk_checks:
    - exact six-file allowlist, base/blob containment and no edits to Core/Infrastructure/Eval/migration/docs/planning
    - v2 public behavior remains exact and active route still uses only v2 methods
    - v3 nested command types are recursively exact, no alias/union/fallback/latest semantics
    - rejected closure has zero accepted effect; DNR has two Bindings, one Task advance and second routing trigger
    - accepted Trace count/order/correlation and identity-first result precedence are exact
    - no target authority from fresh order/DNR Claims and no Application-authored safe provenance/business facts
required_checks:
  - uv run pytest tests/component/application/test_record_contracts.py tests/component/application/test_ports_contract.py tests/component/application/test_agent_run_service.py
  - uv run pytest tests/component/application
  - uv run pytest tests/component/core/test_request_processing.py tests/component/core/test_task_state_contract.py
  - git diff --check
  - repository-wide read-only impact scan for new v3 Application symbols, active v2 routes and downstream Infrastructure/Eval/Composition consumers
full_suite_gate: DEFERRED_TO_FINAL_REFROZEN_02_18
done_when:
  - all focused, Application component and Core neighbor tests pass
  - existing active v2 route remains mechanically unchanged and no persistence/Eval source is modified
  - local independent implementation review and remote exact-head review are PASS with zero open findings
  - product merges as B_C2_W12_RU_V3_APPLICATION and only then 02-18R19R4E planning begins
rollback:
  - before R19R4E consumes the staging surface, revert this six-file Application expansion atomically with no data rollback
  - after R19R4E or later merges, first revert/deactivate R19 Composition if active, then roll back R19R5R → R19R4F → R19R4E, satisfy the Infrastructure/data downgrade gate, and only then revert R19R4D; Core may remain as unused expansion until its own reverse-order rollback
handoff_format:
  - exact branch/base/head/tree and changed files
  - exact command/Port/service surfaces, identity/replay/Trace/routing decisions, tests and results
  - unchanged active v2 route, staging-only status, downstream blockers and rollback
output_barrier: B_C2_W12_RU_V3_APPLICATION
handoff_to: tech-lead
```
