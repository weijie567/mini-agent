---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R4E1
type: application-persistence-codec
depends_on: [02-18R19R4D]
files_modified:
  - src/mini_agent/application/persistence.py
  - tests/component/application/test_persistence_contract.py
requirements: [E2E01-01, E2E01-02, E2E01-03, E2E01-04, E2E01-05, E2E01-06]
---

# 02-18R19R4E1｜W12 Request Understanding v3 semantic codec

```yaml
task_id: 02-18R19R4E1
goal: 在Application持久化语义边界增加显式request_understanding_record.p0.v3 closed versioned codec，完整支持generic Phase 1 ADD_GOAL、Cycle 2 initial与SUPPLY_INPUT child，并保持v2 active registry与运行路由不变；本包不改物理schema、数据或PostgreSQL writer。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-ru-v3-codec
base_branch: integration/e2e01-cycle2
base_sha: 253bb6bd8dc2f84d6ca95a98f625d80fa80e5461
base_tree: fad26473621497252cbaa4eb94a1832cde366ffa
worktree_id: e2e01-cycle2-w12-ru-v3-codec
agent_role: runtime-engineer
writer: w12-ru-v3-codec-owner
owned_files:
  - src/mini_agent/application/persistence.py
  - tests/component/application/test_persistence_contract.py
forbidden_files:
  - all files outside owned_files
  - src/mini_agent/core/**
  - src/mini_agent/application/records.py
  - src/mini_agent/application/ports.py
  - src/mini_agent/application/agent_run_service.py
  - src/mini_agent/infrastructure/**
  - tests/integration/**
  - tests/e2e/**
  - alembic/**
  - evals/**
  - docs/**
  - .planning/**
  - AGENTS.md
  - graphify-out/**
required_contract_blobs:
  docs/implementation/e2e01-thin-slice-implementation-spec.md: f145f31983022167dae077dc956f13365cf5ff13
  docs/implementation/e2e01-cycle2-implementation-spec.md: 7d62c6990cb4b742071262d2f0d9a52727763b0a
  docs/architecture/intent-design-reference.md: 8fe1120a49ba2b4b4b4c12f428ada9e199472a0c
  docs/architecture/memory-design-reference.md: fdcdd3c751eb03b7462257cfd62354c9c2917a69
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
required_dependency_blobs:
  .planning/phases/02-cycle-2-e2e-01/02-18R19R4D-PLAN.md: 1e9516d96db28dc2d3f7c39288abd15f4553298f
  src/mini_agent/core/task_state.py: 3a279b9156eb03768d58c5aa7029479c1da9a974
  src/mini_agent/core/request_processing.py: 6183510fce33049758ffec9d19ce7e32b6e4dd36
required_source_blobs:
  src/mini_agent/application/persistence.py: 0d37764f5673fa7e65345e07114f7626f90ae142
  tests/component/application/test_persistence_contract.py: 25849f4a01ce23d26669c3d9fb6a5d0033fa9e40
preflight_facts:
  - merged Core already defines exact RequestUnderstandingRecordV3, Durable Phase 1/Cycle 2 Candidate union, AcceptedAddGoalTaskDeltaV3 and AcceptedSupplyInputTaskDeltaV3
  - merged Application commands carry exact RequestUnderstandingClosureV3 but PostgreSQL cannot encode it because application/persistence.py currently registers only request_understanding_record.p0.v2
  - the physical p0_records CHECK also rejects request_understanding_record.p0.v3, so codec readiness alone cannot activate or persist v3
  - current v2 versioned codec and tests are required migration-source evidence and must remain callable only by explicit code/version selection
packet_split_ruling:
  - R19R4E is split into R4E1 semantic codec then R4E2 Infrastructure/Alembic because one Task Packet may not cross Application semantic ownership and Infrastructure/migration ownership
  - R4E1 owns explicit source/target versioned codec coexistence only; R4E2 owns physical expand acceptance, PostgreSQL staging writers/readers and integration evidence
  - neither R4E sub-packet activates v3. R19 Composition is an Integrator release barrier, not a cross-owner Task Packet: it serially integrates exact owner-local Application active-registry/runtime-switch, Infrastructure atomic data-cutover, Eval activation and Composition Root packets, then proves the residual-v2 readiness fence before the barrier is releasable
  - each owner-local cutover Packet has its own exact branch/worktree/writer/allowlist/review; no single Plan or commit may own mutations from more than one canonical ownership boundary
contract_freeze:
  baseline: 253bb6bd8dc2f84d6ca95a98f625d80fa80e5461
  status: FROZEN
  allowed_changes: only blockers for exact v3 codec readiness required by 27 Cycle 2 Cases, 16 Phase 1 variants, approved safety invariants, migration or Composition
  forbidden_expansion: new schema version, record family, Case, metric, feature, architecture abstraction, generic refactor or non-blocking optimization
  code_freeze_gate: after reviewed R19R5R merge
owner_decisions:
  - add exact request_understanding_record.p0.v3 top-level schema spec using RequestUnderstandingRecordV3 with no schema_version mirror; record_schema_version remains an explicit logical field and must equal the selected pair
  - add one v3 accepted_task_delta child spec selected strictly by the parent version and accepting only exact AcceptedAddGoalTaskDeltaV3 or AcceptedSupplyInputTaskDeltaV3; child has no independent version
  - preserve parent field order, model input/output axes, Candidate emitted order, keyed validation, accepted exact set, trusted timestamp, NextMove audit and all safe provenance bytes exactly
  - child closure order equals parent Candidate sequence filtered by ACCEPT. Parent accepted_delta_refs is an order-independent exact set; storage order must not be inferred from it or UUID order
  - ADD_GOAL accepts both exact Phase 1 and Cycle 2 initial durable Candidate branches and enforces goal/message/Binding count/task effect correspondence
  - SUPPLY_INPUT accepts only exact Cycle 2 continuation Candidate, preserves target_request_unit_id, requires one or two ordered Binding refs matching Candidate inputs, and enforces one exact base/result Task increment
  - all children across the aggregate have unique accepted identity/effect keys and form one contiguous version chain per Task; zero-Candidate, all-reject, partial-accept and multi-accept generic closures remain legal
  - the v3 codec projects run/message/context/input provenance, child Binding refs and Task refs through the existing declared reference machinery; no raw source quote, owner inference, target authority, business fact or read-time repair is introduced
  - P0_RECORD_SCHEMA_VERSION_CATALOG contains explicit v2 source and v3 target pairs; P0_PERSISTENCE_REGISTRY continues selecting v2 until R19 Composition performs the atomic active switch. All non-RU registry entries remain exact
  - encode/decode dispatch is exact pair based. No alias, latest/default, v3-then-v2, v2-then-v3, union-version field, version inference or automatic conversion is permitted
  - direct unversioned Request Understanding encoding remains forbidden; physical readiness and active route are still blocked until R4E2 and R19 Composition
implementation_tasks:
  - import exact v3 Core models and define v3 top-level/child projection specs without weakening the existing v2 source codec
  - generalize the Request Understanding child-envelope closure helper only as far as exact parent-version dispatch requires; retain strict runtime type/state checks and deterministic canonical payload/reference ordering
  - register both explicit versioned pairs while retaining v2 in the current semantic registry, and keep versioned encode/decode callers responsible for supplying exact code and version
  - add component tests for exact catalog membership/current selection, recursively exact v3 encode/decode, all three branches, zero/all-reject/partial/multi-accept, ordered dual Binding, per-Task chains, reference closure, deterministic round-trip and mutation rejection
  - add negative tests for wrong parent/child version, v2 child under v3, v3 child under v2, wrong branch/operation, reordered/missing/extra child, partial accepted set, duplicate/colliding identity, broken Task chain, provenance mutation, alias/fallback and direct-unversioned RU use
contract_changes: IMPLEMENTS_APPROVED — materializes the dual-Spec v3 semantic codec target and keeps v2 solely as an explicit migration source. No new canonical contract or user outcome change.
security_impact: POSITIVE — exact parent-version child selection and closed references prevent cross-version confusion, provenance loss, partial accepted effects and authority reconstruction.
eval_impact: COMPONENT_ONLY — provides deterministic v3 serialized evidence needed by migration and later Eval, but changes no Case, provider script, artifact, lifecycle or Result.
review_profile:
  planning_review: INDEPENDENT_EXACT_FILE
  implementation_review: W12_RU_V3_CODEC_EXACT_HEAD
  targeted_risk_checks:
    - exact two-file allowlist and base/blob containment
    - v2 explicit migration-source codec remains byte-compatible
    - v3 is explicit versioned target while the current semantic registry, direct RU encoding behavior and active runtime routing remain v2
    - child branch/type/order/task-effect/reference closure is exact and recursively canonical
    - no Infrastructure, Alembic, Eval, docs or planning source edits
required_checks:
  - uv run pytest tests/component/application/test_persistence_contract.py
  - uv run pytest tests/component/application
  - uv run pytest tests/component/core/test_request_processing.py tests/component/core/test_task_state_contract.py
  - git diff --check
  - repository-wide read-only impact scan for request_understanding_record.p0.v3, version catalog consumers, physical constraints, migration and PostgreSQL staging methods
full_suite_gate: DEFERRED_TO_FINAL_REFROZEN_02_18
done_when:
  - focused, Application component and Core neighbor tests pass
  - exact v2 active/v3 staged versioned codec behavior is mechanically proven with no active registry, route or physical data change
  - local independent implementation review and remote exact-head review are PASS with zero open findings
  - product merges as B_C2_W12_RU_V3_CODEC and only then 02-18R19R4E2 planning begins
rollback:
  - before R4E2, revert this two-file semantic expansion atomically; no data rollback is needed
  - after the Composition barrier activates v3, first stop all new v3 writes and run the canonical exact downgrade gate before deactivating any active switch
  - downgrade is allowed only when every v3 aggregate is fully validated and losslessly representable as generic Phase 1 ADD_GOAL e2e01-thin-v2; convert the entire reversible set atomically and require zero residual v3 before switching to v2
  - any Cycle 2 initial, SUPPLY_INPUT, unknown, malformed or incomplete v3 closure blocks the whole downgrade with zero writes; do not deactivate Application/Eval/Composition routing or roll back R4E2/R4E1 while blocked
  - after the gate succeeds, reverse the exact owner-local cutover packets in their approved order, then roll back R19R5R → R19R4F → R19R4E2 before reverting R4E1
handoff_format:
  - exact branch/base/head/tree and changed files
  - exact catalog/codec/child closure behavior, checks/results/NOT_RUN and impact scan
  - unchanged active route/physical data, downstream blockers, risks and rollback
output_barrier: B_C2_W12_RU_V3_CODEC
handoff_to: tech-lead
```
