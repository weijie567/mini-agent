---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R5
type: correction
depends_on: [02-18R19R4]
files_modified:
  - src/mini_agent/evaluation/graders.py
  - tests/component/evaluation/test_e2e01_graders.py
  - tests/integration/evaluation/test_e2e01_offline_harness.py
requirements: [E2E01-05, E2E01-06]
---

# 02-18R19R5｜W12 Eval fresh Claim provenance

```yaml
task_id: 02-18R19R5
goal: 让REQ_BINDING按accepted Claim、fresh supersession、Task/RequestUnit current关系与Gateway版本评分，而不把get_shipment的query-origin Tool argument误当作order Claim来源，并关闭已知enum oracle漂移。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-eval-claim-provenance
base_branch: integration/e2e01-cycle2
product_base_sha: 30ced07e573f85a721c17397ac50174d0a740947
base_sha: 30ced07e573f85a721c17397ac50174d0a740947
base_tree: c219c0502ab494570bba4bbeb0fcc203ece41d9b
worktree_id: e2e01-cycle2-w12-eval-claim-provenance
planning_control_base_sha: 30ced07e573f85a721c17397ac50174d0a740947
planning_control_base_tree: c219c0502ab494570bba4bbeb0fcc203ece41d9b
agent_role: eval-engineer
writer: cycle2-w12-eval-claim-owner
owned_files:
  - src/mini_agent/evaluation/graders.py
  - tests/component/evaluation/test_e2e01_graders.py
  - tests/integration/evaluation/test_e2e01_offline_harness.py
forbidden_files:
  - all files outside owned_files
  - evals/cases/**
  - evals/manifests/**
  - evals/fixtures/**
  - evals/model_scripts/**
  - evals/results/**
  - src/mini_agent/core/**
  - src/mini_agent/application/**
  - src/mini_agent/infrastructure/**
  - src/mini_agent/bootstrap.py
  - docs/**
  - .planning/**
  - alembic/**
  - AGENTS.md
  - graphify-out/**
required_product_blobs:
  docs/implementation/e2e01-cycle2-implementation-spec.md: 892cfeeb2713b198a60d8b12ae29279892d3a2a2
  src/mini_agent/evaluation/graders.py: d8cfbb5faf06e83c438e627bf455b2fb8425b409
  tests/component/evaluation/test_e2e01_graders.py: 77dbdf8ff34fc7da1cf7963600d73de00f505548
  tests/integration/evaluation/test_e2e01_offline_harness.py: 9a28f226ca257b5a3a14e294d1630480b56f144a
  evals/cases/e2e01-cycle2.v1.json: 6b77888471318510f3dcf1d16adebb2f64713d27
  evals/manifests/e2e01-cycle2.v1.json: 03336c0508c2864b31303fcf08b4c60095a80317
required_planning_control_blobs:
  .planning/phases/02-cycle-2-e2e-01/02-18R18-PLAN.md: b6f0836811e2befd5e78b44cf9c7245d31def728
  .planning/phases/02-cycle-2-e2e-01/02-18R19R4-PLAN.md: a347ad3163963452318c764d4737b4b860f3322e
dependencies:
  - exact reviewed R18 Eval product = 06e421d8e4d10bfb81500f71aa6b3f7422d35437
  - exact reviewed R19R2 Spec = 6e9305a9c0ea19fff4700fcd24d36ad83609faab
  - exact reviewed R19R3 Core = b0fe7b08575ad17c18283f44c01bcb1fe258dff2
  - exact reviewed R19R4 Fixture / B_C2_W12_FIXTURE_CLAIM_PROVENANCE = 30ced07e573f85a721c17397ac50174d0a740947
confirmed_gaps:
  - current REQ_BINDING($TASK_VERSION_AT_GATE) only discovers Gate and ToolCall when the Claim binding_id appears in argument_binding_refs
  - get_shipment intentionally carries the UNIQUE product_description query origin while REQ_BINDING(order_id) must grade a separate fresh accepted Claim, so the current matcher rejects the correct graph
  - state-invalidated recovery must grade the pre-crash fresh v3 Claim retained in final v4, not the v2 ordinary old Claim and not a recovery-created Claim
  - current Cycle 2 ToolCallGrader only proves referenced binding ids exist plus attempt_count shape; it does not close each root ToolCall to one accepted Gate Trace or prove the canonical tool-family argument/target provenance required by the active Spec
  - Cycle2EvalEvidence intentionally has no GateDecisionV2 family, so the available Gate owner projection for this correction is the exact GATE_DECISION_RECORDED Trace; verified_target_ref must be closed independently through ToolCall plus CandidateSet/Selection/Search-or-Shipment Observation evidence and cannot be invented on Trace
  - offline harness enum oracle still expects removed SupersededRunInvalidationKind and omits actual Cycle2MappingSourceKind / ImportedMapperReference
implementation_decisions:
  - preserve candidate_ordinal / $SELECTION_EXPECTED_TASK_VERSION logic byte-semantically
  - for $TASK_VERSION_AT_GATE require exactly one current same-name accepted binding owned exactly once by the current RequestUnit and same Task
  - require the binding exact-one source to be a USER Message accepted by the evaluated root Run; require source/binding causal time and existing closed InputBinding vocabulary
  - for order_id require a fresh current child whose supersedes resolves to exactly one historical accepted order_id Claim with the same normalized O-1001 value, distinct source Message and nondecreasing time; supersedes=null old fixture Claim cannot satisfy REQ_BINDING
  - prove REQ_BINDING applicability with an accepted Gate Trace and its exact root ToolCall closure having the same Task, RequestUnit, ContextManifest/model-call identity, requested/canonical tool family, exact argument_binding_refs and validated task version; do not require a separately graded Claim id in a UNIQUE/ordinal target-family Tool argument_binding_refs
  - ordinary paths use the existing TaskStateTransition timeline to prove Gate version; the unique SUPERSEDED / STATE_OR_BINDING_INVALIDATED recovery permits the persisted v3 Gate/Tool snapshot against unchanged final v4 only when the fresh child remains current and the no-result fence is exact
  - strengthen Cycle 2 ToolCallGrader in this packet: every root ToolCall must resolve to exactly one accepted GATE_DECISION_RECORDED Trace by root Run, Task, RequestUnit, model_call_id, context_manifest_id, requested tool name, validated version and byte-exact ordered argument_binding_refs; missing, duplicate, rejected or cross-call Trace projection fails closed
  - require every root ToolCall private_owner_scope_ref to equal both expectations.trusted_customer_id and its exact owning Task.owner_customer_id; foreign or cross-Task owner scope fails before argument/target grading
  - for search_orders require the one exact current product_description binding and verified_target_ref=null
  - split get_order into three mutually exclusive canonical families: direct-order uses the one exact current order_id binding with verified_target_ref=null; UNIQUE uses exactly the current product_description query binding plus the AutoTarget projection whose selection_id exact-equals the non-null verified target; ordinal uses exactly the current candidate_ordinal binding plus the canonical UUID encoded by selected_target_ref. Mixed refs, family fallback or target nullability drift fail closed
  - get_shipment reuses the same mutually exclusive direct/UNIQUE/ordinal target-origin vocabulary from Spec 7.5: each executable target family requires exactly one current origin binding and one non-null ToolCall verified_target_ref; UNIQUE/ordinal close through the corresponding Selection projection, while direct-order may pass only with its independent exact current verified-target owner evidence and is never inferred from order-id value or Shipment output. For every family, target exact-copies into each linked Shipment Observation; a fresh order Claim may coexist with UNIQUE/ordinal but cannot replace or join their target-origin refs
  - add positive direct/UNIQUE/ordinal get_order, get_shipment-query-origin and recovery v3→v4 quadrants; reject old Claim, missing/wrong supersedes, wrong USER source/root, non-current binding, wrong Task/Unit/Gate/Tool version or identity, wrong Tool owner, Gate/Tool wrong-ref, mixed/cross-family refs, duplicate Gate projection and target-nullability/drift
  - update only the import-time enum expected-name oracle: add Cycle2MappingSourceKind and ImportedMapperReference, remove SupersededRunInvalidationKind; do not change result-boundary enum registration
  - keep Case JSON, manifest, fixture/script/lane bytes and lifecycle unchanged; no artifact digest update is authorized
contract_changes: NONE — implements merged R19R2 REQ_BINDING semantics and aligns a stale test oracle with the already-active result-boundary registration.
security_impact: POSITIVE — accepted Claim provenance remains USER/root/current/fresh and cannot usurp query-origin verified-target evidence or trusted owner scope.
eval_impact: CORRECTION — valid get_shipment/recovery trajectories can pass while stale/old/fabricated Claim graphs and wrong Gate/Tool argument or target provenance fail closed; Case predicates and lifecycle remain unchanged.
review_profile:
  planning_review: INDEPENDENT_EXACT_FILE_PASS
  implementation_review: W12_EVAL_FRESH_CLAIM_EXACT_HEAD
  focused_tests: uv run pytest -q tests/component/evaluation/test_e2e01_graders.py tests/integration/evaluation/test_e2e01_offline_harness.py::test_canonical_result_enum_types_are_import_time_closed
  neighbor_tests: uv run pytest -q tests/component/evaluation/test_e2e01_artifact_consistency.py tests/component/evaluation/test_e2e01_versioned_artifact_loader.py tests/integration/evaluation/test_e2e01_offline_harness.py tests/integration/test_cycle2_fixture_seed.py
  full_suite_gate: FINAL_REFROZEN_02_18
  phase_end_deep_audit: FINAL_REFROZEN_02_18
required_checks:
  - exact three-file allowlist and base containment
  - Case/manifest blobs remain exactly 6b77888471318510f3dcf1d16adebb2f64713d27 / 03336c0508c2864b31303fcf08b4c60095a80317
  - direct get_order fresh Claim and get_shipment query-origin Tool both satisfy REQ_BINDING(order_id)
  - every root ToolCall has one exact accepted Gate Trace projection and Tool/Task/trusted owner equality; search_orders uses query/null-target, while get_order accepts only direct-order/null-target or UNIQUE/ordinal exact target closure and get_shipment accepts only one canonical target-origin binding with non-null exact owner/Selection/Observation target closure
  - state-invalidated recovery resolves the v3 fresh child retained in v4 and rejects the v2 old Claim
  - source/supersession/current/CAS/Gate/Tool wrong-owner/wrong-ref/cross-family/duplicate/target-null-or-drift tamper matrix fails closed
  - enum oracle exact set passes without changing registered runtime result types
  - focused and neighbor tests, deterministic artifact consistency, local review and remote exact-head review PASS
done_when:
  - reviewed product merges as B_C2_W12_EVAL_CLAIM_PROVENANCE and R19 Composition is refrozen only from that successor
rollback:
  - revert the three-file Eval correction atomically; Case/manifest bytes require no rollback and W12 remains blocked before Composition
handoff_format:
  - exact branch/base/head/tree and three changed files
  - commands/results/NOT_RUN, predicate quadrants, artifact hash evidence, contract/security/Eval impact, risks and rollback
output_barrier: B_C2_W12_EVAL_CLAIM_PROVENANCE
handoff_to: tech-lead
```
