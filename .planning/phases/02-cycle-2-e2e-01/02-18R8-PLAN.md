---
phase: 02-cycle-2-e2e-01
plan: 02-18R8
type: fix
wave: W12R8
depends_on: [B_C2_COMPLETE_EVIDENCE_CONTRACT]
files_modified:
  - src/mini_agent/application/records.py
  - tests/component/application/test_record_contracts.py
autonomous: false
requirements: [E2E01-06]
user_setup: []
must_haves:
  truths:
    - "successful recovery append 的 APPEND_SECOND_ATTEMPT child 必须与同一 ToolCall 的 attempt 2 fence 精确闭合，但普通成功 terminal 不携带 recovery_decision_ref。"
    - "terminal recovery child 仍必须由 parent recovery_decision_ref 精确引用；不得放宽 unfinished、budget-exhausted 或 state-invalidated recovery。"
    - "孤立、多余、wrong-run、wrong-attempt 或错误 decision kind 的 recovery child 必须 fail closed。"
  artifacts:
    - "Corrected Application exact-evidence recovery graph validator and regression tests."
  key_links:
    - "Cycle 2 recovery canonical contract -> Cycle2ExactRunEvidenceClosure -> PostgreSQL exact reader."
---

# Phase 2 Plan 02-18R8｜Correct exact-evidence recovery child closure

> **EXACT CORRECTION TASK PACKET / APPLICATION CONTRACT SINGLE WRITER**

## Exact Task Packet

```yaml
task_id: 02-18R8
goal: 修复 Cycle2ExactRunEvidenceClosure 对 recovery child 的错误统一反向引用要求，精确区分 successful retry append 与 terminal recovery。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-recovery-evidence-contract
base_branch: integration/e2e01-cycle2
base_sha: 894a851639595e8a5bf61d1ccc251c6b313a6c16
base_tree: eda6f891ff58ad122b409ec9c0f1e2ac76271b9a
planning_control_base_sha: 894a851639595e8a5bf61d1ccc251c6b313a6c16
planning_control_base_tree: eda6f891ff58ad122b409ec9c0f1e2ac76271b9a
worktree_id: e2e01-cycle2-w12-recovery-evidence-contract
agent_role: runtime-engineer
writer: application-record-contract-owner
pull_request_base: integration/e2e01-cycle2
pull_request_mode: draft until independent exact-head review PASS
owned_files:
  - src/mini_agent/application/records.py
  - tests/component/application/test_record_contracts.py
forbidden_files:
  - all repository files outside the exact two-file allowlist
  - src/** except src/mini_agent/application/records.py
  - tests/** except tests/component/application/test_record_contracts.py
  - evals/**
  - docs/**
  - .planning/**
  - alembic/**
  - AGENTS.md
  - graphify-out/**
canonical_inputs:
  - docs/implementation/e2e01-cycle2-implementation-spec.md successful recovery append and terminal recovery semantics
  - src/mini_agent/core/tool_system.py ToolCallRecordV2 closed terminal matrix
read_first:
  - AGENTS.md
  - docs/implementation/e2e01-cycle2-implementation-spec.md
  - src/mini_agent/core/tool_system.py
  - src/mini_agent/application/records.py
  - tests/component/application/test_record_contracts.py
required_product_blobs:
  AGENTS.md: 5904da48f4e3a2d95e86ffa04ebe29694caf5c0a
  docs/implementation/e2e01-cycle2-implementation-spec.md: db387562a69ff3c532d53a2cdac9aa15d56663d0
  src/mini_agent/application/records.py: 4d757e846e15f0887a93b53f9bedd2ed30db6d7d
  tests/component/application/test_record_contracts.py: ab874e39457f79cd80236fc3fa8b4c9572351bf1
required_planning_control_blobs:
  .planning/phases/02-cycle-2-e2e-01/02-18R7-PLAN.md: e566b852a82d5d2b37d81ac006556688ee043362
dependencies:
  - exact B_C2_COMPLETE_EVIDENCE_CONTRACT = 826cdfe5819882e572f3d5ca33a5f84ab9df4508
  - planning-only successor 894a851639595e8a5bf61d1ccc251c6b313a6c16
implementation_decisions:
  - successful APPEND_SECOND_ATTEMPT child is valid only when its ToolCall belongs to the root Run, has exactly two attempts, attempt 1 is RETRY_SCHEDULED, attempt 2 exists after decided_at, and the ordinary terminal parent has no recovery metadata
  - terminal recovery child remains valid only when parent recovery_decision_ref exact-copies its ID
  - every parent recovery_decision_ref still resolves exactly one returned child
  - every returned child must close exactly one of the two approved forms; duplicate decisions per ToolCall remain invalid
contract_changes: CORRECTION — aligns the Application evidence validator with the already-canonical successful recovery append semantics; no new behavior.
security_impact: NONE — no identity, authorization, payload, disclosure, Action or side-effect change.
eval_impact: POSITIVE — valid transient retry trajectories become readable while malformed recovery evidence remains fail closed.
review_profile:
  planning_review: INDEPENDENT_EXACT_FILE_PASS
  implementation_review: TARGETED_RECOVERY_EVIDENCE_MATRIX
  reviewer_timeout: 20_SECONDS_MAX
  targeted_risk_checks:
    - successful retry child exact closure without parent ref
    - terminal recovery parent ref remains mandatory
    - orphan/duplicate/wrong-run/wrong-attempt child rejection
verification:
  focused: uv run pytest tests/component/application/test_record_contracts.py -q -k exact_run_evidence
  neighbor: uv run pytest tests/component/application/test_record_contracts.py tests/component/application/test_ports_contract.py -q
  compile: uv run python -m compileall -q src/mini_agent/application/records.py tests/component/application/test_record_contracts.py
  diff_check: git diff --check exact-base...HEAD
  allowlist: exact two-file containment
  full: NOT_RUN; W12 final stable candidate owns canonical full
required_checks:
  - exact base/tree/blobs/clean preflight PASS
  - positive successful retry and terminal recovery variants PASS
  - orphan/duplicate/wrong matrix variants fail closed
  - focused/neighbor/compile/diff/allowlist and independent exact-head review PASS
done_when:
  - reviewed two-file correction PR merges and reader is rebuilt from the actual successor
rollback:
  - close or revert this exact two-file correction before the reader depends on it
  - never suppress recovery validation in Infrastructure
handoff_to: tech-lead
output_barrier: B_C2_RECOVERY_EVIDENCE_CONTRACT_CORRECTED
```

## Tasks

1. RED：用 successful attempt-2 terminal 证明现有 validator 错拒 canonical child。
2. GREEN：把 recovery child 分为 successful append 与 terminal recovery 两个闭合矩阵。
3. VERIFY：component neighbor、compile、allowlist 与 bounded exact-head review。
