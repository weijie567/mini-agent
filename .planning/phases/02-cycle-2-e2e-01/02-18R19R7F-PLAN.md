---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R7F
type: correction
depends_on: [02-18R19R7, 02-18R19R7E]
files_modified:
  - src/mini_agent/application/records.py
  - tests/component/application/test_record_contracts.py
requirements: [E2E01-05]
---

# 02-18R19R7F｜W12 v3 supporting ToolCall Binding timeline blocker

> **CODE FREEZE / APPLICATION SINGLE-WRITER BLOCKER PACKET**
>
> 本 Packet 只修复 R19R7 all-27 在 R19R7E 后暴露的 v3 exact evidence
> 历史 ToolCall/Binding 时间线误判。现有合同继续冻结；不得修改 schema、Port、
> Infrastructure、Composition、Core、Eval、Fixture、Case 或架构，非阻断优化全部延期。

```yaml
task_id: 02-18R19R7F
goal: 让既有 Cycle2ExactRunEvidenceClosure 在 v3 RequestUnderstanding 完整时间线中，按 ToolCall.validated_task_state_version 验证其 argument_binding_refs 当时确为同一 Task/RequestUnit 的 active Binding；允许 source-provenance supporting Run 的不可变 ToolCall 合法引用后来被 supersede 的 Binding，同时继续拒绝在该版本尚未生效或已经被 supersede 的 Binding。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-r19r7f-application-tool-binding-timeline
base_branch: integration/e2e01-cycle2
base_sha: a9df5668273844067f8188f2dee0ffdd2ae0ccd6
base_tree: 7b1db1ad5fba7f079cc8fe3c1bce65185e6a289d
planning_branch: codex/e2e01-cycle2-w12-r19r7f-plan
worktree_id: e2e01-cycle2-w12-r19r7f-application-tool-binding-timeline
agent_role: runtime-engineer
writer: cycle2-application-exact-evidence-owner
pull_request_base: integration/e2e01-cycle2
pull_request_mode: draft until independent local and remote exact-head review PASS; never use admin bypass
owned_files:
  - src/mini_agent/application/records.py
  - tests/component/application/test_record_contracts.py
forbidden_files:
  - all repository files outside the exact two-file owned_files allowlist
  - AGENTS.md
  - PROJECT_DIRECTION.md
  - docs/**
  - .planning/**
  - alembic/**
  - src/mini_agent/bootstrap.py
  - src/mini_agent/core/**
  - src/mini_agent/evaluation/**
  - src/mini_agent/infrastructure/**
  - tests/integration/**
  - tests/component/core/**
  - tests/component/evaluation/**
  - graphify-out/**
canonical_inputs:
  - existing InputBindingV2.supersedes semantics
  - existing v3 RequestUnderstanding Binding closed-set, acyclic-lineage and Task-effect replay
  - existing immutable ToolCallRecordV2.validated_task_state_version and argument_binding_refs
  - existing exact reader rule that retained historical Observations bring their source-provenance supporting Runs/links/ToolCalls
  - 02-18R19R7 native v3 Composition contract
required_product_blobs:
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
  src/mini_agent/application/records.py: bec405f96ca593dd18decd8fb613973b29d34960
  tests/component/application/test_record_contracts.py: 4afad2449f89a0b032d884394a4dd43681b58269
  .planning/phases/02-cycle-2-e2e-01/02-18R19R7-PLAN.md: 7ff27335a18c7de0039839667c8d8fcc2a33ed49
  .planning/phases/02-cycle-2-e2e-01/02-18R19R7E-PLAN.md: fc4adb94f87be4ed6913fb206ec3e674fc84b544
required_planning_control_blobs:
  .planning/GOVERNANCE.md: f5ec0fe4b83a23b590457e0b407f3f9135dbf4f7
  .planning/ACTIVATION.md: 94a5bc82d4be7b59eeafc1fabab7e68c790ad610
dependencies:
  - exact GitHub verified signed R19R7E successor = a9df5668273844067f8188f2dee0ffdd2ae0ccd6
  - exact downstream red = R19R7 all-27 advances to E2E01-05/logistics-required-uses-shipment and fails while load_cycle2_exact_run_evidence_v3_for_owner constructs the terminal exact closure
  - traced underlying ValueError = Cycle 2 exact evidence ToolCall root mismatch
  - traced bad predicate = one supporting get_order ToolCall references persisted historical order_id Binding 93e10cd9-57c9-4099-a529-2a419767afa5, which is present in the closed v3 supersedes lineage but no longer appears in the terminal RequestUnit.input_binding_refs; the root get_order/get_shipment and supporting search_orders calls all satisfy the existing root predicate
implementation_decisions:
  - preserve the existing v3 Task-effect replay and derive an exact active Binding ref set for every replayed Task state version by walking the already-validated edge chain backwards from terminal RequestUnit.input_binding_refs
  - at an accepted SUPPLY_INPUT edge, replace each accepted Binding with its exact supersedes predecessor, or remove a clean newly introduced Binding; business and status-transition edges do not change the active Binding set
  - when v3 request_understanding_closures are present, require every ToolCall argument Binding to exist and to belong to its exact Task/RequestUnit active set at ToolCall.validated_task_state_version; unknown versions, early successors, late ancestors, wrong Task/RequestUnit and missing refs fail closed
  - preserve the existing current-only ToolCall argument rule when no v3 RequestUnderstanding closure is present; do not widen the legacy/v2 exact evidence contract
  - do not infer activity from timestamps, Tool arguments, normalized values, name matching, current descendants alone or supporting-run status; only the already-closed v3 accepted-effect/state-version timeline is authoritative
  - do not mutate/rewrite historical ToolCalls, reattach them to current Bindings, omit retained historical Observations/source calls, or weaken owner, Run, Task, RequestUnit, Gate, Manifest, target-authority, lineage-cycle or closed-set validation
  - add a focused positive component regression with a retained historical Order Observation and its supporting get_order ToolCall at the exact pre-supersession version, plus negative regressions for the ancestor after supersession and successor before activation
  - retain the existing named non-v3 regression that rejects a ToolCall argument absent from terminal RequestUnit.input_binding_refs
contract_changes: NONE — this corrects the existing exact historical-evidence timeline validation; no public field, method, schema, record family, Case, metric, lifecycle or behavior is added.
security_impact: POSITIVE — immutable source ToolCalls remain auditable at their exact accepted Task version, while stale, early, cross-graph, missing and fabricated Binding refs continue to fail closed.
eval_impact: BLOCKER_ONLY — unblocks the existing E2E01-05 Composition pair/Case; no Eval artifact, Case, grader, predicate, threshold or lifecycle change.
verification:
  preflight: exact signed base/tree/blobs and clean isolated worktree
  red: focused historical supporting ToolCall fixture fails with existing ToolCall root mismatch before the product change
  focused: uv run pytest -q tests/component/application/test_record_contracts.py -k 'v3_exact_tool_binding_timeline'
  neighbor: uv run pytest -q tests/component/application/test_record_contracts.py
  application_neighbor: uv run pytest -q tests/component/application tests/integration/test_e2e01_cycle2_execution_seam.py
  composition_probe: NOT_RUN in this Packet; R19R7 rebases from the signed R19R7F successor and resumes E2E01-05 pair/all-27 immediately
  diff_check: git diff --check exact-base...HEAD
  allowlist: exact two-file containment
  full: NOT_RUN; final refrozen 02-18 owns canonical full
required_checks:
  - one source-provenance supporting ToolCall using an exact active ancestor Binding at its historical validated state version passes
  - the same ancestor at or after its successor activation fails closed
  - the successor before its accepted result version fails closed
  - non-v3/current-only ToolCall arguments remain restricted to terminal RequestUnit.input_binding_refs
  - missing/cyclic/closed-set lineage, owner, Run, Task, RequestUnit, Gate, Manifest and target-authority checks remain unchanged
  - no Infrastructure/Composition/Core/Eval/Fixture/schema/migration/doc change
  - focused, record-contract neighbor and Application neighbor tests PASS
  - independent local implementation review PASS with all findings closed
  - remote exact-head review PASS before merge
done_when:
  - reviewed feature PR merges by signed squash as B_C2_W12_R19R7F_V3_TOOL_BINDING_TIMELINE
  - R19R7 Composition rebases from that exact signed successor and resumes fail-fast at E2E01-05 pair/all-27 without widening its two-file allowlist
rollback:
  - before Composition merge, revert the exact two-file Application squash atomically
  - after descendants exist, invalidate/revert descendants first; never rewrite persisted ToolCalls, drop source provenance or add a Composition/reader fallback
handoff_to: tech-lead
handoff_format:
  - branch and exact base/head/tree plus both file blobs
  - actual changed files and allowlist containment
  - red/focused/neighbor results and downstream Composition probe explicitly NOT_RUN
  - contract_changes NONE and remaining W12 blocker handed back to R19R7
```
