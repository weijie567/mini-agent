---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R7B
type: correction
depends_on: [02-18R19R7]
files_modified:
  - src/mini_agent/infrastructure/cycle2_runtime.py
  - tests/integration/test_cycle2_runtime_adapter.py
requirements: [E2E01-05]
---

# 02-18R19R7B｜W12 direct `get_order` business-read blocker

> **CODE FREEZE / INFRASTRUCTURE SINGLE-WRITER BLOCKER PACKET**
>
> 本 Packet 只修复 real Composition 已复现的 direct `get_order` dispatch 断点。
> 现有合同继续冻结；不得修改 Application、Composition、Core、Eval、Fixture、
> schema、migration、Case 或架构，非阻断优化全部延期。

```yaml
task_id: 02-18R19R7B
goal: 让既有 Cycle2BusinessReadHandler 按现行 Gateway/Application 合同执行 verified_target_ref=None 的 direct get_order，从当前 owner-scoped durable order_id InputBinding 与唯一完整 current target/observation closure 共同形成 GetOrderQuery；target-bearing get_order、get_shipment 与 search_orders 路径保持不变，使 R19R7 real Composition 的 E2E01-05 首个 blocker 消失。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-r19r7b-business-read
base_branch: integration/e2e01-cycle2
base_sha: c753ebd67c06b65c8a1a548b12d75ae39206f1b1
base_tree: f4e879bb3c0079cc465a6926eaf89fca8ee86956
planning_branch: codex/e2e01-cycle2-w12-r19r7b-plan
worktree_id: e2e01-cycle2-w12-r19r7b-business-read
agent_role: infra-engineer
writer: infrastructure-business-read-owner
pull_request_base: integration/e2e01-cycle2
pull_request_mode: draft until independent local and remote exact-head review PASS; never use admin bypass
owned_files:
  - src/mini_agent/infrastructure/cycle2_runtime.py
  - tests/integration/test_cycle2_runtime_adapter.py
forbidden_files:
  - all repository files outside the exact two-file owned_files allowlist
  - AGENTS.md
  - PROJECT_DIRECTION.md
  - docs/**
  - .planning/**
  - alembic/**
  - src/mini_agent/bootstrap.py
  - src/mini_agent/application/**
  - src/mini_agent/core/**
  - src/mini_agent/evaluation/**
  - tests/integration/test_offline_composition_root.py
  - graphify-out/**
canonical_inputs:
  - src/mini_agent/core/tool_system.py AuthorizedToolCommandV2 target_is_separate_and_matches_tool_path
  - src/mini_agent/core/control_gateway.py direct GET_ORDER validation branch
  - src/mini_agent/application/agent_run_service.py committed v3 direct Order route
  - docs/implementation/e2e01-cycle2-implementation-spec.md E2E01-05 exact direct get_order requirement
  - 02-18R19R7 real Composition Task Packet and captured red failure
required_product_blobs:
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
  src/mini_agent/infrastructure/cycle2_runtime.py: 90999c6f1239a0f26584ce949a3c478225399440
  tests/integration/test_cycle2_runtime_adapter.py: 37ea3a0d2fe7a0bf8e42bbce26649bf6f4b257e1
  src/mini_agent/application/agent_run_service.py: e6a7d2d6d5b6c1681cb9b325a6da54f11eedf02f
  src/mini_agent/core/control_gateway.py: 84d674c9aec95668f2a5fd59951a27b9506478d2
  src/mini_agent/core/tool_system.py: d5763899da0590f1b5da1a7d32c907a4da9a16b4
  .planning/phases/02-cycle-2-e2e-01/02-18R19R7-PLAN.md: 7ff27335a18c7de0039839667c8d8fcc2a33ed49
required_planning_control_blobs:
  .planning/GOVERNANCE.md: f5ec0fe4b83a23b590457e0b407f3f9135dbf4f7
  .planning/ACTIVATION.md: 94a5bc82d4be7b59eeafc1fabab7e68c790ad610
dependencies:
  - exact GitHub verified signed R19R7 planning successor = c753ebd67c06b65c8a1a548b12d75ae39206f1b1
  - exact red at R19R7 current product WIP: E2E01-05/order-only-no-shipment raises Cycle2BusinessReadDispatchError("verified target unavailable") before real get_order dispatch
implementation_decisions:
  - preserve owner-scoped initial ToolCall closure, exact argument_binding_refs matching and current durable binding checks
  - when canonical tool is GET_ORDER and verified_target_ref is null, require exactly one selected current InputBinding whose name is order_id and whose normalized_value is a string
  - independently require exactly one complete current verified Order target and exactly one matching current target Observation in the owner-scoped durable closure; require target.order_id to equal the fresh order_id binding value before business read
  - preserve the two authorities as distinct identities: the fresh direct-order argument binding UUID is not the target-origin binding UUID and must never be required to equal target.input_binding_refs
  - only after both authorities agree, call the existing GetOrderPort with trusted owner customer_id plus the fresh binding value
  - never infer order_id from message, model output, Case identity, expected evidence, stale target or fixture metadata
  - preserve the existing verified-target branch for target-bearing GET_ORDER and GET_SHIPMENT byte-for-byte except the minimum branch placement needed for direct GET_ORDER
  - preserve SEARCH_ORDERS, retry/fault, timeout, result projection and error redaction behavior
  - add exact positive direct-order coverage with distinct fresh/target-origin binding UUIDs and fail-closed wrong binding, missing/ambiguous target, missing/ambiguous Observation and target/order mismatch coverage; retain all existing three-tool target-bearing coverage
contract_changes: NONE — implements the already-active null-target direct GET_ORDER branch accepted by Core and emitted by Application; no new public or canonical contract.
security_impact: POSITIVE — direct Order dispatch remains bound to authenticated owner scope and the exact current durable order_id binding; no target or identity is accepted from user/model text outside the existing Gateway grant.
eval_impact: BLOCKER_ONLY — unblocks the already-frozen E2E01-05 real Composition path; no Case, grader, artifact, metric, lifecycle or expected outcome changes.
verification:
  preflight: exact signed base/tree/blobs and clean isolated worktree
  red: reproduce a direct GET_ORDER ToolCall with null verified_target_ref failing before the source change
  focused: uv run pytest -q tests/integration/test_cycle2_runtime_adapter.py
  composition_probe: NOT_RUN in this Packet; R19R7 must run the exact all-27 Composition test only after rebasing/reapplying its two-file WIP onto the signed R19R7B merge successor
  neighbor: uv run pytest -q tests/component/application/test_agent_run_service.py tests/integration/test_cycle2_fixture_seed.py tests/integration/test_cycle2_runtime_adapter.py
  diff_check: git diff --check exact-base...HEAD
  allowlist: exact two-file containment
  full: NOT_RUN; final refrozen 02-18 owns canonical full
required_checks:
  - exact positive null-target direct GET_ORDER query uses only trusted owner, durable fresh order_id binding and the unique complete current target/Observation closure; fresh and target-origin binding UUIDs remain distinct
  - wrong binding name/value type, ambiguous/missing binding, owner mismatch, missing/ambiguous target or Observation and target/order mismatch remain fail closed before business read
  - target-bearing GET_ORDER and GET_SHIPMENT behavior remains green
  - no Application/Core/Composition/Eval/Fixture/schema/migration/doc change
  - independent local implementation review PASS with all findings closed
  - remote exact-head review PASS before merge
done_when:
  - reviewed feature PR merges by signed squash as B_C2_W12_R19R7B_DIRECT_ORDER_READ
  - R19R7 Composition rebases from that exact signed successor and resumes without widening its two-file allowlist
rollback:
  - before Composition merge, revert the exact two-file Infrastructure squash atomically
  - after descendants exist, invalidate/revert descendants first; never add a Composition-local duplicate business handler as fallback
handoff_to: tech-lead
handoff_format:
  - branch and exact base/head/tree plus both file blobs
  - actual changed files and allowlist containment
  - red/focused/neighbor results and downstream Composition probe explicitly NOT_RUN
  - contract_changes NONE and remaining W12 blocker handed back to R19R7
```
