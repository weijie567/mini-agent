---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R7G
type: correction
depends_on: [02-18R19R7, 02-18R19R7F]
files_modified:
  - docs/implementation/e2e01-cycle2-implementation-spec.md
requirements: [E2E01-05, E2E01-06]
---

# 02-18R19R7G｜W12 frozen Cycle 2 execution seam blocker ruling

> **CONTRACT FREEZE / SPEC SINGLE-WRITER BLOCKER PACKET**
>
> 本 Packet 只裁决 R19R7 all-27 在 signed R19R7F 后暴露的可复现执行矛盾：
> 普通 logistics continuation 缺少既有 post-order control、retry-finalize fault
> 缺少 attempt-1 transient 注入，以及两个 recovery-only Case 的既定持久化
> closure 尚未被 execution contract 精确落点。不得新增 schema、record family、
> Intent、NextMove authority、Tool、Case、指标或架构抽象；非阻断优化全部延期。

```yaml
task_id: 02-18R19R7G
goal: 在不扩大架构或业务范围的前提下，把既有 Cycle 2 Scripted Provider、fault catalog、ordinary logistics 路由和 recovery-only closure 对齐到当前 Application 已实现的确定性 seam，使后续 owner Packet 可以按单一可复现合同恢复 R19R7 all-27。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-r19r7g-spec-execution-seam
base_branch: integration/e2e01-cycle2
base_sha: 2ac6b020031045d5d1b5ffbc9ee41732abf2d89f
base_tree: 917493f89cee796a36943d7788637b740411b615
planning_branch: codex/e2e01-cycle2-w12-r19r7g-plan
worktree_id: e2e01-cycle2-w12-r19r7g-spec-execution-seam
agent_role: tech-lead
writer: cycle2-scoped-spec-owner
pull_request_base: integration/e2e01-cycle2
pull_request_mode: draft until independent local and remote exact-head review PASS; never use admin bypass
owned_files:
  - docs/implementation/e2e01-cycle2-implementation-spec.md
forbidden_files:
  - all repository files outside the exact one-file owned_files allowlist
  - AGENTS.md
  - PROJECT_DIRECTION.md
  - docs/business-capabilities.md
  - docs/architecture/**
  - docs/evaluation/**
  - .planning/**
  - evals/**
  - src/**
  - tests/**
  - alembic/**
  - graphify-out/**
canonical_inputs:
  - existing continuation RU contract persists only accepted Claims and carries no Intent, capability or NextMove/tool authority
  - existing Cycle2AgentRunHandler routes an order_id-only current continuation through get_order, then requests the existing PROPOSE_POST_ORDER control before any get_shipment call
  - existing DNR dual Binding and current ShipmentObservation branches retain their direct get_shipment authority
  - existing fault controller can consume an ordered attempt-indexed directive sequence at real lifecycle boundaries
  - existing Cycle2ToolRestartRecoveryService owns v2 retry recovery and existing OA-10 state-invalidated no-result finalization
  - existing unfinished-attempt requirement is Run INCOMPLETE, active Task/RequestUnit BLOCKED, ToolCall INTERRUPTED and no outbound result
required_product_blobs:
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
  docs/implementation/e2e01-cycle2-implementation-spec.md: 7d62c6990cb4b742071262d2f0d9a52727763b0a
  .planning/phases/02-cycle-2-e2e-01/02-18R19R7-PLAN.md: 7ff27335a18c7de0039839667c8d8fcc2a33ed49
  .planning/phases/02-cycle-2-e2e-01/02-18R19R7F-PLAN.md: 97cc47d22418239c6ec0723612053dfc09270ec6
required_planning_control_blobs:
  .planning/GOVERNANCE.md: f5ec0fe4b83a23b590457e0b407f3f9135dbf4f7
  .planning/ACTIVATION.md: 94a5bc82d4be7b59eeafc1fabab7e68c790ad610
dependencies:
  - exact GitHub verified signed R19R7F successor = 2ac6b020031045d5d1b5ffbc9ee41732abf2d89f
  - exact downstream red = all-27 first fails at E2E01-06/transient-once-then-success because the order_id-only RU correctly dispatches get_order while the next script step is FAULT_DIRECTIVE intended for get_shipment
  - reversible probe = inserting the existing PROPOSE_GET_SHIPMENT post-order control after RU advances all-27 past transient-once to the next uncorrected ordinary logistics script
  - information boundary = ordinary order-only and logistics messages have the same persisted order_id Binding shape after RU; fixture identity, Case ID, script expectation and message keywords are forbidden dispatch authority
  - retry-finalize fault catalog red = restart-after-retry-finalize-v1 currently carries only AFTER_RETRY_FINALIZE restart, so a fresh ordinary execution can never first produce the required attempt-1 transient RETRY_SCHEDULED terminal
  - state-invalidated fixture is the unique preseeded recovery root with an already-finalized attempt 1 and advanced current Task; executing a new RU would violate FORBID_TASK_OR_REQUEST_UNIT_MUTATION
  - unfinished-attempt recovery currently terminates only the ToolCall aggregate and cannot yet produce the already-required atomic Run/Task/RequestUnit/link/Trace no-result closure
implementation_decisions:
  - preserve the current Application routing rule; do not add Intent, capability classification, NextMove persistence, tool allowlist, message-keyword branching, Case branching or fixture-derived authority
  - for exactly eleven ordinary logistics scripts whose current setup has neither a current ShipmentObservation nor the DNR trigger, insert the existing CONTROL_CANDIDATE/PROPOSE_GET_SHIPMENT immediately after RU and before the existing FAULT_DIRECTIVE or terminal control: E2E01-06/transient-once-then-success, E2E01-06/transient-exhausted-blocked, E2E01-06/deterministic-source-integrity-no-retry, E2E01-06/insufficient-promise-need-human, E2E01-06/no-shipment-need-human, T2-assessment-delayed-boundary, T2-assessment-claim-corrected, T2-timeout-after-dispatch-then-success, T2-retry-finalize-before-second-fence-recovery, T2-retry-unfinished-attempt-restart-blocked and T2-two-active-packages-integrity-blocked
  - add exact successful get_order ToolCall/attempt predicates before get_shipment predicates for those eleven ordinary Cases; preserve all existing Shipment, retry, recovery, state, disclosure and stop predicates
  - preserve direct get_shipment for stale-refresh-success, delivered-not-received-current-claim and refresh-returns-already-stale because their existing current ShipmentObservation or DNR Binding is deterministic authority; do not add a redundant get_order or post-order control
  - make T2-retry-finalize-before-second-fence-state-invalidated a true recovery-only script containing only its existing FAULT_DIRECTIVE; the preseeded root already owns its historical accepted RU/Binding evidence, and recovery must neither call the provider for a new RU nor persist a new Message/Binding/Task/RequestUnit mutation
  - refreeze exact script cardinality at 26 REQUEST_UNDERSTANDING, 37 CONTROL_CANDIDATE across 26 scripts, and 7 FAULT_DIRECTIVE; exactly one script is recovery-only fault-without-RU/control; logistics-required plus the ten listed ordinary scripts that already have a terminal control each have two controls, while unfinished-attempt restart has RU plus one post-order control plus fault and no terminal control
  - correct fault:get-shipment:restart-after-retry-finalize-v1 to carry two ordered real-boundary directives: attempt 1 BEFORE_DISPATCH SYSTEM_FAILURE/SHIPMENT_SERVICE_TRANSIENT, then attempt 1 AFTER_RETRY_FINALIZE PROCESS_RESTART/RETRY_RECOVERY; retain one authenticated FAULT_DIRECTIVE script step that activates and exhaustively validates that exact fault plan
  - keep fault:get-shipment:restart-after-retry-finalize-state-invalidated-v1 as its existing single AFTER_RETRY_FINALIZE restart directive because attempt 1 RETRY_SCHEDULED is already durable in the authenticated recovery setup
  - require fx-retry-scheduled-obsolete-run-owner-a-v1 to include one distinct newer authoritative v2 Run and active RunTaskLink at the unchanged current Task version; recovery uses only that persisted typed identity as replacement_run_id and returns the obsolete preseed root as the evaluated no-result subject
  - require the state-invalidated Composition branch to select recovery from typed recovery_subject_run_id/setup provenance, authenticate the exact fault directive at the already-materialized boundary, invoke the existing recovery service, and read the old root closure; Case ID, expectation and fixture-name branching remain forbidden
  - require unfinished-attempt recovery to close the existing v2 aggregate in one CAS: ToolCall INTERRUPTED plus recovery decision, AgentRun INCOMPLETE/PROCESS_RESTART_DETECTED, active Task and RequestUnit advanced once to BLOCKED, terminal RunTaskLink result version, RunStopped(BLOCKED), TaskStateChanged and ToolCallInterrupted traces, with no fabricated attempt terminal, AgentRunResult, assistant Message or ResponseRendered
  - require the ordinary retry-finalize restart bridge to stop the old process only at the durable AFTER_RETRY_FINALIZE boundary and hand off to the existing recovery service before attempt 2; it must not synthesize success, drain directives or allocate a second ToolCall identity
  - record that Eval must derive successful get_order result evidence from exact OrderObservation source_tool_call_id/result_ref rather than treating all get_order results as INVALID; this is an implementation correction in a later Eval single-writer Packet, not a new predicate or metric
  - split downstream writes serially by ownership: Application recovery command/service, Infrastructure persistence/fixture/fault catalog, Eval scripts/cases/manifest/grader, then the existing two-file Composition Packet rebased from the final signed blocker successor
contract_changes: BLOCKER_CORRECTION_ONLY — aligns contradictory consumers to already-active RU, control, fault and recovery semantics; no public capability, schema, record family, Tool, Case, metric, threshold or architecture family is added.
security_impact: POSITIVE — all dispatch remains derived from accepted Binding/current Observation/DNR authority, recovery remains exact owner-scoped CAS, and no Case/script/fixture/message inference is admitted.
eval_impact: BLOCKER_ONLY — preserves the same 27 Case identities and expected outcomes while making their existing required get_order/get_shipment/recovery evidence executable; no coverage expansion.
verification:
  preflight: exact signed base/tree/blobs and clean isolated worktree
  source_alignment: rg/read comparison across scoped Spec, signed Application handler, fault catalog, recovery service, fixture seed, Eval scripts/cases/loader/grader and R19R7 Composition
  document_checks: rg exact eleven IDs, exact cardinalities, exact two-directive retry-finalize sequence, recovery-only exception and unfinished atomic closure
  diff_check: git diff --check exact-base...HEAD
  allowlist: exact one-file containment
  tests: NOT_RUN; this Packet changes only the scoped active Spec
  full: NOT_RUN; final refrozen 02-18 owns canonical full
required_checks:
  - all eleven ordinary scripts and only those scripts are named once in the routing correction
  - state-invalidated is excluded from ordinary routing and defined as no new RU/Task/RequestUnit mutation
  - DNR and current-ShipmentObservation direct paths remain unchanged
  - retry-finalize success fault has both transient attempt-1 and post-finalize restart boundaries; state-invalidated retains one preseed-aware restart boundary
  - unfinished and state-invalidated no-result closures remain exact, owner-scoped and outbound-free
  - no Intent, NextMove authority, schema, record family, Tool, Case, metric, threshold, architecture or generalization is introduced
  - downstream ownership/ordering is explicit and does not widen the current Composition two-file allowlist
  - independent local implementation review PASS with all findings closed
  - remote exact-head review PASS before merge
done_when:
  - reviewed feature PR merges by signed squash as B_C2_W12_R19R7G_EXECUTION_SEAM_RULING
  - downstream blocker-only Packets use that exact signed successor and R19R7 resumes all-27 immediately after their serial merge
rollback:
  - before downstream blocker products merge, revert this single-file Spec squash
  - after descendants exist, invalidate/revert descendants first; never add a Composition fallback, infer routing from message/Case/fixture, or weaken recovery CAS
handoff_to: tech-lead
handoff_format:
  - branch and exact base/head/tree plus Spec blob
  - actual changed files and allowlist containment
  - source alignment/diff checks and tests explicitly NOT_RUN
  - blocker-only contract ruling plus exact downstream Packet order and remaining R19R7 red
```
