---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R4E2A
type: application-evidence-readiness-contract
depends_on: [02-18R19R4E1]
files_modified:
  - src/mini_agent/application/records.py
  - src/mini_agent/application/ports.py
  - tests/component/application/test_record_contracts.py
  - tests/component/application/test_ports_contract.py
requirements: [E2E01-01, E2E01-02, E2E01-03, E2E01-04, E2E01-05, E2E01-06]
---

# 02-18R19R4E2A｜W12 Request Understanding v3 evidence / readiness Application contract

```yaml
task_id: 02-18R19R4E2A
goal: 在Application ownership内补齐已冻结activation barrier所需的最小v3 owner-scoped exact evidence与global readiness Port合同，使后续Infrastructure能在单一snapshot返回完整Run+RU v3闭包并阻断任何v2 residual；不实现数据库、迁移、active route或Eval切换。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-ru-v3-evidence-contract
base_branch: integration/e2e01-cycle2
base_sha: c194c6663535913ea2458045eab547a4deaa1fe9
base_tree: 457650d156e8d6def2002c4bb6a896f54c5c6083
worktree_id: e2e01-cycle2-w12-ru-v3-evidence-contract
agent_role: runtime-engineer
writer: w12-ru-v3-evidence-contract-owner
owned_files:
  - src/mini_agent/application/records.py
  - src/mini_agent/application/ports.py
  - tests/component/application/test_record_contracts.py
  - tests/component/application/test_ports_contract.py
forbidden_files:
  - all files outside owned_files
  - src/mini_agent/core/**
  - src/mini_agent/application/persistence.py
  - src/mini_agent/application/agent_run_service.py
  - src/mini_agent/infrastructure/**
  - src/mini_agent/evaluation/**
  - alembic/**
  - tests/integration/**
  - tests/e2e/**
  - evals/**
  - docs/**
  - .planning/**
  - AGENTS.md
  - graphify-out/**
required_contract_blobs:
  docs/implementation/e2e01-thin-slice-implementation-spec.md: f145f31983022167dae077dc956f13365cf5ff13
  docs/implementation/e2e01-cycle2-implementation-spec.md: 7d62c6990cb4b742071262d2f0d9a52727763b0a
  docs/architecture/intent-design-reference.md: 8fe1120a49ba2b4b4c12f428ada9e199472a0c
  docs/architecture/memory-design-reference.md: fdcdd3c751eb03b7462257cfd62354c9c2917a69
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
required_dependency_blobs:
  .planning/phases/02-cycle-2-e2e-01/02-18R19R4E1-PLAN.md: f5b02d7fde3a231971eacb5f38629016a73e1fb5
  src/mini_agent/application/persistence.py: d556bfd98f4e6d79ddc2ba888b0a1cd1cc8b35e8
  src/mini_agent/core/request_processing.py: 6183510fce33049758ffec9d19ce7e32b6e4dd36
  src/mini_agent/core/task_state.py: 3a279b9156eb03768d58c5aa7029479c1da9a974
required_source_blobs:
  src/mini_agent/application/records.py: b044e550990d932f0d650038237dc17f00380552
  src/mini_agent/application/ports.py: 40d1de351aff85bc5972676a444162aa5db24c5a
  tests/component/application/test_record_contracts.py: f68b0c04c9a2613af7780e2c3b379a2d739f7ec3
  tests/component/application/test_ports_contract.py: d2c4689a4e28213ef2a3036d1b2fb4ca59bd7825
preflight_facts:
  - R4E1已合并exact v2 source / v3 target codec与六个v3 staging write commands/Port；active registry、runtime route与existing exact evidence reader仍是pre-cutover状态
  - Cycle2ExactRunEvidenceClosure当前没有Request Understanding parent/child字段；PostgreSQL exact Run reader因此无法向R4F提供v3 actual evidence
  - 现有load_cycle2_exact_run_evidence_for_owner没有v3-only guarantee，且仓库没有在active switch前证明全库v2 RU residual为零的Application Port
  - Eval当前仍静态绑定request_understanding_records_v2 / accepted_task_deltas_v2；R4F只能在owner-scoped v3 reader与readiness合同先合并后安全切换
  - 把Application evidence DTO/Port直接塞进Infrastructure migration Packet会跨ownership boundary，故它是R4E2B前的真实W12 blocker，不是架构扩张
packet_split_ruling:
  - 原R4E2 Infrastructure Plan因缺少owner-correct v3 evidence/readiness surface被独立review判定BLOCK并废弃，未commit、未发布、未执行
  - R4E2A只拥有Application evidence closure与Port signature；R4E2B随后单独拥有models/PostgreSQL/migration及integration evidence，两包串行review/merge
  - R4E2A不实现adapter、不访问数据库、不定义Alembic revision、不修改Eval或active caller；R4E2B不得回改本包合同
  - R4F只在R4E2B reader/readiness通过后切换Eval fields与actual evidence；Composition最后串行协调Application/Infrastructure/Eval owner-local active switches并调用同一readiness fence
contract_freeze:
  baseline: 253bb6bd8dc2f84d6ca95a98f625d80fa80e5461
  status: FROZEN
  allowed_changes: only the missing v3 evidence/readiness contract required by the frozen activation barrier, 27 Cycle 2 Cases, 16 Phase 1 variants and Composition
  forbidden_expansion: new schema version, record family, Case, metric, feature, generic evidence API, architecture abstraction, database surface, refactor or non-blocking optimization
  code_freeze_gate: after reviewed R19R5R merge
owner_decisions:
  - extend Cycle2ExactRunEvidenceClosure by exactly one default-empty request_understanding_closures tuple whose element type is the already-frozen exact RequestUnderstandingClosureV3; do not duplicate parent/child models or introduce v2/v3 unions
  - each non-empty tuple element contains exactly one RequestUnderstandingRecordV3 plus its parent-version-selected AcceptedAddGoalTaskDeltaV3 / AcceptedSupplyInputTaskDeltaV3 children in Candidate ACCEPT order; the existing RequestUnderstandingClosureV3 remains the sole local parent/child validator
  - the enclosing evidence closure validates unique parent and accepted-delta identities, root/supporting run membership, message membership, Task/RequestUnit effect closure and exact Binding-ref membership against the same returned Run graph; it accepts no v2 parent/child, alias, expectation, Case, grader or reconstructed authority
  - across every RU parent from the root and supporting Runs, reconcile accepted-child edges with task_state_transition_records into one canonical timeline per Task. The applicable RunTaskLink base is the disclosed starting fence; distinct effects cannot overlap or reuse a base/result edge, gaps are forbidden, and the terminal result must equal the exact current TaskRecord/RequestUnitRecord state_version and applicable RunTaskLink result. An accepted edge and transition may represent the same logical increment only when every shared Task/RequestUnit/base/result/time/reason relation is exact; no duplicate may hide a conflicting effect
  - no-task/all-reject closures may have zero children; accepted generic multi-Candidate, Cycle 2 initial and SUPPLY_INPUT closures preserve every child and cannot be flattened to one optional delta
  - add load_cycle2_exact_run_evidence_v3_for_owner(owner_scope, run_id) to Cycle2RuntimeRecordPort as an explicit inactive staging read. One call must later return the entire existing Cycle2 exact evidence plus all applicable RU v3 closures from one transactionally consistent owner-scoped snapshot
  - the staged v3 reader contract uses only TrustedOwnerScope and run_id; absent/unauthorized remains indistinguishable None, while selected-owner version/decode/provenance/reference/cardinality/closed-set failure raises the existing bounded P0PersistenceIntegrityError
  - the staged reader must revalidate each Candidate/Input source span and SHA-256 against authoritative Message content inside that same owner-scoped snapshot; codec success, physical owner, provenance content and complete graph are separate required gates
  - the staged reader forbids v1/v2/future RU in the selected active Run graph, fallback, read-time conversion, repair, a second Port call or stitching grader evidence from independent snapshots
  - add assert_request_understanding_v3_ready() to Cycle2RuntimeRecordPort as an internal no-argument readiness fence. Success returns None only when the approved physical cutover is present, active v2 RU residual is globally zero, and every active v3 parent/child exact-decodes with owner/reference/provenance closure in one consistent database fence
  - readiness failure raises the existing bounded persistence integrity error and never returns a boolean/count that callers could reinterpret. It accepts no user/model owner, Case, expectation, bypass flag or repair option and grants no dispatch/result authority
  - readiness may admit the exact archival v1 family but never treats it as active or as a substitute for v3; empty active RU data is ready only when the approved physical v3 cutover itself is verifiably installed
  - preserve the existing load_cycle2_exact_run_evidence_for_owner behavior and every active v2 writer/caller byte-for-byte in R4E2A; neither new method is called before R4E2B/R4F/Composition barriers
implementation_tasks:
  - add the single typed request_understanding_closures field and exact enclosing Run/message/effect/Binding identity validators to Cycle2ExactRunEvidenceClosure, including the cross-parent per-Task timeline from each disclosed RunTaskLink base through the current Task/RequestUnit/link result fence
  - add the staged v3 exact-evidence and readiness method declarations/docstrings to Cycle2RuntimeRecordPort without changing existing method signatures
  - add component record tests for zero, generic no-task/multi-accept, Cycle 2 initial and SUPPLY_INPUT evidence; reject v2 models, duplicate identity, wrong run/message/task/request-unit/binding membership, partial children and reconstructed/extra fields
  - add explicit two-parent same-Task negatives for overlapping accepted edges, a base/result gap, wrong current Task/RequestUnit/link terminal version and an accepted-edge/TaskStateTransition mismatch; each individual RU closure must remain locally canonical so the test isolates the enclosing evidence timeline gate
  - add Port contract tests for exact method names/signatures/return types, TrustedOwnerScope requirement, no expectation/Case inputs, no readiness inputs/result and unchanged existing active method declarations
contract_changes: IMPLEMENTS_APPROVED — exposes only the missing Application evidence/readiness surface explicitly required by both frozen Specs and R4E1 handoff. No canonical contract, business behavior or active route changes.
security_impact: POSITIVE — one owner-scoped snapshot, exact typed v3 closure, provenance-content verification contract and global zero-v2 readiness prevent cross-owner evidence stitching, version fallback and false readiness.
eval_impact: CONTRACT_PREREQUISITE_ONLY — provides the typed source R4F must consume but changes no EvalEvidence field, Case, dataset, metric, grader, provider, artifact or lifecycle.
review_profile:
  planning_review: INDEPENDENT_EXACT_FILE
  implementation_review: W12_RU_V3_EVIDENCE_CONTRACT_EXACT_HEAD
  targeted_risk_checks:
    - exact four-file Application allowlist and base/blob containment
    - no duplicated RU model/codec, v2/v3 union, expectation-bearing reader, two-call evidence stitching or user-controlled readiness
    - evidence closure proves Run/message/Task/RequestUnit/Binding membership and one non-overlapping, gap-free per-Task effect timeline through the current terminal version without inventing Infrastructure completeness
    - active reader/writer/service/Eval routes remain unchanged and new methods are staging-only
    - downstream R4E2B has an implementable single-snapshot reader and global readiness fence
required_checks:
  - uv run pytest tests/component/application/test_record_contracts.py
  - uv run pytest tests/component/application/test_ports_contract.py
  - uv run pytest tests/component/application
  - git diff --check
  - repository-wide read-only impact scan for Cycle2ExactRunEvidenceClosure constructors, Cycle2RuntimeRecordPort implementers/callers, EvalEvidence consumers and PostgreSQL evidence/readiness blockers
full_suite_gate: DEFERRED_TO_FINAL_REFROZEN_02_18
done_when:
  - focused and complete Application component suites pass
  - exact four-file scope is proven and all current active routes remain unchanged
  - local independent implementation review and remote exact-head review are PASS with zero open findings
  - product merges as B_C2_W12_RU_V3_EVIDENCE_CONTRACT and only then 02-18R19R4E2B planning begins
rollback:
  - before R4E2B, revert the four-file inactive contract atomically; no data, migration or runtime rollback is needed
  - after R4E2B/R4F but before active cutover, first remove their consumers/implementations in reverse owner order, then revert R4E2A
  - after Composition activates v3, stop v3 writes/dispatch, satisfy the exact Infrastructure downgrade gate and reverse Eval/Infrastructure/Application active switches before removing this evidence/readiness contract
  - any non-reversible v3-only data blocks rollback with zero writes; do not delete evidence, re-enable a v2 reader or bypass readiness
handoff_format:
  - exact branch/base/head/tree and changed files
  - exact evidence field/validator and Port signatures, commands/results/NOT_RUN and impact scan
  - unchanged active routes, R4E2B implementation blockers, risks and rollback
output_barrier: B_C2_W12_RU_V3_EVIDENCE_CONTRACT
handoff_to: tech-lead
```
