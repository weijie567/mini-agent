---
phase: 02-cycle-2-e2e-01
plan: 02-18R19R7E
type: correction
depends_on: [02-18R19R7, 02-18R19R7D]
files_modified:
  - src/mini_agent/infrastructure/persistence/postgres.py
  - tests/integration/test_postgres_v3_request_understanding_writes.py
requirements: [E2E01-05]
---

# 02-18R19R7E｜W12 Cycle 2 v3 exact Binding lineage reader blocker

> **CODE FREEZE / INFRASTRUCTURE SINGLE-WRITER BLOCKER PACKET**
>
> 本 Packet 只修复 R19R7 all-27 已复现的 v3 exact evidence Binding lineage
> 漏读。现有合同继续冻结；不得修改 schema、Port、Application、Composition、Core、
> Eval、Fixture、Case 或架构，非阻断优化全部延期。

```yaml
task_id: 02-18R19R7E
goal: 让既有 load_cycle2_exact_run_evidence_v3_for_owner 在构造 Cycle2ExactRunEvidenceClosure 时，从当前及既有业务记录直接引用的 InputBindingV2 出发，完整装载每条 supersedes 前驱链，使 Application 已冻结的 v3 Binding root/cycle/closed-set 校验能够在真实 continuation run 上验证完整持久化证据，而不是因 reader 漏行拒绝合法图。
repository: weijie567/mini-agent
remote: origin
head_branch: codex/e2e01-cycle2-w12-r19r7e-infrastructure-binding-lineage
base_branch: integration/e2e01-cycle2
base_sha: 29b9c15e791038dd521419c6a73d41fee36a7854
base_tree: 6f64fff3fc829b1609002d27bff3186fc6ddaa98
planning_branch: codex/e2e01-cycle2-w12-r19r7e-plan
worktree_id: e2e01-cycle2-w12-r19r7e-infrastructure-binding-lineage
agent_role: infra-engineer
writer: cycle2-postgres-exact-reader-owner
pull_request_base: integration/e2e01-cycle2
pull_request_mode: draft until independent local and remote exact-head review PASS; never use admin bypass
owned_files:
  - src/mini_agent/infrastructure/persistence/postgres.py
  - tests/integration/test_postgres_v3_request_understanding_writes.py
forbidden_files:
  - all repository files outside the exact two-file owned_files allowlist
  - AGENTS.md
  - PROJECT_DIRECTION.md
  - docs/**
  - .planning/**
  - alembic/**
  - src/mini_agent/application/**
  - src/mini_agent/bootstrap.py
  - src/mini_agent/core/**
  - src/mini_agent/evaluation/**
  - tests/integration/test_offline_composition_root.py
  - tests/component/**
  - graphify-out/**
canonical_inputs:
  - existing InputBindingV2.supersedes semantics
  - existing Cycle2ExactRunEvidenceClosure v3 Binding root, cycle and closed-set validators
  - existing owner-scoped PostgreSQL exact record decoder and integrity categories
  - 02-18R19R7 native v3 Composition contract
required_product_blobs:
  AGENTS.md: e8721c7845b36d2671b317a0dc1cd44f1b52ee2e
  src/mini_agent/infrastructure/persistence/postgres.py: 11b28b592e6b2b83b16e19a17b385ba84a5e38ed
  tests/integration/test_postgres_v3_request_understanding_writes.py: a837abd0c6ac808c2f0fd8bc4d5650296a2af966
  src/mini_agent/application/records.py: bec405f96ca593dd18decd8fb613973b29d34960
  .planning/phases/02-cycle-2-e2e-01/02-18R19R7-PLAN.md: 7ff27335a18c7de0039839667c8d8fcc2a33ed49
  .planning/phases/02-cycle-2-e2e-01/02-18R19R7D-PLAN.md: 0fda62d50cce2e6126cfe5b9a958bfba1b8c2c4b
required_planning_control_blobs:
  .planning/GOVERNANCE.md: f5ec0fe4b83a23b590457e0b407f3f9135dbf4f7
  .planning/ACTIVATION.md: 94a5bc82d4be7b59eeafc1fabab7e68c790ad610
dependencies:
  - exact GitHub verified signed R19R7D successor = 29b9c15e791038dd521419c6a73d41fee36a7854
  - exact downstream red = R19R7 all-27 fails at E2E01-05/order-only-no-shipment while load_cycle2_exact_run_evidence_v3_for_owner builds the continuation exact closure
  - traced underlying ValueError = Cycle 2 v3 RequestUnderstanding Binding root mismatch
  - traced persisted facts = current order_id Binding a6300ee1-55b8-4387-99b2-e0a560b2fe7d supersedes 662a5005-2b60-4f3c-8228-75f296f87396, while the reader returned only the current Binding plus the unrelated current product_description Binding
implementation_decisions:
  - preserve the current first-hop selection: current RequestUnit refs plus CandidateSet query refs, ordinal Selection ref, ShipmentAssessment claim ref and ToolCall argument refs remain the only roots admitted to the exact Binding set
  - only when include_v3_request_understanding is true, follow InputBindingV2.supersedes recursively from every admitted root until None; load each predecessor by exact binding_id through the existing owner-scoped _cycle2_row decoder
  - deduplicate by binding_id and terminate traversal for an already loaded id; do not suppress a persisted cycle, because the existing Application v3 validator must still receive the closed cycle and reject it deterministically
  - an absent or owner-unresolvable predecessor remains LINK_PROJECTION_MISMATCH; schema, model and physical-projection corruption must preserve the existing exact category emitted by _cycle2_row; do not catch/remap decoder integrity errors, synthesize, truncate, infer or fall back to current-only evidence
  - do not broaden the root set, load unrelated historical Bindings, change current-session reads, or alter legacy load_cycle2_exact_run_evidence_for_owner behavior
  - add focused PostgreSQL regressions using the existing accepted continuation fixture: prove the pre-fix reader rejects a legitimate superseding Binding lineage, then prove the v3 reader returns current plus every rooted predecessor exactly once and the closure validates
  - add named/parameterized negative and no-expansion regressions for an absent predecessor, an owner-unresolvable predecessor, a self-cycle, a multi-node cycle, an unrelated historical Binding row, the legacy exact reader and the current-session reader; add a malformed schema/projection case only by asserting the existing decoder category without remapping it
  - retain existing owner-scope, physical projection, source Message, v3 readiness and Application closed-set validation without duplication in Infrastructure
contract_changes: NONE — this is a reader completeness correction for the already-required InputBindingV2 supersedes closure; no public field, method, schema, migration, record family, Case, metric or behavior is added.
security_impact: POSITIVE — exact evidence includes the persisted owner-scoped predecessor required to prove lineage; missing, cross-owner, malformed and cyclic evidence remains fail closed through existing deterministic gates.
eval_impact: BLOCKER_ONLY — unblocks the existing E2E01-05 Composition Case; no Eval artifact, Case, grader, predicate, threshold or lifecycle change.
verification:
  preflight: exact signed base/tree/blobs and clean isolated worktree
  red: named accepted-continuation exact-reader regression fails with existing Binding root mismatch before the product change
  focused: uv run pytest -q tests/integration/test_postgres_v3_request_understanding_writes.py -k 'cycle2_v3_exact_binding_lineage'
  neighbor: uv run pytest -q tests/integration/test_postgres_v3_request_understanding_writes.py
  composition_probe: NOT_RUN in this Packet; R19R7 reruns E2E01-05 pair then all-27 only on the signed R19R7E merge successor
  diff_check: git diff --check exact-base...HEAD
  allowlist: exact two-file containment
  full: NOT_RUN; final refrozen 02-18 owns canonical full
required_checks:
  - current and recursively superseded Binding records are present exactly once in v3 Cycle2 exact evidence
  - named regressions prove absent and owner-unresolvable predecessor evidence fails closed as LINK_PROJECTION_MISMATCH, while malformed schema/model/projection evidence preserves the exact existing decoder integrity category
  - named regressions prove self-cycle and multi-node cycle evidence reaches the existing Application cycle validator and fails closed without traversal looping
  - a named regression proves unrelated historical Binding rows are not admitted
  - named regressions prove the legacy Cycle2 exact reader and current-session closure behavior are unchanged
  - no Port/Application/Composition/Core/Eval/Fixture/schema/migration/doc change
  - focused and PostgreSQL v3 RequestUnderstanding neighbor tests PASS
  - independent local implementation review PASS with all findings closed
  - remote exact-head review PASS before merge
done_when:
  - reviewed feature PR merges by signed squash as B_C2_W12_R19R7E_EXACT_BINDING_LINEAGE
  - R19R7 Composition rebases from that exact signed successor and resumes fail-fast at the E2E01-05 pair, then all-27, without widening its two-file allowlist
rollback:
  - before Composition merge, revert the exact two-file Infrastructure squash atomically
  - after descendants exist, invalidate/revert descendants first; never weaken Application lineage validation or add a Composition fallback
handoff_to: tech-lead
handoff_format:
  - branch and exact base/head/tree plus both file blobs
  - actual changed files and allowlist containment
  - red/focused/neighbor results and downstream Composition probe explicitly NOT_RUN
  - contract_changes NONE and remaining W12 blocker handed back to R19R7
```
